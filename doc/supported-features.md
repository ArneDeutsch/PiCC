# Supported features

> **Generated file — do not edit by hand.** This matrix is generated from the living capability registry (`src/registry/capability-registry.ts`), the single source of truth for what PiCC supports. The same registry drives the runtime `/doctor` report and this generated matrix, keeping both surfaces anchored to the same support claims.
>
> **Claude Code baseline:** `claude-code-2.1.x (mid-2026)`. Every support claim is stated relative to this
> baseline; anything upstream added after it is treated as *unassessed* and degrades safely.
>
> **Regenerate:** `node scripts/gen-capability-matrix.mjs`

## Tier legend

| Tier | Meaning |
|---|---|
| full | Implemented for real; every field the format defines is functional. |
| partial | Works within limits — parsed and matched, but a constraint applies (see the note). |
| degraded-noop | Parsed and reported, then a visible, documented no-op. Never crashes. |
| not-supported | The capability behavior is unavailable in PiCC; any recognized input degrades as its note describes. |
| n/a | Not applicable to this harness. |

Tier and Note describe PiCC behavior. **Claude evidence** describes only the upstream Claude Code surface: documented = stated by an allowlisted official page; observed = reproduced on the named Claude version and bounded path; inferred = reasoned from indirect evidence; unverified = the reviewed evidence does not establish the behavior. Multiple qualities on one row are intentionally mixed; a blank cell means the entry was not part of this audit.
The structured official-document review horizon is **2026-07-31**.
**Related references** are navigation and context only; search this document or the registry by ID. They do not imply a shared tier, evidence quality, or dependency. A separate ⚠ marker means PiCC fails to enforce an upstream restriction or mandatory gate, allowing an operation that should be restricted to run freely; `/doctor` labels detected project-specific safety findings.

## Tools (40)

Built-in tool names a project can reference in `tools:`, `permissions.*`, or a hook `if:`.

| ID | Tier | Claude evidence | Related | Note |
|---|---|---|---|---|
| `tool.Bash` | full |  |  | real implementation (from Pi) — shell execution, bash + PowerShell aware; PiCC exports CLAUDE_PROJECT_DIR (the project root, NOT the live worktree cwd) into every Bash subprocess, main and subagent alike, but deliberately disables Pi's PI_SESSION_ID/PI_SESSION_FILE/PI_PROVIDER/PI_MODEL/PI_REASONING_LEVEL projection so Claude-targeted commands do not acquire Pi-only session metadata. Built-in Bash keeps Pi's own 50 KB tail-truncation and is NOT reshaped by the oversized-result clip backstop, which covers the Claude-named/subagent/MCP outputs Pi does not bound — see feature.tool-output-clip |
| `tool.Edit` | full |  |  | real implementation — exact-string replacement edits |
| `tool.EnterWorktree` | full |  |  | creates/re-enters .claude/worktrees/<flat>/ and swaps the session cwd |
| `tool.ExitWorktree` | full |  |  | keep\|remove lifecycle with cwd restore, Windows-tolerant removal |
| `tool.Glob` | full |  |  | real implementation — file pattern matching |
| `tool.Grep` | full |  |  | real implementation — Claude-baseline parameter surface (-n, -A/-B/-C/context, -i, -o, type, glob, multiline, content/files_with_matches/count modes, head_limit/offset) with ripgrep/JS engine parity. A Grep result the oversized-result clip backstop reshapes carries a Grep-specific recovery hint — re-run with a tighter pattern or a smaller head_limit/offset — see feature.tool-output-clip |
| `tool.MultiEdit` | full |  |  | real implementation of the historical Claude Code MultiEdit — NOTE the pinned Claude Code baseline no longer ships MultiEdit — removed in the 2.0 line, confirmed gone by v2.0.8 — so `full` means faithful to the pre-removal contract, a superset of the pinned baseline kept as an older-project compatibility courtesy, NOT 'matches current Claude Code': batched, strictly exact-string edits (no fuzzy fallback, unlike PiCC's Edit) applied sequentially to one running buffer (each edit sees the prior edit's result), atomic (any miss rejects the whole batch, file left untouched), per-edit replace_all with unique-else-error, and an empty old_string on the first edit of a new file creates it |
| `tool.NotebookEdit` | full |  |  | real cell-level notebook editing — replace, insert, and delete require a successful notebook Read in the active conversation before mutation. Existing raw cells are addressable; omitting cell_type on replace preserves the existing type, including raw, while created/requested cell types remain code or markdown. Main conversations and ordinary child conversations keep independent authorization state; only a genuine inheriting main-session fork copies the parent branch's latest notebook snapshot. Every edit still revalidates the notebook's canonical file identity and exact bytes, so restored or forked authorization is never a write bypass. Authorization snapshots persist as bounded non-model-visible custom entries on a best-effort basis: after an append failure, resume can restore only the newest successfully persisted snapshot and may therefore lose an unpersisted revocation or positional-fallback-stale transition; identity and byte revalidation still applies to that older snapshot. |
| `tool.Read` | full |  |  | real implementation — text/image/notebook file reads with Claude-shaped input. A .ipynb renders CELL-AWARE (source + outputs), and a raster notebook output becomes a real image content block; an image FILE is delivered as a real image content block too — EXCEPT on a non-vision model, where both degrade to a model-visible text note (the vision-gate exception, never a silent drop or garbage). The image-FILE path is INHERITED from base Pi (not newly built here); PiCC adds the notebook and binary routing around it — see feature.read.images. An unsupported binary returns the Claude-style binary error instead of mojibake. IMAGE and BINARY classification is BYTE-BASED (magic bytes), not extension-based — a deliberate, slightly-more-correct divergence from Claude's extension-based classification; NOTEBOOK reads, by contrast, are keyed on the .ipynb extension (matching Claude's merged Read), so that routing is PARITY, not a divergence. Tier full for the text/image/notebook core ONLY; PDF reading is BELOW the Claude baseline and is tracked in its own entry — the deficit is disclosed at runtime via the Claude-style binary error (a PDF read returns "...looks like a PDF, which PiCC does not support reading yet") and via the support-matrix table / this registry entry, NOT named by /doctor (which lists only project-triggered findings) — see feature.read.pdf. Built-in Read is NOT reshaped by the oversized-result clip backstop: Pi's own 50 KB head-truncation applies below the clip budget, so the head+tail+recovery-hint clip covers only the Claude-named/subagent/MCP reads Pi does not already bound — see feature.tool-output-clip (which also discloses, without fixing, the separate pre-existing Pi-50 KB-vs-Claude-25k-token Read divergence) |
| `tool.Skill` | full |  |  | skill activation by name with argument substitution; bare Skill tokens `plugin`, `plugins`, and `reload-plugins` are reserved for the corresponding /plugin, /plugins, and /reload-plugins commands and cannot suffix-resolve to plugin content. /plugins is a PiCC-defined extension, not Claude parity. An otherwise-permitted explicit namespaced skill name remains activatable |
| `tool.TaskCreate` | full |  |  | current task-tracking surface |
| `tool.TaskGet` | full |  |  | current task-tracking surface |
| `tool.TaskList` | full |  |  | current task-tracking surface |
| `tool.TaskUpdate` | full |  |  | current task-tracking surface |
| `tool.WebFetch` | full |  |  | implemented for real — research skills and permission allowlists depend on it |
| `tool.WebSearch` | full |  |  | implemented for real — research skills and permission allowlists depend on it |
| `tool.Write` | full |  |  | real implementation — file creation/overwrite |
| `tool.Agent` | partial |  |  | subagent dispatch — built-in general-purpose/Explore/Plan + project agents, omitted type defaults to general-purpose; the final message is returned verbatim, EXCEPT a completed or truncated-completed resumable result appends a clearly-delimited in-band identity/resume trailer to the model-visible text (Claude-faithful — Claude Code likewise appends a resume handle to resumable results and none to one-shot Explore/Plan; the human TUI strips it, so a strict exact-token/JSON/YAML consumer must account for it); a terminal API error is a LOUD failure naming the cause (never an empty success), partial output preserved with a cut-off note (2.1.199/2.1.200). A malformed terminal `pending` is likewise a loud incomplete failure with retained partial output, never a normal-looking success. POST-COMPACTION CANCELLATION HARDENING: confirmed retained input is exposed through one canonical report identity shared by foreground results, background settlement, TaskOutput, and the TUI panel; unconfirmed records remain quarantined and are not claimed or released twice. STATE-AWARE RECOVERY is PiCC reliability UX, not verified Claude Code recovery-policy parity: after Pi-owned retries settle, a fresh explicit Agent/Task dispatch is favored only when complete lifecycle observation proves a transient-category failure had no successful assistant response, retained model/tool-call content, or started tool execution. Observed progress or incomplete lifecycle evidence takes the conservative branch: SendMessage is favored only when the result says the agent is factually resumable; otherwise retained work and possible side effects require review before another explicit dispatch. Non-transient or unclassified failures recommend neither blind replacement nor resume. Guidance and resumability are separate, one structured disposition and fixed formatter feed foreground, background settlement, and TaskOutput, and PiCC takes no automatic action; every resumable dispatch gets a stable agent id; in the interactive TUI, for main-session/depth-1 background work, a successful acceptance is transient in human chat, while a normal-path result replaces its pending call in the same tool row; the first terminal delivery through TaskOutput or settlement creates one concise semantic record. The configured `app.tools.expand` action (Ctrl+O by default) reveals retained output and operational detail; an unbound action fails open. At unusable widths, detail waits for widening; a resize prompt appears only when it fits. A later already-reported retrieval adds no human row, while model-visible background dispatch still returns the task ID. Nested work at depth >= 2 instead keeps Pi's default notice box and appears in the panel tree and its parent's transcript. Interactive-TUI status-panel observability is defined by feature.background-agents; shared terminal recovery guidance also reaches print/RPC delivery; dispatch is BACKGROUND-BY-DEFAULT (Claude 2.1.198): an omitted run_in_background returns a task id immediately so an implicit-concurrency fan-out parallelizes, run_in_background:false selects a synchronous inline run, and CLAUDE_CODE_DISABLE_BACKGROUND_TASKS forces every dispatch foreground. An eligible uncollected current task pushes one bounded settlement notice on the idle parent's next turn; polling TaskOutput while running preserves eligibility, but successfully returning its terminal TaskOutput record counts as delivery and suppresses the redundant later notice. PARTIAL residual — PiCC's notice is next-turn; reporter observations anthropics/claude-code#21343 (Claude Code 2.1.20) and anthropics/claude-code#24752 describe late notifications while a conversation is active, but official docs define neither notification consumption nor exact mid-turn/next-turn timing, so reports establish no normative contract, so PiCC suppression is intentional UX hardening, not verified parity. INFERRED: PiCC fires no agent_completed-style Notification for in-session settlement (a conservative under-claim) and the env override takes precedence over background:true frontmatter (see feature.background-agents); a subagent_type:"fork" dispatch inherits the parent conversation instead of starting fresh — see tool.Agent.fork. MAIN-SESSION-ONLY BY DEFAULT: at the default subagents.maxDepth of 1, dispatched subagents do NOT recurse — they receive neither Agent nor Task and their prompt omits the subagents catalog; set subagents.maxDepth to a positive integer greater than 1 to opt into nested generations. This is PiCC's conservative default and `subagents.maxDepth` is its explicit nesting control. Named user/project agents may compose declared session references and agent-owned inline MCP capabilities before tool gating; bounded setup and cleanup qualification on Agent/TaskOutput results is PiCC-defined, not exact Claude result framing |
| `tool.Agent.fork` | partial |  |  | Task/Agent subagent_type:"fork" — VERIFIED behavior: inherits the parent conversation (full message history) + parent model + parent tools, seeded via SessionManager.forkFrom into a brand-new persisted child transcript; OUTPUT ISOLATION IS KEPT (only the fork's final message returns; its intermediate steps stay out of the parent conversation); MAIN-SESSION dispatch ONLY — a nested (depth ≠ 1) dispatcher visibly degrades to fresh context (never seeds the root conversation into a nested subagent), and every degrade surfaces a specific footer notice, never the generic unknown-type warning; the system prompt is a same-context RECONSTRUCTION from the parent's project rules/skills/memory/steering, NOT byte-identical (PiCC is an extension on a Pi-assembled base prompt), so a fork FORGOES the prompt-cache cost saving a real fork gets; gated by CLAUDE_CODE_FORK_SUBAGENT (=1 forces on / present-but-off like =0 forces an explicit visible degrade / UNSET ⇒ ENABLED — a deliberate PiCC parity choice over Claude's under-specified staged-rollout default, a DIRECTIONAL divergence: PiCC may inherit where a staged-rollout Claude with fork unset would run fresh); the inherited parent model is overridden by an operator CLAUDE_CODE_SUBAGENT_MODEL env AND by a per-call `model` argument on the fork dispatch (both disclosed so the "same model as parent" claim stays truthful); NON-RESUMABLE (the inherited context is the parent conversation at fork time and cannot be safely re-derived — SendMessage refuses it). PiCC-DEFINED / INFERRED limits: a fork CANNOT SPAWN ANOTHER FORK — a nested fork request is a visible refusal (INFERRED mechanism — enforced via a runtime-set dispatcher marker, not a tool parameter); print/headless/no-session forks degrade (no parent transcript to fork from); fork-mode's run_in_background removal is NOT adopted — PiCC keeps run_in_background:false as a synchronous selector and relies on background-by-default dispatch for the "all background" half; isolation:"worktree" on a fork is NOT honored (the fork shares the parent cwd); the Claude version gate (v2.1.117+) is not mirrored; as a non-resumable consumer of tool.Agent's failure presenter, progressed transient failures require review of retained work and possible side effects rather than a resume recommendation; a `name` does not affect inheritance and PiCC does NOT reproduce Claude's interactive named-fork zero-context regression (anthropics/claude-code#76019). Tier PARTIAL: the prompt reconstruction plus these deferrals |
| `tool.ListMcpResourcesTool` | partial | documented: MCP reference — Use MCP resources (2026-07-31) | `feature.mcp-resources`, `tool.ReadMcpResourceTool` | fixed-name model tool first registered and activated when the live main-session catalog gains an advertised resource capability, including through changed-definition administration and for an advertised-empty or `resources/list`-failed catalog. Host registration may persist across reconnect, terminal retention, or retirement while active exposure retires when no current main-session definition remains capable. It lists the current definition-bound resource snapshots across capable servers or one exact `server`; same-definition reconnect is immutable and admitted changed definitions refresh the live main-session catalog; sibling failures remain attributable. Ordinary built-in controls apply: bare-name and `*` deny, agent tools:/disallowedTools:, generic top-level `server:` deny/ask matching, and PreToolUse/PostToolUse/PostToolUseFailure hooks; `mcp__server` is NOT an alias. Main sessions, foreground subagents, and genuine conversation forks may receive it through normal gating, while Claude's built-in filter removes it from non-fork background subagents. Unfiltered presentation is capped at 256 servers and 1,024 resources per server, with visible omissions; exact filtered routing still covers the complete bounded snapshot. Output is untrusted, control-neutralized, and bounded by clipMaxTokens. PARTIAL because resource templates, subscriptions/list_changed refresh, and resource attachment UI are absent |
| `tool.ReadMcpResourceTool` | partial | documented: MCP reference — Reference MCP resources (2026-07-31) | `feature.mcp-resources`, `tool.ListMcpResourcesTool` | fixed-name model tool registered and activated under the same first-live-capability condition as ListMcpResourcesTool; host registration may persist while active exposure retires with the last capable main-session definition. Same-definition routing remains immutable while an admitted changed definition refreshes the live catalog. It requires exact `server` and opaque `uri`, forwards the URI unchanged only to that selected current client, and may read an unlisted URI. Text is returned as bounded labeled text; valid binary is complete labeled base64 only when it fits, while malformed or oversized binary receives a visible omission/error rather than native image delivery, invalid clipped base64, disk persistence, or ambient fetching. Ordinary built-in controls apply: bare-name and `*` deny, agent tools:/disallowedTools:, generic top-level `server:`/`uri:` deny/ask matching, and PreToolUse/PostToolUse/PostToolUseFailure hooks; `Read(...)` and `mcp__server` are NOT aliases. Main sessions, foreground subagents, and genuine conversation forks may receive it through normal gating; non-fork background subagents do not. Results are bounded by clipMaxTokens plus the 1,024-content safety limit and then traverse the generic tool-result clip backstop. PARTIAL because native binary/image delivery, resource attachment UI, and exact Claude formatting are absent |
| `tool.SendMessage` | partial |  |  | resumes any actually resumable completed or failed subagent by agent id in the background with prior context, or steers a running background one (Claude 2.1.x). CHECKPOINT-PAUSED PRECOMMIT RECOVERY: foreground and background dispatches exhausted before commit are retained by agent id; after repairing the operational or hook cause, awaited SendMessage compacts/continues that live agent and returns its result directly, with no TaskOutput or new task generation. POSTCOMMIT CHILD TERMINATION IS FINAL: an exact authenticated resumed cancellation may produce a canonical retained-input report, but that terminal child is not resumable and must be replaced; unconfirmed input remains quarantined. Ordinary resumed failed/completed agents return to running (2.1.205), create a new task generation, and supersede older generations for settlement delivery (newest generation wins). An eligible uncollected current resumed task receives the same conditional next-turn notice; terminal TaskOutput collection suppresses it and running polls do not. A confirmed USER-initiated stop from the status panel marks both the agent and linked background task terminal and is permanent and PiCC-defined: SendMessage refuses to steer or resume that terminal user-stopped agent with a distinct refusal, unlike a model TaskStop. An accepted generation still waiting for configured capacity visibly refuses steering until admitted; this is PiCC-defined because Claude's queue behavior is undocumented. GAPS: PiCC allows resume after TaskStop (a model stop), while the Claude Code 2.1.x reference refuses stopped-agent resume; no cross-restart resume; steering is background-only (panel drill-down steering rides the same steer path); idle-parent settlement delivery is next-turn; fork dispatches are non-resumable (see tool.Agent.fork), and agentOverride and subagent-to-subagent/agent-teams messaging are unsupported. For ordinary resume, the acknowledgment includes the new task id, resolved registry name, and stable agent id in PiCC-defined model-visible wording, not verified as exact Claude wording |
| `tool.SlashCommand` | partial |  |  | thin alias over the skill-activation path (mirrors the Skill tool): parses a leading /name (incl. plugin-namespaced /plugin:name) + trailing args from the command string and activates the resolved skill; an unknown or model-blocked command throws a model-visible error like the Skill tool. Reserved Pi/PiCC names are rejected case-insensitively; an ordinary colliding skill remains available only through direct Skill invocation when its model-invocation metadata permits, except the bare Skill tokens `plugin`, `plugins`, and `reload-plugins` are reserved in both tools for the corresponding slash commands and require an explicit namespaced skill invocation. /plugins is a PiCC-defined extension, not Claude parity. PARTIAL: covers other user-defined skills/commands but NOT the built-in commands Claude 2.1.x can also invoke via the Skill/SlashCommand skill-activation path (/init, /review, /security-review) — PiCC ships no such built-ins; other built-ins (/clear, /compact, ...) are non-model-invocable in Claude too |
| `tool.Task` | partial |  |  | alias of the Agent subagent-dispatch tool — it shares Agent's canonical retained-input report/quarantine identity; its terminal failures with a structured recovery disposition carry tool.Agent's canonical state-aware guidance with the same loud-failure/agent-id semantics, condensed-record/status-panel rendering contract, and background-by-default routing (Claude 2.1.198); its next-turn settlement notice is conditional on an eligible current task remaining uncollected (running polls preserve it; terminal TaskOutput collection suppresses it). Reporter-observed Claude Code 2.1.x may instead enqueue a redundant post-retrieval notification, so this is PiCC UX hardening rather than verified parity; subagent_type:"fork" inherits the parent conversation — see tool.Agent.fork. MAIN-SESSION-ONLY BY DEFAULT: at the default subagents.maxDepth of 1, dispatched subagents do NOT recurse (no Agent/Task and no subagents catalog); set subagents.maxDepth to a positive integer greater than 1 to opt into nesting — a deliberate PiCC extension. This is PiCC's conservative default and `subagents.maxDepth` is its explicit nesting control. Named-agent MCP scope and result qualification follow tool.Agent |
| `tool.TaskOutput` | partial |  |  | retrieves background subagent results (wait or poll) from the background-task registry, reusing the shared subagent renderer: while the task is waiting for configured capacity, an in-progress await shows waiting in human/streaming partial output; polling, or an interrupted await, returns waiting to the model in PiCC-defined wording. A completed await instead returns the canonical terminal result; completed or truncated-completed resumable results may carry identity framing. Failed results with a structured recovery disposition carry the separate state-aware guidance canonically defined by tool.Agent. Once admitted and still running it renders a concise status line that retains the explicitly requested target ID alongside agent identity/state and usage when known; interactive-TUI status-panel observability is defined by feature.background-agents, and an await resolves in the same call to the canonical terminal result. For a main-session retrieval of depth-1 work, the FIRST terminal delivery renders one bounded PiCC-defined semantic record when no settlement record was emitted yet: it prioritizes the truthful outcome marker, agent identity and textual state, actionable exceptional evidence, dispatch description, expansion cue, and duration as width permits. The configured `app.tools.expand` action (Ctrl+O by default) reveals retained output and operational detail; an unbound action fails open. At unusable widths, detail waits for widening; a resize prompt appears only when it fits. Retrieval AFTER an emitted terminal record is suppressed from the main-session human TUI, never adding a reference or duplicate row; the registry and canonical identity remain unchanged; a poll (wait:false) preserves settlement-notice eligibility; successfully returning any terminal record counts as delivery and suppresses a redundant not-yet-sent notice, including a cut-off result (all output available for that run was delivered). Retrieval remains available after a notice and does not re-arm it. This PiCC-defined collection-aware lifecycle is intentional UX hardening: reporter-observed Claude Code 2.1.x can enqueue a redundant notification after retrieval, while public docs do not specify notification-consumption semantics and available reports establish no exact normative background-subagent contract; it is NOT claimed as verified parity. PRE-EXISTING SCHEMA GAP: reporter evidence anthropics/claude-code#21343 shows Claude Code 2.1.20 TaskOutput using block:true, and anthropics/claude-code#76335 shows 2.1.206 local_agent using block:true with timeout, while PiCC exposes wait; official tools docs list TaskOutput and its deprecation but publish no parameter schema. This gap makes the tier partial. Agent-scoped MCP setup or cleanup degradation qualifies the canonical terminal retrieval outside the final-message body in PiCC-defined framing. The agent-<id> identity is carried on the expanded completion record INCLUDING for non-resumable one-shot builtins (transient status lines carry the Task/Agent chip), with no false 'resumable via SendMessage' invite; the render is display-only — the retained final-message body is unchanged, while model-visible completed or truncated-completed resumable retrieval may append the existing resume identity framing and compact usage metadata outside that body; a failed task with a structured recovery disposition reports failed status with separate state-aware guidance and any partial output (never an empty success), with identity/usage outside the verbatim body; a stopped terminal record reports the outcome but cannot recover deliberately discarded output. For a confirmed retained-input outcome, retrieval uses the canonical task or agent locator and returns the same ordered counts/report including its exact stage; count-only TaskStop, settlement, and panel notices also name that stage without consuming detail; quarantined unconfirmed input is never presented as confirmed custody. PiCC EXTENSION/DIVERGENCE: official Claude Code subagent documentation removes TaskOutput from every named subagent even when it is listed in `tools:`; PiCC intentionally exposes TaskOutput to subagents, scoped so a subagent reaches only tasks it dispatched while the coordinator reaches every session task. Foreign/unknown task_id is refused cleanly without leaking existence or data |
| `tool.TaskStop` | partial |  |  | stops a background subagent; confirmed retained-input records are stopped through the canonical owner exactly once, while quarantined unconfirmed records are skipped rather than disposed or released again. CHECKPOINT STOP EXCEPTIONS: while the originating process remains alive, TaskStop can abandon either a pre-commit compaction-paused dispatch or the exact current dispatch generation's authenticated post-commit resumed abort/join epoch; the latter remains nonterminal until its canonical cancellation disposition and linked background settlement join. Stable agent-id lookup is process-lifetime only, scoped nested callers reach only their own children, and stale task generations cannot stop a newer generation. Otherwise PiCC accepts only task_id, while Claude 2.1.198+ also accepts agent id/name. Stop is cooperative; the stopped status and discarded late result are PiCC-defined because Claude's post-stop result semantics are undocumented. Every result includes the task record's stored display type and stable agent id in PiCC-defined model-visible wording, not verified as exact Claude wording. Subagents INHERIT TaskStop (per Claude's sub-agents 'Available tools' list), but a subagent's TaskStop reaches only tasks it dispatched; the coordinator can stop any session task; a foreign or unknown task_id is refused cleanly and non-leakingly. This is the identical per-dispatcher guard as tool.TaskOutput — a faithful hardening of Claude's fresh-context isolation, stricter than Claude only on the #15098 coordinator-passed-id edge, NOT a blanket 'non-divergent' claim (see tool.TaskOutput) |
| `tool.TodoWrite` | partial |  |  | deprecated todo tool — mapped onto the Task* equivalents, not a native implementation |
| `tool.mcp__*` | partial | documented: MCP reference — Scale with MCP tool search (2026-07-31) | `feature.mcp-capability-discovery`, `feature.mcp-max-result-size-chars`, `feature.mcp-requires-user-interaction` | transport-neutral MCP tool proxies are exposed as mcp__<server>__<tool>. For one exact execution definition, the first successfully discovered name/schema/description catalog remains immutable across outages and reconnect; calls resolve the current client and use fixed local transient/terminal wording. An administratively admitted changed definition is rediscovered and refreshes main-session host registration (see feature.mcp-list-changed and feature.mcp-remote-transports). Tool names are sanitized underscore-style like Claude (binary-verified 2.1.218: characters outside [A-Za-z0-9_-] become "_"); a name EMPTY after sanitizing is dropped — PiCC's own floor, not observed Claude behavior — while SERVER names remain PiCC's stricter drop-with-diagnostic charset gate because "__" or grammar characters would break mcp__ permission matching. Deny rules at every grammar level (mcp__*, mcp__server, mcp__server__*, mcp__server__tool) are enforced; bare-name and server-level denies REMOVE tools from model context (Claude parity), with the guard's call-time deny as the backstop; deny matching is case-sensitive and allow/ask remain no-ops under PiCC's deny-only posture. After aggregate initial settlement, the main session can add, retire, or exact-definition-refresh registrations through serialized PiCC-owned active-set merging and call-time route validation. A selected main agent composes a session-owned universe from borrowed eligible globals and its own admitted inline runtime; Dispatched agents compose immutable dispatch-local universes by the same route rules. Both restrict/drop them via tools:/disallowedTools:, and parent or sibling inline capabilities do not propagate. Reconnect never widens a same-definition universe; changed-definition host refresh is PiCC-defined and never enters agent-inline catalogs. PiCC-only bounds, NOT parity: input schemas are normalized — valid bounded compileable root anyOf/oneOf/allOf combinators are preserved, while a $ref-bearing, non-object-root, compile-failing, or >32KB-serialized schema degrades to a permissive object schema with a diagnostic that is STDERR-ONLY currently (not in /doctor; the server still validates arguments), whereas Claude passes schemas through verbatim; descriptions are bounded at 2KB, also PiCC-only and NOT a containment guarantee because schema-embedded strings ride to the model unbounded by that cap (Claude parity); a registered name over the 64-char model tool-name limit is dropped with a stderr diagnostic; connect-time tools/list pagination stops after 16 pages, and tool-metadata diagnostics cap at 5 per server with an overflow summary. DEGRADED tool-result content: text blocks, embedded TEXT resources, and resource links join into one text result, while image/audio/binary blocks are omitted with a bounded note and structuredContent is ignored — Claude renders images natively and saves blobs to disk (deferred). This deny enforcement and subagent inheritance preserve the prior transport-neutral policy. Valid metadata, tool results, and model-facing protocol-result trust behavior remain server-controlled. Results flow through the tool-output clip backstop (see feature.tool-output-clip) |
| `tool.Artifact` | degraded-noop |  |  | callable no-op stub — Artifacts out of scope; the notice directs output to a regular file |
| `tool.AskUserQuestion` | degraded-noop |  |  | callable no-op stub — deliberately not provided; the notice redirects questions to plain chat |
| `tool.BashOutput` | degraded-noop |  |  | callable no-op stub — background shells not implemented; commands run in the foreground |
| `tool.EnterPlanMode` | degraded-noop |  |  | callable no-op stub — plan mode is a no-op; planning guidance is treated as ordinary instructions |
| `tool.ExitPlanMode` | degraded-noop |  |  | callable no-op stub — plan mode is a no-op; 'use plan mode' instructions are treated as guidance |
| `tool.KillBash` | degraded-noop |  |  | callable no-op stub — background shells not implemented; there is no shell to kill |
| `tool.KillShell` | degraded-noop |  |  | callable no-op stub — background shells not implemented; there is no shell to kill |
| `tool.LSP` | degraded-noop |  |  | callable no-op stub — LSP tooling out of scope; the notice directs Grep/Read navigation |
| `tool.NotebookRead` | degraded-noop |  |  | callable no-op stub — retired: notebook reading is merged into Read, which renders .ipynb cell-aware (source + outputs, image outputs as blocks on a vision model); a NotebookRead call returns a notice pointing at Read, so no capability is lost. The name is retained as a PiCC gating token — a deliberate PiCC safety choice, NOT Claude parity — so existing deny/allow/tools: references keep working, and a deny: NotebookRead(<glob>) still protects the notebook read now that it flows through Read |
| `tool.computer` | degraded-noop |  |  | callable no-op stub — computer use out of scope |
| `tool.ToolSearch` | not-supported | documented: MCP reference — Scale with MCP tool search (2026-07-31) | `feature.mcp-model-failure-visibility`, `feature.mcp-server-always-load`, `feature.mcp-tool-always-load`, `feature.mcp-tool-search` | Claude's documented MCP tool-search selector is absent; PiCC loads eligible schemas upfront, and absence of the selector cannot execute a denied named tool. In the main session this also leaves proxy names over the model's 64-character tool-name limit inert: they are dropped rather than recoverable through search. Selected-main setup degradation denies admission before provider work; dispatched-agent setup degradation instead reaches the child before its first request through fixed warning context, while dispatch cleanup can only qualify the result after the run |
| `tool.WaitForMcpServers` | not-supported | documented: MCP reference — Scale with MCP tool search (2026-07-31) | `feature.mcp-control-status`, `feature.mcp-model-failure-visibility`, `feature.mcp-server-always-load` | Claude's documented wait-for-MCP-startup fallback is absent. Ordinary main-session startup state stays human-only through /mcp and bounded input-time settlement; selected-main setup must settle before admission, while dispatched-agent setup settles before its first provider request and injects fixed warning context on degradation. Dispatch cleanup uncertainty arises only at result time |

## Hook events (20)

Lifecycle events the hooks engine can fire (`settings.json` `hooks`, plus skill/agent-scoped hooks).

| ID | Tier | Claude evidence | Related | Note |
|---|---|---|---|---|
| `hook.event.PostToolUse` | full |  |  | fires after successful tool calls; matcher + if: conditions honored; exit-2 block feedback is fed back to the model |
| `hook.event.PostToolUseFailure` | full |  |  | fires after failed tool calls |
| `hook.event.PreToolUse` | full |  |  | fires before each tool call; Claude matcher semantics (exact/list/unanchored-regex) + full stdin/stdout contract incl. deny + updatedInput |
| `hook.event.SessionEnd` | full |  |  | fires at session end |
| `hook.event.Stop` | full |  |  | fires when the main agent wants to stop; exit 2 blocks stopping |
| `hook.event.SubagentStart` | full |  |  | fires when a subagent is spawned; payload carries agent_id + canonical agent_type (plugin agents use <plugin>:<agent>) while transcript_path stays the MAIN session transcript (Claude Code parity); exit 2 is diagnostic and non-blocking, while universal continue:false terminates the dispatch |
| `hook.event.SubagentStop` | full |  |  | fires when a subagent wants to stop; payload carries agent_id + canonical agent_type (plugin agents use <plugin>:<agent>) while transcript_path stays the MAIN session transcript (Claude Code parity); exit 2 blocks and re-prompts the subagent (bounded) |
| `hook.event.UserPromptSubmit` | full |  |  | fires on user prompt; stdout injected as context (main-conversation prompts only — panel drill-down steering bypasses it, see feature.background-agents) |
| `hook.event.WorktreeRemove` | full |  |  | fires on worktree removal |
| `hook.event.PostCompact` | partial |  |  | fires only after compaction commits; main and child sessions surface diagnostics/systemMessage without injecting stdout/additionalContext into model context, ignore ordinary exit-2/decision:block control, and honor universal continue:false as a terminal post-commit stop before skill restoration. PARTIAL because the complete cross-scope contract is not verified |
| `hook.event.PreCompact` | partial |  |  | fires before compaction with manual\|auto trigger and can block; PARTIAL because complete payload/order/control/output semantics are not verified across both main and child sessions; PiCC deliberately fails a blocked proactive checkpoint closed |
| `hook.event.SessionStart` | partial |  |  | fires at session start and matches startup\|resume\|clear\|compact\|fork; Pi startup and reload reasons map to startup, new maps to clear, and resume/fork are preserved, while compact is emitted synthetically after compaction commits. For compact-source main and child events, stdout/additionalContext is restored, ordinary exit-2/decision:block output is non-blocking, and universal continue:false terminates the committed-summary session. PARTIAL because the complete cross-scope contract is not verified |
| `hook.event.WorktreeCreate` | partial |  |  | fires after worktree creation and entry; PRE-EXISTING DIVERGENCE: ordinary nonzero/exit-2 hook outcomes cannot abort creation. Universal continue:false aborts subsequent run processing, while cleanup and the tool result remain truthful about the completed creation/entry. PARTIAL because the hook cannot control creation itself |
| `hook.event.Elicitation` | degraded-noop | documented: Hooks reference — Elicitation (2026-07-31) | `feature.mcp-elicitation`, `hook.event.ElicitationResult` | current MCP elicitation request hook — its server-name matcher and input contract are recognized, but the event never fires because server-initiated elicitation is unsupported; PiCC therefore cannot apply hook output or denial |
| `hook.event.ElicitationResult` | degraded-noop | documented: Hooks reference — ElicitationResult (2026-07-31) | `feature.mcp-elicitation`, `hook.event.Elicitation` | current MCP elicitation result hook — its server-name matcher and input contract are recognized, but the event never fires because PiCC produces no elicitation result; PiCC therefore cannot apply hook output or blocking-to-decline behavior |
| `hook.event.Notification` | degraded-noop |  |  | UI-notification event — parsed, never fired (no equivalent harness surface); background-subagent settlement does NOT fire an agent_completed Notification. An eligible uncollected current task is instead reported by PiCC's conditional next-turn settlement message (terminal TaskOutput collection suppresses that message). SubagentStop fires independently as part of settlement, not alongside or synchronously with that conditional notice |
| `hook.event.PermissionRequest` ⚠ | degraded-noop |  |  | tied to interactive permission machinery — never fired under the default-permissive posture |
| `hook.event.TaskCompleted` | degraded-noop |  |  | task-list event — parsed, never fired in v1 |
| `hook.event.TeammateIdle` | degraded-noop |  |  | agent-teams event — teams out of scope, parsed and never fired |
| `hook.event.mcp__elicitation` | degraded-noop | unverified: Historical PiCC alias; not present in the reviewed current hooks reference | `feature.mcp-elicitation`, `hook.event.Elicitation`, `hook.event.ElicitationResult` | historical PiCC compatibility alias for MCP elicitation hooks; it is parsed and never fired, and is not the current Claude Code hook contract |

## Settings (52)

`settings.json` / `settings.local.json` keys.

| ID | Tier | Claude evidence | Related | Note |
|---|---|---|---|---|
| `setting.autoMemoryDirectory` | full |  |  | overrides the auto-memory storage directory (~ and env expanded) |
| `setting.autoMemoryEnabled` | full |  |  | gates auto-memory loading (default true; CLAUDE_CODE_DISABLE_AUTO_MEMORY also honored) |
| `setting.claudeMd` | full |  |  | managed-scope inline CLAUDE.md content injected at highest priority; ignored with a diagnostic in other scopes |
| `setting.claudeMdExcludes` | full |  |  | excludes CLAUDE.md/rules files from loading |
| `setting.disableAllHooks` | full |  |  | disables all hook dispatch |
| `setting.disableSkillShellExecution` | full |  |  | disables !`cmd` skill shell injection |
| `setting.hooks` | full |  |  | hook config dispatched with Claude matcher semantics (exact/list/unanchored-regex), parallel execution with dedup, async handlers, systemMessage/suppressOutput honored (command handlers full) |
| `setting.permissions.deny` | full |  |  | hard, non-interactive block — the kept deterministic safety valve; a Read(<glob>) deny also gates Grep/Glob/NotebookRead and, deny-direction only, blocks Edit/MultiEdit (not Write/NotebookEdit) on a matching path, one-directionally |
| `setting.skillListingBudgetFraction` | full |  |  | caps the startup skill-listing token budget |
| `setting.skillListingMaxDescChars` | full |  |  | caps per-skill description length in the startup listing (default 1536, Claude parity; tiered degradation, never omits a skill) |
| `setting.skillOverrides` | full |  |  | per-skill overrides applied at load: off / user-invocable-only / name-only |
| `setting.subagentConcurrency` | full |  |  | configured capacity applies to root dispatches and separately to each nested-background depth (subagents.concurrency); foreground nested dispatch bypasses those pools, so this PiCC extension is not a total or session ceiling and has no Claude-settings equivalent |
| `setting.subagentMaxDepth` | full |  |  | caps subagent nesting depth (subagents.maxDepth) — a PiCC extension with no Claude-settings equivalent, NOT Claude parity; default 1 = MAIN-SESSION-ONLY: main can spawn depth-1 subagents but those subagents cannot recurse; accepts any positive integer |
| `setting.subagentsEnabled` | full |  |  | gates subagent dispatch — a PiCC extension with no Claude-settings equivalent (subagents.enabled, plus the disableSubagents alias for the inverse); false / disableSubagents:true disables ALL subagent dispatch, fork included (the Agent/Task/SendMessage tools are not registered) |
| `setting.worktree.baseRef` | full |  |  | head\|fresh base resolved to a concrete commit before worktree creation |
| `setting.agent` ⚠ | partial |  | `agent.frontmatter.disallowedTools`, `agent.frontmatter.permissionMode`, `agent.frontmatter.tools`, `feature.selected-main-agent` | selects a Claude-format custom agent for the main session when neither the registered --agent flag nor a persisted branch identity is present. CLI > persisted branch > merged setting selector precedence is PiCC-defined and independent of definition order: managed, then nearest project, then user, then lower-precedence plugin definitions. Claude's CLI-defined --agents definitions are unsupported and absent. A fresh missing selection denies provider admission; missing or uncertain persisted evidence installs a no-tools recovery identity. PARTIAL because selector persistence, collision handling, diagnostics, and fallback are PiCC-defined rather than verified Claude parity |
| `setting.allowManagedMcpServersOnly` ⚠ | partial | documented: Settings reference — Available settings (2026-07-31); documented: Settings reference — Invalid entries in managed settings (2026-07-31) | `feature.mcp-managed-config`, `setting.allowedMcpServers`, `setting.deniedMcpServers`, `setting.strictPluginOnlyCustomization.mcp` | the highest-precedence managed occurrence restricts authorization to managed allow contributions while all deny contributions remain effective; Claude documents that invalid managed values act as true. It prevents valid lower-scope soft allowlists from broadening authorization, but does not turn an ordinary soft allowlist into an immutable administrator list when false or absent. Enforcement covers all currently loaded ordinary, standalone-managed, and agent-inline source classes before materialization. PARTIAL because server-managed delivery, deferred plugin/explicit-runtime sources, some URL edges, and PiCC hardening/reporting differ or remain unsupported |
| `setting.allowedMcpServers` ⚠ | partial | documented: Settings reference — Available settings (2026-07-31); documented: Settings reference — Invalid entries in managed settings (2026-07-31) | `feature.mcp-managed-config`, `setting.deniedMcpServers` | all currently loaded ordinary, standalone-managed, and agent-inline MCP source classes cross central transport-aware allow admission before materialization. This is Claude's documented soft allowlist: valid contributions merge and a valid lower-scope list can broaden access unless managed-only is active; present-empty lists authorize nothing and invalid managed allow values become active-empty. For stdio, any command rules take precedence over name rules; for remote servers, any URL rules take precedence over names. Command vectors and names match exactly and case-sensitively; URL wildcards are anchored, host matching folds case and one trailing dot, and path matching is case-sensitive. Rules and candidate identity templates expand against one frozen launch-environment snapshot. PiCC additionally makes over-limit allow material active-empty and blocks malformed, ambiguous, or over-limit candidate identities. PARTIAL because server-managed delivery, deferred plugin/explicit-runtime sources, some URL edges, Claude's silently hidden blocked UI, and PiCC hardening/reporting differ or remain unsupported |
| `setting.blockedMarketplaces` ⚠ | partial |  |  | bounded read-only observation preserves managed blocked-marketplace declarations, provenance, and observed match posture. PiCC does not enforce this enterprise restriction against its explicit lifecycle commands or plugin loading; blocking remains observational |
| `setting.cleanupPeriodDays` | partial |  |  | 30-day default; accepts only a literal integer >= 1. Destructive retention pauses when any applicable settings source is unreadable, malformed, not an object, or contains an invalid occurrence. Best-effort startup cleanup covers recognized children of stale verified parents only when no ownership marker conflicts, individually stale children in marker-owned orphan collections, and orphaned worktrees. Transcript scanning is limited to the active manager-supplied default or custom session directory; orphan-worktree scanning is limited to the project's .claude/worktrees directory. Activation and approximately hourly mtime refresh protect an existing active main transcript without a lock-grade guarantee. Excludes the exact startup session's child transcript collection, malformed, unreadable, or mismatched ownership evidence, main-session transcripts, markerless legacy orphans, unfamiliar content, unrelated Pi data, secure erasure or deletion-time guarantees, and Claude Code's broader application-data cleanup |
| `setting.deniedMcpServers` ⚠ | partial | documented: Settings reference — Available settings (2026-07-31) | `feature.mcp-managed-config`, `setting.allowedMcpServers` | all currently loaded ordinary, standalone-managed, and agent-inline MCP source classes cross central deny-first admission before materialization; valid denylists merge across scopes and deny always wins over every allow. Name and complete command-vector matching is exact and case-sensitive; URL wildcards are anchored, host matching folds case and one trailing dot, path matching is case-sensitive, and deny URL matching canonicalizes default ports. Rules and candidate identity templates expand against one frozen launch-environment snapshot. Claude documents invalid managed deny values as dropped; schema-invalid ordinary settings files are rejected with zero policy projection. PiCC additionally fails closed when bounded-source or restrictive-projection loss may have omitted deny material. PARTIAL because server-managed delivery, deferred plugin/explicit-runtime sources, some URL edges, Claude's silently hidden blocked UI, and PiCC hardening/reporting differ or remain unsupported |
| `setting.disabledMcpjsonServers` | partial | documented: Settings reference — Available settings (2026-07-31) | `feature.mcp-project-approval`, `setting.enableAllProjectMcpServers`, `setting.enabledMcpjsonServers` | broad per-name compatibility decline list for project servers; PiCC-defined application covers ordinary project and agent-inline MCP servers. PiCC interactive reject writes a separate private exact-definition decision. It is honored from EVERY scope and always wins over enableAllProjectMcpServers and enabledMcpjsonServers; it is also the quiet first-class path for dismissing the pending-review notice, and a declined server raises no expansion warnings. Each UTF-16 code unit outside ASCII letters, digits, '_', and '-' becomes '_' on both sides; an astral symbol therefore becomes '__' (binary-verified Claude Code 2.1.218). The setting and ordinary project-decline contract are documented; agent-inline application, honoring from every scope, and accumulate-and-dedupe across settings files are PiCC-defined or inferred, so this remains PARTIAL |
| `setting.enableAllProjectMcpServers` | partial | documented: Settings reference — Available settings (2026-07-31) | `feature.mcp-project-approval`, `setting.disabledMcpjsonServers`, `setting.enabledMcpjsonServers` | blanket approval for project-scope MCP servers; PiCC-defined application also covers project-agent inline servers — approves every current and future project server and is NOT a shortcut for a large pending set; prefer explicitly named enabledMcpjsonServers approvals. Honored ONLY from user and managed settings; checkout-local and committed project occurrences cannot authorize with a diagnostic so a cloned repo can never self-approve. Nearest-honored-scope-wins boolean. Part of PiCC's settings gate replacing Claude Code's interactive trust dialog (see feature.mcp-project-approval); disabledMcpjsonServers always wins per server |
| `setting.enabledMcpjsonServers` | partial | documented: Settings reference — Available settings (2026-07-31) | `feature.mcp-project-approval`, `setting.disabledMcpjsonServers`, `setting.enableAllProjectMcpServers` | broad per-name compatibility approval list for project-scope MCP servers; PiCC-defined application also covers project-agent inline servers. It is distinct from PiCC private exact-definition review and honored ONLY from user and managed settings; checkout-local and committed project occurrences cannot authorize. Each UTF-16 code unit outside ASCII letters, digits, '_', and '-' becomes '_' on both sides; an astral symbol therefore becomes '__' (Claude parity, binary-verified 2.1.218); one persisted named approval can therefore match a differently named current or future server, so re-review aliases when project MCP names change. Approval from ANY honored scope wins — binary-corroborated (2.1.218: Claude's approval loop iterates scopes with a project-settings skip path); only the accumulate-and-dedupe of the lists across settings files remains PiCC-inferred (the permissions.allow/deny idiom). Part of the settings gate (see feature.mcp-project-approval); disabledMcpjsonServers always wins per server |
| `setting.enabledPlugins` | partial |  |  | qualified name@marketplace keys accept literal booleans only. Lifecycle commands write only the selected authentic declaration: user uses the active Claude profile settings.json, project uses the active checkout .claude/settings.json, and local uses the canonical main checkout shared .claude/settings.local.json. Realized load order remains user, then each root-to-cwd project/local pair, then, for a reciprocally verified linked worktree, the canonical main checkout's shared .claude/settings.local.json, and finally managed policy; later wins per identity. This interleaving can diverge from Claude's documented global Local > Project > User priority. Existing explicit effective enablement survives install and update; otherwise initial install uses marketplace defaultEnabled, then manifest defaultEnabled, then true. Declaration-only consent permits writing a declaration whose requested value is ineffective; it never overrides a higher-precedence effective declaration. PARTIAL because PiCC mutates only selected PiCC-owned lifecycle state |
| `setting.env` | partial |  |  | applied to project-owned main/subagent Bash, hooks, skills, and MCP processes after launcher-only ambient context is removed; excluded from PiCC-owned startup and worktree Git administration so project Git/loader variables cannot redirect it; surface-required and MCP server values retain later precedence |
| `setting.extraKnownMarketplaces` | partial |  |  | PiCC can write selected user, project, or local declarations for exact PiCC-owned marketplace lifecycle records; existing Claude-owned, seed, and managed registrations remain bounded inspectable read-only observations. A declaration alone never authorizes executable content. PARTIAL because imported ownership and unsupported private/enterprise sources remain outside PiCC mutation |
| `setting.mcpServers` | partial | documented: MCP reference — Environment variable expansion in .mcp.json (2026-07-31); documented: MCP reference — Installing MCP servers (2026-07-31); documented: MCP reference — Scope hierarchy and precedence (2026-07-31) | `feature.mcp`, `feature.mcp-claude-json-scopes`, `feature.mcp-managed-config`, `feature.mcp-project-approval` | settings-file mcpServers is a retained PiCC compatibility extension for stdio plus explicit http, streamable-http, and deprecated sse servers; Claude Code honoring this settings-file key remains version-dependent/unconfirmed. A present standalone managed-mcp.json suppresses this source entirely. Otherwise authentic Claude sources always outrank it and every effective winner crosses central policy admission before approval, native disablement, or materialization. Whole entries never field merge: native local > project .mcp.json > native user > managed settings extension > untracked settings.local.json extension > project settings extension > user settings extension. Project-origin extension winners require exact PiCC review or a broad user/managed compatibility grant; user/managed and untracked-local extension winners enable by default. Native runtime disablement applies only to authentic native/.mcp.json winners, never settings-extension winners; all applicable inactive gates run before expansion, so inactive entries materialize no command, URL, or headers. Stdio command/args/env and remote URL/static-header values use ${VAR}/${VAR:-default}; unset-without-default retains the literal and warns with variable names only. Remote interpolation reads PiCC ambient launch environment, not settings.env; the upstream ordering interaction is unverified, while settings.env continues to overlay stdio child environments. Remote config enforces URL/header size/count/syntax caps and reserved transport-header restrictions (see feature.mcp-remote-transports). PARTIAL for PiCC-defined extension precedence and unconfirmed upstream settings-file semantics |
| `setting.memory` | partial |  |  | auto memory: MEMORY.md (first 200 lines / 25 KB) loads at session start with full parity; autoMemoryEnabled/autoMemoryDirectory + CLAUDE_CODE_DISABLE_AUTO_MEMORY honored. PARTIAL: injected write guidance is conservative by default — the model writes/updates memory only on an explicit user request to remember, whereas Claude Code also writes proactively; opt into eager writes via CLAUDE.md |
| `setting.permissions.allow` | partial |  |  | parsed and matched, but moot under the default-permissive posture — nothing waits on an allow |
| `setting.strictKnownMarketplaces` ⚠ | partial |  |  | bounded read-only observation preserves managed strict-marketplace declarations, provenance, and observed match posture. PiCC does not enforce this enterprise restriction against its explicit lifecycle commands or plugin loading; no override is claimed |
| `setting.apiKeyHelper` | degraded-noop |  |  | parsed, never invoked — auth comes from the harness subscription/provider flow; reported when set |
| `setting.attribution` | degraded-noop |  |  | parsed, not consumed — no commit/PR attribution machinery; reported when set |
| `setting.checkpointing` | degraded-noop |  |  | checkpointing/rewind deferred — Pi's session model instead |
| `setting.includeCoAuthoredBy` | degraded-noop |  |  | parsed, not consumed — PiCC has no commit-attribution machinery either way; reported when set |
| `setting.model` | degraded-noop |  |  | parsed, not consumed — session model selection uses PiCC config (model/effort), not Claude model names; reported when set |
| `setting.outputStyle` | degraded-noop |  |  | cosmetic output styles not honored beyond Pi defaults |
| `setting.permissions.additionalDirectories` | degraded-noop |  |  | parsed, no-op — the default-permissive posture applies no directory sandbox, so extra grants are moot; reported when set |
| `setting.permissions.ask` ⚠ | degraded-noop |  |  | ask rules will NOT prompt — default-permissive posture runs them without asking |
| `setting.permissions.defaultMode` ⚠ | degraded-noop |  |  | permission modes/auto-mode are a no-op — sessions run default-permissive regardless |
| `setting.planMode` | degraded-noop |  |  | plan mode is a no-op — treated as guidance |
| `setting.pluginSuggestionMarketplaces` | degraded-noop | documented: Settings reference — Available settings (2026-07-31) |  | PiCC does not perform organization-governed plugin suggestions |
| `setting.statusLine` | degraded-noop |  |  | cosmetic statusline not honored beyond Pi defaults |
| `setting.allowAllClaudeAiMcps` | not-supported | documented: Settings reference — Available settings (2026-07-31) | `feature.mcp-connectors`, `setting.disableClaudeAiConnectors` | the managed setting that permits all claude.ai MCP integrations is not consumed; PiCC has no claude.ai connector loading |
| `setting.allowManagedHooksOnly` ⚠ | not-supported |  | `feature.plugins-content`, `feature.plugins-hooks`, `setting.hooks` | PiCC does not enforce this managed restriction for hooks in PiCC-owned or imported plugins; an otherwise admitted plugin command hook can run. Do not rely on this setting under PiCC |
| `setting.allowedChannelPlugins` | not-supported | documented: Settings reference — Available settings (2026-07-31) | `feature.mcp-channels`, `setting.channelsEnabled` | the enterprise channel plugin allowlist, replacement and empty-list semantics, and development-channel exception are inert, but PiCC cannot load channels, so the absent restriction cannot permit a channel to run |
| `setting.channelsEnabled` | not-supported | documented: Settings reference — Available settings (2026-07-31) | `feature.mcp-channels`, `setting.allowedChannelPlugins` | the enterprise channel master switch is inert, but PiCC cannot load channels, so disabling it cannot permit an otherwise prohibited channel to run |
| `setting.disableClaudeAiConnectors` | not-supported | documented: Settings reference — Available settings (2026-07-31) | `feature.mcp-connectors`, `setting.allowAllClaudeAiMcps` | the managed connector-disable setting is not consumed; PiCC has no claude.ai connector loading |
| `setting.disableCommandPluginSources` ⚠ | not-supported | documented: Settings reference — Available settings (2026-07-31) |  | PiCC does not support command plugin sources, but it also does not enforce this managed policy as a general restriction on supported plugin lifecycle sources. Do not rely on this setting under PiCC |
| `setting.disableSideloadFlags` | not-supported | documented: Settings reference — Available settings (2026-07-31) | `feature.mcp-cli-invocation-controls` | the managed restriction governs unsupported Claude sideload flags and is not consumed. It neither disables nor authorizes PiCC's explicit `picc plugin` and interactive `/plugin` lifecycle commands |
| `setting.pluginConfigs` | not-supported | documented: Settings reference — Available settings (2026-07-31) |  | PiCC observes the bounded marketplace userConfig declaration shape but does not consume configured values or apply plugin user configuration |
| `setting.strictPluginOnlyCustomization.mcp` ⚠ | not-supported | documented: Settings reference — Available settings (2026-07-31) | `feature.mcp-managed-config`, `feature.mcp-plugin-servers`, `setting.allowManagedMcpServersOnly` | the managed strictPluginOnlyCustomization setting's `mcp` membership is ignored, so its plugin-only restriction does not stop currently loaded native, .mcp.json, settings-extension, or supported agent-inline servers. Plugin-provided and explicit runtime sources remain unsupported; standalone administration mutates supported native/.mcp.json inputs and does not make this restriction effective. Do not use PiCC where this enterprise policy is required; use Claude Code or independently ensure prohibited servers are absent or disabled through PiCC-supported configuration |

## Frontmatter fields (35)

Skill (`SKILL.md`), agent (`.claude/agents/*.md`), and rule frontmatter keys.

| ID | Tier | Claude evidence | Related | Note |
|---|---|---|---|---|
| `rule.frontmatter.paths` | full |  |  | path-scoped rule injection on matching file access |
| `skill.frontmatter.agent` | full |  |  | names the agent context used with context: fork |
| `skill.frontmatter.argument-hint` | full |  |  | shown for slash-command argument entry |
| `skill.frontmatter.arguments` | full |  |  | named argument specs with required/default handling |
| `skill.frontmatter.context` | full |  |  | context: fork runs the skill as a fresh-context subagent; a fork that fails on a terminal error is a LOUD failure naming the cause with partial output preserved (parity with the Agent tool, 2.1.199). It consumes tool.Agent's state-aware failure presenter but is non-resumable, so progressed transient failures require review of retained work and possible side effects before another explicit dispatch; Esc cancels an in-flight fork and reports it aborted — a model-invoked fork (Skill/SlashCommand tool) via Pi's per-call signal, and a typed top-level /forked-skill in interactive mode via the input hook watching raw terminal input for Esc (print/RPC modes have no Esc); fork dispatches are non-resumable |
| `skill.frontmatter.description` | full |  |  | enters the startup listing; drives model invocation |
| `skill.frontmatter.disable-model-invocation` | full |  |  | hides the skill from model-invocation listing |
| `skill.frontmatter.disallowed-tools` | full |  |  | denylist enforced — resident-skill denials feed the session deny guard; context:fork dispatch receives them as subagent gating |
| `skill.frontmatter.hooks` | full |  |  | skill-scoped hook config dispatched while the skill is active |
| `skill.frontmatter.metadata` | full |  |  | metadata.* preserved and exposed |
| `skill.frontmatter.name` | full |  |  | skill identity; nearest-scope wins on clashes |
| `skill.frontmatter.paths` | full |  |  | path-scoped skills are surfaced (once) when a matching file is accessed; activation stays explicit via the Skill tool |
| `skill.frontmatter.shell` | full |  |  | bash (default) \| powershell for !`cmd` injection |
| `skill.frontmatter.when_to_use` | full |  |  | appended to the routing/listing surface |
| `agent.frontmatter.background` | partial |  |  | background: true forces dispatched work into the background even against run_in_background:false, subject to CLAUDE_CODE_DISABLE_BACKGROUND_TASKS. PiCC applies no background behavior when the definition is selected for a main session and does not claim Claude applies this subagent field there |
| `agent.frontmatter.color` | partial |  |  | recognized documented colors tint dispatched-agent identity in the interactive-TUI status panel, drill-down, and Agent lifecycle rows; exact hues and invalid handling are PiCC-defined, print/RPC remain uncolored, and selected-main identity has no color behavior |
| `agent.frontmatter.description` | partial |  |  | drives description-routed subagent selection and catalog display; a selected main agent is chosen explicitly and does not use its description for execution |
| `agent.frontmatter.disallowedTools` ⚠ | partial |  |  | tool denials gate dispatched subagents and selected-main active tools, catalog, and call-time execution, including Agent/Task aliases. Selected-main host reconciliation has the same observable get/set limits as agent.frontmatter.tools |
| `agent.frontmatter.effort` | partial |  |  | dispatch maps per-agent effort to Pi thinking; selected-main applies it after the host baseline and PiCC config. Unsupported or refused selected effort retains the working effort with a warning; exact Claude ordering and residual mapping are unverified |
| `agent.frontmatter.hooks` ⚠ | partial |  |  | Claude documents project-folder agent hooks as project-trust-gated, user and CLI-defined agent hooks as trusted by their explicit source, --add-dir agent hooks as governed by that directory's trust, and -p as unable to approve an untrusted folder. PiCC gives non-plugin dispatched agents a scoped runner and maps Stop to SubagentStop; selected-main hooks share the main lifecycle with base hooks and retain a collision-proof slot through SessionEnd. Selected-main project/local hooks require a positive Pi project-trust result. After dispatch admission, dispatched project/local agent hooks currently have no equivalent positive trust gate. PiCC's exact mapping to Claude's origin and mode cases remains partial and unverified; replacement and shutdown use PiCC-defined exactly-once/fail-closed transitions. Plugin agents strip the field |
| `agent.frontmatter.initialPrompt` | partial |  |  | Claude documents a main-session agent initialPrompt as an automatically submitted first user turn: commands and skills are processed, and the value is prepended when the user also provides a prompt. PiCC dispatch uses it as the first child user message, a PiCC-defined and unverified use. PiCC selected-main instead records a separate no-trigger user-role message, does not process commands/skills or compose it with the later prompt, and proves it synchronously on the live selected branch before provider admission. A new persisted session becomes reopenable from disk only when Pi persists it, currently after the first assistant response; once persisted, resume does not replay initialPrompt |
| `agent.frontmatter.isolation` | partial |  |  | isolation: worktree pins a dispatched subagent to its own worktree. PiCC applies no isolation behavior when the same definition is selected for a main session and does not claim Claude applies this subagent field there |
| `agent.frontmatter.maxTurns` | partial |  |  | best-effort dispatched-subagent cap: later tool calls are blocked with an instruction to answer and the model may still produce its final message. PiCC applies no maxTurns behavior to a selected main session and does not claim Claude applies this subagent field there |
| `agent.frontmatter.mcpServers` | partial | documented: Subagents reference — MCP servers (2026-07-31) | `feature.mcp`, `feature.mcp-model-failure-visibility`, `feature.mcp-project-approval`, `tool.Agent`, `tool.TaskOutput`, `tool.mcp__*` | Claude documents list syntax, configured-server references, inline definitions, and plugin/managed-policy boundaries. Selected-main and dispatched user/project agents borrow eligible published routes without duplicate clients and can own admitted inline stdio/HTTP/SSE runtimes; published routes win same-name collisions. Selected-main ownership lasts for the session, uses exact definition/source-family/agent-owner review, publishes only selected-owned inline state in selected-authority /mcp and /doctor sections, and treats any declared capability that is missing, invalid, blocked, disabled, pending/rejected, or startup-failed as an admission failure. Dispatch ownership starts after SubagentStart/worktree admission, remains absent from parent inventory, and reports equivalent setup degradation as bounded child/result warnings rather than blocking parent admission. Nested omitted/clean-empty declarations inherit eligible globals while parent-inline routes do not propagate; prompt commands remain main-session-only. Plugin agents strip the field, managed agents retain inert evidence, and managed-agent MCP execution, WebSocket, cross-process scope persistence, broader permission modes, interactive per-tool approval, and verified Claude parity remain unsupported. General admission, policy, lifecycle, cleanup, exposure, status, and transport mechanics are owned by the corresponding feature.mcp-* entries |
| `agent.frontmatter.memory` | partial |  |  | Claude documents user\|project\|local memory scopes and MEMORY.md loading for subagents. PiCC applies that contract to dispatch; applying it to selected-main prompt context is PiCC-defined and unverified. Write guidance remains conservative unless the user explicitly requests memory, unlike Claude's proactive behavior |
| `agent.frontmatter.metadata` | partial |  |  | metadata is absent from Claude's documented supported agent-frontmatter table. PiCC preserves metadata.* for dispatched-agent loading and in the immutable selected-main snapshot but does not consume or expose it; these semantics are PiCC-defined and unverified |
| `agent.frontmatter.model` | partial |  |  | dispatch applies an available per-agent model; selected-main restores the host baseline, applies PiCC config, then the agent override. Claude aliases retain PiCC's working-model mapping and unavailable or refused models retain the working model with a warning, so ordering and fallback are PiCC-defined |
| `agent.frontmatter.name` | partial |  |  | the selected-main selector and subagent_type resolve the effective agent identity through exact name or one unique plugin suffix. PiCC named-definition precedence is managed, then nearest project, then user, then lower-precedence plugin agents, independently of selector precedence. Claude's separate CLI-defined --agents definitions are unsupported and do not participate; plugin namespace collision and persisted requested-versus-resolved identity behavior are PiCC-defined |
| `agent.frontmatter.skills` | partial |  |  | Claude documents listed-skill preload for subagents. PiCC preloads them for dispatch; selected-main preload and retention of activated session skill context across every-turn prompt replacement are PiCC-defined and unverified, as are plugin runtime substitution and missing-skill omission |
| `agent.frontmatter.tools` ⚠ | partial |  |  | capability allowlisting gates dispatched subagent construction and selected-main active tools, subagent catalog, and call-time execution. Selected-main reconciliation intersects the frozen policy with Pi's observable active-tool intent; uncertainty fails admission closed, but Pi exposes no atomic registry transaction or mutation provenance |
| `skill.frontmatter.allowed-tools` ⚠ | partial |  |  | gates tools for context:fork dispatch; trivially satisfied for in-session activation under the default-permissive posture |
| `skill.frontmatter.effort` | partial |  |  | honored for context:fork dispatch and ${CLAUDE_EFFORT} substitution; does not change the parent session's reasoning effort |
| `skill.frontmatter.model` | partial |  |  | honored for context:fork dispatch; in-session activation cannot switch the parent session's model |
| `skill.frontmatter.user-invocable` | partial |  |  | true (default) permits typed slash invocation when the effective name matches PiCC's token grammar: an ASCII alphanumeric first, then ASCII alphanumerics/underscore/hyphen, with optional colon-separated nested-alias segments. Prompt-palette stubs use the narrower single-segment ASCII token form. Reserved Pi/PiCC names are shadowed case-insensitively; this precedence is PiCC-defined and unverified against Claude Code; direct model invocation remains governed separately by model-invocation metadata |
| `agent.frontmatter.permissionMode` ⚠ | degraded-noop |  |  | for non-plugin agents, parsed and reported but no-op in both selected-main and dispatched sessions: PiCC remains default-permissive while deny rules and tools:/disallowedTools: stay enforced. Plugin-provided agents forbid, diagnose, and strip this field |

## Runtime features (99)

Cross-cutting runtime subsystems and behaviors.

| ID | Tier | Claude evidence | Related | Note |
|---|---|---|---|---|
| `feature.claude-md-import` | full |  |  | @import expansion, recursive up to 4 hops, incl. the AGENTS.md bridge |
| `feature.nested-claude-md` | full |  |  | full ancestor-chain CLAUDE.md/CLAUDE.local.md load (to filesystem root) + nearest-ancestor injection on subdir file access, incl. worktrees |
| `feature.rules` | full |  |  | .claude/rules/ unconditional load + path-scoped injection at project and user scope |
| `feature.worktrees` | full |  |  | EnterWorktree/ExitWorktree lifecycle incl. .worktreeinclude, parallel sessions, Windows tolerance |
| `feature.agent-memory` | partial |  |  | Documented dispatched-subagent project/user/local memory scopes load into child context. Applying agent memory to a selected-main prompt is PiCC-defined and unverified. Auto memory also loads project MEMORY.md; injected write guidance remains conservative by default, so the model writes or updates memory only on an explicit user request to remember whereas Claude Code also writes proactively; opt into eager writes via CLAUDE.md |
| `feature.background-agents` | partial |  |  | background-by-default dispatch (Claude 2.1.198), TaskOutput/TaskStop, loud failures; failed terminal delivery with a structured recovery disposition carries tool.Agent's canonical state-aware guidance. CHECKPOINT EXCEPTIONS: exhausted pre-commit foreground/background dispatches are retained by agent id; awaited SendMessage recovery returns the result directly without a new task generation, while TaskStop by agent id abandons the paused agent. Separately, TaskStop during the exact current dispatch generation's authenticated post-commit resumed abort/join epoch enters one registry-owned flight and waits for canonical cancellation plus linked background settlement; this does not turn ordinary running, pre-start, normally completed, non-resumed, or stale task generations into checkpoint-joining stops. Confirmed resumed-cancellation input is represented by one immutable canonical report consumed by Agent/Task, background settlement, TaskOutput, and the panel; unconfirmed input remains quarantined and is skipped by repeat stop/release paths. On confirmed shutdown, one bounded best-effort attempt either exactly reopens one Pi session entry or one restrictive atomic recovery file under the verified session owner and emits its locator, or warns of possible loss before ordinary cleanup continues; unconfirmed work still blocks cleanup. Restoration-paused agents must be replaced rather than compacted again. In the interactive TUI — an always-on status panel closing the in-session Agent View gap, whose inline layout is PiCC-defined presentation rather than verified Claude Code presentation parity: every individually rendered running or capacity-waiting agent uses one physical status row, with a bounded sanitized live-activity payload after its dispatch description, or after identity when no distinct description exists, when row space permits. With a valid theme, every dynamic payload is muted and italic. While synthetic default `Thinking…` is displayed, it includes the remembered activity as `<activity> · Thinking…`; genuine activity replaces it immediately. As available row space changes, the payload may be truncated with an ellipsis or omitted together with its separator; terminal rows omit it. A bounded agent window remains navigable across overflow. At widths too narrow for individual rows, truthful aggregates remain without per-agent activity. The panel also provides a keyboard-entered drill-down that retains multiline history and richer detail (initial prompt, bounded structured live detail, final answer), steering of running background agents, per-agent stop (background-only; a user stop is permanent — see tool.SendMessage), dismiss, and stop-all. Normal-path calls and results share a tool row. For main-session/depth-1 background work, successful acceptance is transient in human chat and awaiting/polling TaskOutput retains its requested task ID in its status presentation. The first terminal delivery through TaskOutput or settlement creates one concise semantic record. The configured `app.tools.expand` action (Ctrl+O by default) reveals retained output and operational detail; an unbound action fails open. At unusable widths, detail waits for widening; a resize prompt appears only when it fits. A later already-reported TaskOutput retrieval adds no human row; the panel and condensed-record UI remain interactive-TUI-only, while canonical model-visible results — including tool.Agent's state-aware terminal recovery guidance when a structured recovery disposition exists — are shared with print/RPC delivery. Nested work at depth >= 2 instead keeps Pi's default notice box and appears in the panel tree and its parent's transcript. Row cap, entry chord, stop-all confirm, the in-panel stop-all key, background-only per-agent stop (a v1 scoping choice), and the two-tier auto-expiry of finished rows (~10s success / ~60s failed-or-stopped, frozen while the panel has focus) are PiCC-chosen details, NOT parity — Claude Code keeps finished agents listed until dismissed; drill-down steering does not fire UserPromptSubmit hooks (PiCC decision; see hook.event.UserPromptSubmit). An eligible uncollected current task receives one bounded settlement notice on the coordinator's NEXT turn; a running TaskOutput poll preserves eligibility, while a successful terminal return counts as delivery and suppresses a redundant not-yet-sent notice. Explicit retrieval remains available after notification without re-arming it; stopped notices are outcome-only because final output is deliberately discarded; resume uses a new task generation and newest-generation-wins supersession. This collection-aware suppression is intentional PiCC UX hardening, NOT verified parity: reporter-observed Claude Code 2.1.x can enqueue a redundant post-retrieval notification, public docs specify no notification-consumption semantics, and reports establish no exact normative contract. PiCC EXTENSION/DIVERGENCE: official Claude Code subagent documentation removes TaskOutput from every named subagent even when listed in `tools:`; PiCC intentionally exposes TaskOutput to subagents scoped to the subagent's own dispatched tasks while the coordinator retains full session-wide reach (see tool.TaskOutput). TaskStop keeps the same own-dispatch scope for subagents and session-wide coordinator reach. GAPS: PiCC notice timing is next-turn, while reporter observations (anthropics/claude-code#21343, Claude Code 2.1.20 background agents, and anthropics/claude-code#24752) describe late notification during an active conversation without establishing exact normative timing; idle parents are not re-invoked; one-shot print mode can finish before eligible uncollected work is surfaced; TaskStop accepts only task_id (Claude 2.1.198+ also accepts agent id/name); PiCC allows SendMessage resume after TaskStop (a model stop) while the Claude Code 2.1.x reference refuses it; the status panel is interactive-TUI-only (print/RPC have no panel) and there is no cross-session agent view; no remote/cloud agents; stop is cooperative. PiCC admits root dispatches up to the effective configured concurrency and queues additional accepted work FIFO with no separate waiter cap. When nesting is opted into with subagents.maxDepth greater than 1, each nested-background depth has a separate configured-capacity pool; maxDepth × concurrency bounds the background pools only. Foreground nested dispatch bypasses those pools to prevent same-pool parent/child deadlock, so total active work can be higher. Claude Code 2.1.217 documents a concurrently-running subagent cap (default 20, overridden by CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS); it does not establish queue-versus-rejection behavior or precise concurrency scope. Claude Code 2.1.212 separately documents a default-200 per-session subagent-spawn cap, overridden by CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION and reset by /clear; PiCC has no corresponding per-session spawn budget. Waiting status in TaskOutput and the interactive panel is PiCC-defined UX, not verified Claude parity. Lifecycle identity uses the task record's stored display type and stable agent id; resume uses a new task id and resolved registry name. Wording is model-visible and PiCC-defined, not verified as exact Claude wording. A subagent_type:"fork" dispatch inherits the parent conversation (main-session only) and is non-resumable — see tool.Agent.fork |
| `feature.collaborative-planning` | partial |  |  | always-on interaction posture — a standalone `## Working with the user` section (rebuilt each turn, survives compaction) injected into the MAIN SESSION only, NOT dispatched subagents (which return reports and have no user to converse with): steers the grounded, collaborative partner a Claude Code session is. On a substantial or open-ended request: ground first by reading the repo and share the specific files/lines/constraints found, resolve discoverable facts instead of asking, ask only about goals/preferences/material tradeoffs, surface the real choices and recommend one, verify load-bearing claims (yours or a subagent's) by reading the code, delegate broad context-heavy sweeps to a subagent and orient from its returned summary while still doing small lookups inline and still re-reading the code for load-bearing claims, and don't collapse a restated request straight into "go"/"confirm"; then switch to decisive autonomous implementation once scope is agreed. PARTIAL: guidance only, outcome model-dependent (a prompt nudge, not a deterministic conversation shape), NOT a mode or state machine and NOT gated on plan mode — a posture, not plan mode; the plan-mode tool surface stays degraded-noop (see feature.plan-mode); the delegation nudge mirrors Claude Code's documented delegate-and-summarize disposition (fan context-heavy detail work out to a subagent, work from the returned summary, preserve the coordinator's context) as a model-dependent guidance nudge, NOT a claim that PiCC reproduces Claude's delegation outcome |
| `feature.commit-message-guidance` | partial |  |  | always-on, every-turn nudge in the conventions block (rebuilt each turn, survives compaction): when asked to commit, first read the changes (git status/diff) and recent git log, match this repo's commit-message style where it is richer, and — for a non-trivial change — write a short why-over-what body; the --no-verify prohibition is preserved. PARTIAL: guidance only, outcome model-dependent (a prompt nudge, not a deterministic output shape); NOT full Claude Code commit parity — omits the HEREDOC commit form, the attribution trailer, and parallel git status/diff/log batching; commit attribution is unchanged — still no attribution trailer either way (see setting.includeCoAuthoredBy) |
| `feature.compaction-preservation` | partial |  |  | durable project instructions and unconditional rules are rebuilt in the system prompt; the latest rendered invoked skill bodies are restored most-recent-first when they fit PiCC's heuristic per-skill and combined character budgets. PARTIAL: Claude Code uses a token-counted retention policy, so PiCC can under- or over-retain it |
| `feature.compaction-summary-recovery` | partial |  |  | Default provider-backed automatic, manual, split-turn, and branch Codex summaries through Pi's shared summarization seam force summary-only SSE and provider maxRetries: 0. Pi's configured bounded summarization loop remains the sole owner of transient transport/provider-overload classification, attempts, abortable exponential backoff, and retry lifecycle events; cancellation and deterministic failures stop category-appropriately. Ordinary PiCC Agent turns and non-Codex summaries retain their configured transport and provider retry behavior. COMPATIBILITY BOUNDARY: public request fields cannot prove provenance, so an exact-signature custom caller receives the same summary policy until Pi exposes a purpose marker. Tier PARTIAL: PiCC reliability hardening, NOT Claude Code transport/retry parity |
| `feature.hook-handler.http` | partial |  |  | http hook handlers dispatched best-effort |
| `feature.managed-policy` | partial |  | `feature.managed-policy-windows-registry`, `feature.mcp-managed-config` | managed-settings source discovery implements the system managed settings file followed by lexically ordered drop-in files. Scalar replacement plus recursive object merge and stable array dedup follow Claude's documented drop-in merge shape. Managed MCP setting contributions discovered here feed central admission, but the platform-fixed standalone managed-mcp.json exclusive-server mechanism is distinct (see feature.mcp-managed-config). PARTIAL: Windows registry delivery is unsupported (see feature.managed-policy-windows-registry), and no complete server-managed settings, MDM, MDM-preference, or policy-helper parity is claimed; malformed or unreadable retained sources diagnose and are ignored |
| `feature.mcp` | partial | documented: MCP reference — Option 3: Add a local stdio server (2026-07-31); documented: MCP reference — Push messages with channels (2026-07-31) | `feature.mcp-capability-discovery`, `feature.mcp-claude-json-scopes`, `feature.mcp-managed-config`, `feature.mcp-remote-transports`, `setting.mcpServers` | Ordinary main-session enabled stdio and remote MCP servers admitted from standalone managed MCP or selected from native Claude local/user state, project .mcp.json, and the subordinate settings extension start non-blockingly with one main-session-only aggregate initial-settlement opportunity. Separately, each eligible named-agent dispatch composes borrowed published session routes with an agent-owned inline runtime; that ephemeral universe never registers into the parent or sibling context. A selected main agent separately composes borrowed published global routes with a session-owned inline runtime, while each eligible named-agent dispatch uses an isolated dispatch-owned runtime; neither owned inline universe widens global, parent, sibling, or same-name ordinary catalogs. Standalone exclusive control and settings policy are resolved before post-admission materialization; blocked servers never reach runtime. PiCC's resolved tool timeout is a hard wall-clock bound and progress notifications do not extend it. Every advertised tools, prompts, or resources capability list uses one initial attempt plus at most three transient network/server retries with 100/200/400 ms backoff, restarting pagination at page one; authentication/4xx/request-timeout failures are not retried. Malformed config or startup failure degrades without crashing. An initial `tools/list` failure is fatal to that server's staged capability publication: it publishes no capability snapshot, `mcp__...` proxies, or fixed resource tools. Separately, when an otherwise successfully settled server advertises resources, a `resources/list` failure retains that advertised capability, registers the two fixed resource tools, and remains attributable in their catalog results; with no server both configured and enabled the model receives zero MCP context — NO MCP context of any kind. Stdio retains strict .mcp.json/settings discovery and ${VAR}/${VAR:-default} command/args/env interpolation (unset-without-default keeps the literal and warns with the VARIABLE only); enabled children receive binary-verified Claude 2.1.218 CLAUDE_PROJECT_DIR, CLAUDECODE=1, and CLAUDE_CODE_SESSION_ID defaults with config env winning, live until PiCC attempts process-tree shutdown on Windows/POSIX with bounded confirmation; dispatch-owned agent-inline shutdown uncertainty qualifies the dispatch output and receives one session-shutdown retry, but residual cleanup may remain unconfirmed. Session load remains non-blocking; the MCP_TIMEOUT 30 s connect default and MCP_TOOL_TIMEOUT unset default ~27.8 h are binary-verified Claude parity, and <1000 ms per-server timeout values are ignored (Claude parity). Safe pre-handshake connection-closure wording, value-redacted warnings naming a rejected non-empty timeout variable and its fallback, transient first-turn wait status, and bounded presentation are PiCC-defined and not verified Claude Code presentation parity; they do not change timeout resolution, settlement, or registration, transient status is interactive-TUI-only, and none enters model context. Transport distinction: eligible failed main-session stdio and supported remote servers expose PiCC-defined manual reconnect, but stdio children do not reconnect automatically; remote lifecycle is linked below. Stdio servers stay project-root cwd-pinned, including worktree calls (EFFECTIVE parity: Claude passes no cwd, so launch-at-root servers inherit the project root), inherit sanitized ambient after launcher-only marker removal, then settings.env, Claude defaults, and configured env last — including deliberate overrides of injected or removed launcher keys (config-last parity). Full schemas register upfront (no tool search — see feature.mcp-tool-search), so Selected-main setup failure denies provider admission with redacted recovery; ordinary main-session startup failures are not injected into MAIN-SESSION model context, and raw failure details are not model-reported; they instead surface through /mcp, /doctor, a one-time warning notice, and stderr. An explicit dispatched-agent reference to an unpublished route may receive bounded PiCC-defined unavailability wording; distinctly, direct dispatch-inline setup degradation receives bounded warning context before that child's first provider request. Remote servers share aggregate settlement, while connection and reconnection lifecycle belongs to feature.mcp-remote-transports; initial capability-list retry policy is owned here. Same-definition catalogs remain immutable; an admitted changed execution definition is rediscovered and reconciles dynamic main-session tool/resource exposure and typed prompt routing, but palette stubs remain startup-bounded and list_changed remains absent. PARTIAL because transport-specific and deferred MCP surfaces remain split across feature.mcp-* entries |
| `feature.mcp-capability-discovery` | partial | documented: MCP reference — Scale with MCP tool search (2026-07-31); unverified: MCP reference — Scale with MCP tool search (cross-capability failure coupling is not specified) (2026-07-31) | `feature.mcp`, `feature.mcp-model-failure-visibility`, `feature.mcp-prompts`, `feature.mcp-resources` | PiCC discovers advertised tools, prompts, and resources, but an initial tools/list failure prevents publication of every capability for that server. Same-definition reconnect/re-enable reuses immutable discovery; an admitted changed definition performs fresh discovery and dynamic main-session host refresh, while prompt palette stubs remain startup-bounded and agent catalogs stay isolated. That PiCC coupling is tested, while Claude's cross-capability failure coupling is undocumented; model-visible failed-server reporting is classified separately |
| `feature.mcp-claude-json-scopes` | partial | documented: MCP reference — Local scope (2026-07-31); documented: MCP reference — Scope hierarchy and precedence (2026-07-31); documented: MCP reference — User scope (2026-07-31); documented: Settings reference — Environment variables (2026-07-31); inferred: Private native state — physical .claude.json shape is not documented | `feature.mcp`, `feature.mcp-connectors`, `feature.mcp-managed-config`, `feature.mcp-project-approval`, `feature.mcp-runtime-disabled`, `feature.mcp-runtime-enabled`, `setting.mcpServers` | native Claude state loading supports top-level user mcpServers and the canonical project's local mcpServers, disabledMcpServers, and recognized-but-non-enabling enabledMcpServers. Native state is acquired only when standalone managed MCP is absent; every effective native winner then crosses central policy admission before native disablement or materialization. Claude's public local/user/project scope concepts are documented, but this private .claude.json physical shape is inferred rather than documentation-verified. The default profile pairs ~/.claude with ~/.claude.json; PICC_CLAUDE_USER_DIR or CLAUDE_CONFIG_DIR selects one coherent user profile for user-scoped settings and artifacts, imported installed-plugin state and data, memory, and native .claude.json state. Project and managed contributions plus supplementary authorized plugin roots remain in effect. Same-name definitions resolve as whole entries with local > project .mcp.json > user precedence before the subordinate settings extension. Canonical real paths unify equivalent spellings and symlinks; verified linked-worktree metadata also considers the main checkout identity. Bounded canonical-equivalent MCP projections coalesce when equivalent. Only conflicting projections or invalid matching project-record, MCP-block, or runtime-list shapes make matching project state unusable and fail all MCP loading closed; bounded individual server definitions retain skip-or-adjust handling. This identity, coalescing, and present-unusable-state recovery are conservative PiCC policies, not verified Claude behavior for multiple canonical-equivalent project records: an absent state file preserves other sources, while unreadable, malformed, ambiguous, non-regular, invalidly encoded, or oversized state fails all MCP closed before expansion and emits bounded value-redacted diagnostics. Standalone `picc mcp` can transactionally add/remove local and user declarations, project scope writes `.mcp.json`, and interactive enable/disable edits only native `disabledMcpServers`; unrelated state is preserved. PARTIAL because the private state shape, upstream canonicalization, linked-worktree sharing, multiple canonical-equivalent project record semantics, ambiguous-key recovery, and malformed-whole-file semantics remain inferred or unverified |
| `feature.mcp-cli-management` | partial | documented: MCP reference — Add MCP servers from JSON configuration (2026-07-31); documented: MCP reference — Import MCP servers from Claude Desktop (2026-07-31); documented: MCP reference — Installing MCP servers (2026-07-31); documented: MCP reference — Managing your servers (2026-07-31); documented: MCP reference — Project scope (2026-07-31) | `feature.mcp-claude-json-scopes`, `feature.mcp-control-status`, `setting.mcpServers` | standalone `picc mcp` supports bounded list/get plus direct or --dry-run add, add-json, remove, and reset-project-choices at local/project/user declaration scopes. Reads may perform bounded transient health discovery. Dry-run evaluates the current safe snapshot without recovery or writes, so direct execution may recover where dry-run refuses. Scoped reads, secret-safe field-selected output, terminal-versus-pending transaction/recovery taxonomy, bounded invalid-syntax help, and linked-worktree review reset are PiCC-defined. Add-json file/stdin/inline input has a 1 MiB UTF-8 byte bound; file/stdin avoids argv exposure, while inline JSON and env/header argv can enter shell history. Unscoped remove refuses ambiguity and any inventory with omitted declarations, leaving explicit `--scope` as the conservative path. Claude's add-from-claude-desktop import command and byte-identical output are unavailable; OAuth is not implemented |
| `feature.mcp-control-status` | partial | documented: MCP reference — Managing your servers (2026-07-31) | `feature.mcp`, `feature.mcp-cli-management`, `feature.mcp-managed-config`, `tool.WaitForMcpServers` | bare /mcp retains bounded read-only status for resolved MCP servers and invokes no recovery, including explicit blocked rows, policy posture/authority summaries, and authority-specific remediation also shared by /doctor; one bounded startup notice reports exclusive-empty, fail-closed, or blocked outcomes once per loaded extension. Pending review uses count-only TUI copy pointing to `/mcp manage`; headless administration returns deterministic no-action guidance and never waits. These policy views, reasons, formatting, redaction, and notice behavior are PiCC-defined observability, not Claude's silently hidden blocked-entry or interactive management UI. It reports the ordinary global runtime plus a selected-authority section when a custom main agent is active: selected-owned inline rows and excluded globals are bounded and ownership-labelled. Ephemeral dispatch-owned runtimes do not appear in parent /mcp or /doctor live status, and their startup/cleanup outcomes are carried by dispatch results. Separately, TUI-only `/mcp manage` and exact no-tail deep links first invoke service-owned recovery preparation, then expose its fresh inventory after completed rollback or remain blocked on pending/uncertain recovery; they provide bounded details, exact review, native enable/disable, reconnect, and an unavailable authentication entry. These workflows and deep-link grammar are PiCC-defined; enabledMcpServers remains recognized without activating default-off servers. Status covers: at most 32 detailed rows, covering connecting/retrying/connected/reconnecting/failed lifecycle; detail caps prioritize actionable states including auth/not-found/skipped, truthful further-omission guidance, bounded advertised tool/prompt/resource capability counts, advertised-empty and capability-discovery-failed states, terminal-retained catalogs, safe summaries, transport identity, attempt bounds, and SSE deprecation. /doctor preserves the same bounded actionable lifecycle truth and limits diagnostic-bearing per-server findings to that 32-server budget with fixed omission guidance. Interactive and RPC use an immediate live snapshot; one-shot text and JSON await bounded MCP startup settlement; output never includes endpoints, headers, or raw transport failure speech and never enters model context. Claude Code 2.1.205+ documents no-argument /mcp in -p as textual status; PiCC's JSON event, RPC entry, report formatting, aggregate bounds, safety redaction, lifecycle/deprecation rendering, omission policy, and timing are PiCC-defined. PARTIAL: PiCC-defined prose rather than Claude Code's interactive management UI or individual-tool view |
| `feature.mcp-hook-matching` | partial | documented: Hooks reference — Match MCP tools (2026-07-31) | `feature.hook-handler.mcp_tool`, `feature.hook-handler.mcp_tool-blocking-enforcement`, `feature.mcp-plugin-servers`, `tool.mcp__*` | MCP tool names use Claude's documented mcp__<server>__<tool> naming with exact, list, and unanchored-regex hook matching; ordinary configured, selected-main inline, and dispatch-owning-agent inline tools match through the same command-hook guard. Plugin-scoped names remain unavailable because plugin MCP servers are not discovered |
| `feature.mcp-managed-config` ⚠ | partial | documented: MCP reference — Managed MCP configuration (2026-07-31) | `feature.managed-policy`, `feature.mcp-project-approval`, `setting.allowManagedMcpServersOnly`, `setting.allowedMcpServers`, `setting.deniedMcpServers`, `setting.mcpServers`, `setting.strictPluginOnlyCustomization.mcp` | the platform-fixed `managed-mcp.json` is loaded as an exclusive administrator server set before ordinary MCP acquisition; absence preserves ordinary sources, a loaded empty set disables MCP, and populated sets suppress ordinary sources while retaining managed rows. A present but unusable or oversized authority, or a bounded projection with restrictive material omitted, fails MCP closed with bounded repair evidence. Entries cross central admission before materialization; agent-inline entries cross the same immutable managed policy without joining the standalone exclusive server inventory. PiCC-defined /mcp and /doctor summaries, explicit blocked rows/reasons, and a single startup notice expose outcomes that Claude silently hides or presents differently. PARTIAL because server-managed delivery is absent, deferred plugin/explicit-runtime sources are not loaded, PiCC hardens undocumented whole-source failures and resource limits, some URL identity edges remain unverified, and reporting is not Claude UI parity |
| `feature.mcp-model-failure-visibility` | partial | documented: MCP reference — Scale with MCP tool search (2026-07-31) | `feature.mcp-capability-discovery`, `feature.mcp-tool-search`, `tool.ToolSearch`, `tool.WaitForMcpServers` | Ordinary main-session startup failures are not injected into model context, and raw failure details are not model-reported; they remain human-visible through /mcp, /doctor, warnings, and stderr rather than Claude's ToolSearch/WaitForMcpServers path. Selected-main MCP setup, publication, or uncertain cleanup instead fails admission before provider work with redacted recovery diagnostics. A dispatched agent's unpublished reference or inline setup degradation settles before its first request and receives bounded PiCC-defined warning context that later qualifies Agent/TaskOutput; cleanup discovered after child work qualifies only the parent-facing result. No path exposes raw config or runtime errors |
| `feature.mcp-project-approval` | partial | documented: MCP reference — Project scope (2026-07-31) | `feature.mcp-claude-json-scopes`, `feature.mcp-managed-config`, `feature.mcp-runtime-disabled`, `feature.mcp-runtime-enabled`, `setting.disabledMcpjsonServers`, `setting.enableAllProjectMcpServers`, `setting.enabledMcpjsonServers` | the enablement gate for project-origin stdio and remote MCP servers (.mcp.json, committed .claude/settings.json mcpServers, and project-agent inline declarations) after central policy admission and only when standalone exclusive control is absent: disabled by default, started only through PiCC private exact-definition review or a broad user/managed compatibility approval; disabledMcpjsonServers always rejects each server by compatibility name. Native local and user definitions are not project-gated; authentic winners instead honor native disabledMcpServers before expansion. PiCC private review binds normalized execution definition, source family, linked-checkout family, and agent owner; changed same-name definitions and different owners return to pending. Invalid private review state blocks that exact review path but does not revoke independent trusted broad authority. Broad enableAllProjectMcpServers/enabledMcpjsonServers remain separate name-based compatibility settings. Checkout-local and committed project approval keys never authorize, so a cloned repo cannot self-approve. Ordinary pending servers surface as a count-only one-time TUI notice pointing to `/mcp manage`; headless modes never prompt and pending review starts nothing. A selected-main inline pending declaration prevents selected admission and appears through selected recovery diagnostics; dispatch-inline static pending declarations appear only in /doctor, and dispatch-time setup or cleanup outcomes appear only in bounded Agent/TaskOutput results. Only selected-owned inline state enters the parent selected-authority /mcp section. Broad compatibility names still use underscore normalization and can match aliases; PiCC private review does not. Approve/reject are confirmed TUI actions, reset-project-choices clears only private review, and startup consumes review/recovery passively without writes. UI, storage, linked-worktree state, and exact digest mechanics are PiCC-defined rather than Claude trust-dialog parity |
| `feature.mcp-prompts` | partial | documented: MCP reference — Use MCP prompts as commands (2026-07-31) | `feature.mcp-capability-discovery`, `feature.mcp-list-changed` | for each admitted definition, advertised prompts/list catalogs become immutable user-typed `/mcp__<normalized-server>__<normalized-prompt>` commands after initial settlement. Each UTF-16 code unit outside ASCII letters/digits/_/- becomes `_`; an astral symbol therefore becomes `__`. The `/` palette receives bounded frontmatter-only metadata stubs; exact typed invocation remains available when the raw server and prompt names are known if stub publication fails. Generated stubs persist metadata only and never write prompt bodies or results; successful transformed prompt content follows ordinary conversation/session transcript retention. Arguments are positional in declaration order with single/double-quoted groups; missing required names, surplus values, malformed quoting, unsafe/colliding names, optional-before-required schemas, and oversized complete hints fail visibly without a provider request. Successful prompts/get messages become one bounded user-turn transform with explicit untrusted user/assistant role labels; text and embedded text stay ordered, while image/audio/blob/unknown blocks receive visible omission notes. UserPromptSubmit and local command precedence apply. Prompts are NOT model SlashCommand calls and never enter main/subagent tool context; agent-inline prompt catalogs are intentionally not published, so prompt commands remain main-session-only. Same-definition catalogs remain immutable across reconnect and ignored list_changed; an admitted changed definition refreshes exact typed routing, while `/` palette stubs remain startup-bounded and are not dynamically republished; components, metadata, argument values, diagnostics, and aggregate content are bounded. PARTIAL because list_changed-driven refresh, native non-text prompt content, and exact Claude edge formatting are absent |
| `feature.mcp-remote-transports` | partial | documented: MCP reference — Automatic reconnection (2026-07-31); documented: MCP reference — Option 1: Add a remote HTTP server (2026-07-31); documented: MCP reference — Option 2: Add a remote SSE server (2026-07-31) | `feature.mcp`, `feature.mcp-headers-helper`, `feature.mcp-oauth`, `feature.mcp-websocket` | explicit http/streamable-http and deprecated sse remote MCP transports with static headers; same-origin redirects only, no secure-to-insecure redirect, no cross-origin header forwarding, and replayable requests capped at 1 MiB. Initial connection uses one attempt plus at most three transient retries at 1/2/4 s within aggregate MCP_TIMEOUT. After connection, five reconnects run at 1/2/4/8/16 s, each attempt bounded by MCP_TIMEOUT; authentication/not-found/permanent failures stop immediately. The same adapter and timeout/recovery boundaries apply to selected-main session-owned and dispatched agent-inline remote servers; their lifetimes remain separately owned. Initial tools/prompts/resources capability-list discovery and its separate 100/200/400 ms retry policy belong to feature.mcp; same-definition immutable and changed-definition refresh behavior belongs to tool.mcp__*. Eligible failed main-session remote servers also expose PiCC-defined manual reconnect, while automatic reconnection remains remote-only; authentication remains static-header-only because the auth entry cannot perform OAuth. See setting.mcpServers, feature.mcp, tool.mcp__*, feature.mcp-control-status, and feature.mcp-project-approval |
| `feature.mcp-resources` | partial | documented: MCP reference — Reference MCP resources (2026-07-31) | `feature.mcp-resource-subscriptions`, `feature.mcp-resource-templates`, `tool.ListMcpResourcesTool`, `tool.ReadMcpResourceTool` | the live main-session catalog conditionally exposes ListMcpResourcesTool and ReadMcpResourceTool when it first gains an advertised resource capability, including through changed-definition administration and for an advertised-empty or `resources/list`-failed catalog. Host registration may persist across reconnect, terminal retention, or retirement, while active exposure retires when no current main-session definition remains capable. Same-definition per-server catalogs are immutable across reconnect and ignored list_changed; an admitted changed definition refreshes the main-session live catalog, while reads validate and use the current client. Listing supports all capable servers or one exact server; reading forwards an opaque URI only to the selected server. Text is bounded labeled content and valid in-budget binary is labeled complete base64, not native binary/image delivery. Session-borrowed and agent-inline advertised resources feed the owning selected-main session or dispatch fixed tools, subject to the existing non-fork background filter for dispatch; they never widen the ordinary, parent, or sibling catalog. Fixed-name, top-level parameter, hook, and subagent controls are recorded on the tool entries. PARTIAL because resource `@` attachment/autocomplete, templates, subscriptions/list_changed refresh, native binary delivery, and exact Claude formatting are absent |
| `feature.mcp-root-schema-combinators` | partial | documented: MCP reference — Tool input schemas with a root-level combinator (2026-07-31) | `tool.mcp__*` | valid bounded compileable root anyOf, oneOf, and allOf tool input schemas are preserved; general schema normalization can still fall back to PiCC's permissive object schema for $ref, a non-object root, an over-32KB schema, or compile failure |
| `feature.mcp-runtime-disabled` ⚠ | partial | documented: MCP reference — Disable a server without removing it (2026-07-31); inferred: Private native state — disabledMcpServers persistence and list interpretation are not documented | `feature.mcp-claude-json-scopes`, `feature.mcp-control-status`, `feature.mcp-managed-config`, `feature.mcp-project-approval`, `feature.mcp-runtime-enabled` | after central policy admission, the selected native project's exact-name `disabledMcpServers` list is the final pre-expansion deny for authentic native local, project .mcp.json, and native user winners when standalone exclusive control is absent. It does not disable settings-extension winners. Interactive disable/enable transactionally edits only this list, preserves unrelated state, and reconciles eligible main-session runtime/exposure; the exact private-state persistence, list interpretation, upstream matching, and broader runtime-management semantics are inferred or unverified rather than documentation-verified |
| `feature.plugin-dependencies` | partial |  | `feature.plugin-install`, `feature.plugins-enablement` | PiCC-owned lifecycle activation checks declared dependencies against already-installed effective plugins; missing, incompatible, cyclic, ambiguous, or indeterminate dependencies block that activation. Imported dependency metadata remains observational. PiCC never acquires, updates, or enables dependencies automatically |
| `feature.plugin-install` | partial |  |  | explicit install supports PiCC-owned user, project, and local records from one exact retained PiCC-owned marketplace snapshot. It previews immutable source/revision/digests, executable components, dependencies, trust, enablement, settings, and consequences before confirmation, then atomically admits one complete executable generation. Private credentials, SSH, automatic dependency acquisition, and unattended automation are unsupported |
| `feature.plugin-marketplace` | partial |  |  | explicit add, inspect, refresh, list, and remove support absolute local directory/file inputs, GitHub repositories, anonymous public HTTPS Git repositories, and public HTTPS catalog descriptors in user, project, and local scopes. Exact same-name conflicts with different authority are refused rather than replaced. Existing Claude-owned, seed, and managed registrations remain read-only. Refresh changes catalog state only; removal requires preserving installed dependents. Private/SSH sources, credentials, enterprise distribution, release channels, and unattended automation are unsupported |
| `feature.plugin-source-acquisition` | partial |  | `feature.plugin-install`, `feature.plugin-marketplace` | catalog plugins can acquire immutable relative content only from retained materialized local or Git-backed marketplace trees; standalone public HTTPS catalog descriptors cannot confer relative content authority. Other supported plugin sources are GitHub or anonymous public HTTPS Git repositories and subdirectories, public npm packages from registry.npmjs.org without lifecycle scripts, and public HTTPS ZIP archives. Remote HTTPS acquisition accepts only ports 443 and 8443. Local acquisition is snapshot-based and marketplace command inputs are absolute. Private networks, credentials, SSH, custom npm registries, unsafe trees/archives, and mutable unresolved authority are rejected |
| `feature.plugin-uninstall` | partial |  |  | explicit uninstall applies only to one exact PiCC-owned record and separately confirms enabledPlugins declaration removal and optional plugin-data removal. Data removal logically retires the selected data into PiCC-owned quarantine; it does not provide secure erasure. Imported Claude and managed records remain read-only; dependents and higher-precedence effective declarations can block or qualify the action |
| `feature.plugin-update` | partial |  |  | explicit update preserves existing explicit enablement, acquires and validates an immutable replacement, and atomically publishes a complete generation while retaining safe pending recovery. Updates are manual only: no unattended updates, release channels, dependency acquisition, or private credentials |
| `feature.plugins-agents` | partial |  |  | installed plugin agents support default and explicit Markdown file/directory layouts. Installed qualified name@marketplace identity owns selection, variable/data context, and lazy persistent-data creation, while a present validated kebab-case manifest name owns the visible agent namespace and manifestless content uses the validated installed lifecycle name; plugin-agent local names are validated, and plugin-agent hooks, mcpServers, and permissionMode are diagnosed and stripped before construction. Global ordinary-agent parser gaps remain outside this plugin claim. A wrong-typed agents declaration is VERIFIED rejected; invalid explicit paths cause PiCC-defined whole-plugin rejection |
| `feature.plugins-command-plugin` | partial |  |  | interactive `/plugin` combines session-captured loaded-runtime truth with a fresh effective desired-state projection from the active checkout for passive Discover, Installed, Marketplaces, and Errors views and focused install, enable, disable, update, uninstall, marketplace add/refresh/remove, and recovery workflows. Assembly failure leaves captured runtime unchanged and starts no action. Values, exact selectors, destructive choices, preview, and confirmation are collected in the TUI; headless slash use is guidance-only. Passive views remain observational and acquisition-free. This is PiCC-defined UX, not exact Claude rendering parity or stable automation |
| `feature.plugins-command-plugins` | partial |  |  | `/plugins` remains a PiCC-defined exact-list alias over the same captured observational session snapshot, not Claude parity. It performs no lifecycle mutation, refresh, acquisition, or reload and defines no stable JSON automation contract |
| `feature.plugins-command-reload` | partial |  |  | interactive-TUI `/reload-plugins` validates a fresh complete candidate and fingerprint from the active checkout, then calls Pi `ctx.reload()` to adopt one replacement generation. Validation failure starts no Pi reload and preserves the current snapshot. After Pi starts replacement, PiCC cannot roll back live operational failure; rejection or unconfirmed activation throws authoritative new-session guidance even if Pi briefly showed generic reload success, while failures detected later by Pi may use Pi's own reload-error reporting. Headless use is guidance-only |
| `feature.plugins-commands` | partial |  |  | installed plugin commands support default and explicit Markdown file/directory layouts. Installed qualified name@marketplace identity owns selection, variable/data context, and lazy persistent-data creation, while a present validated kebab-case manifest name owns the visible command namespace; manifestless content uses the validated installed lifecycle name. A wrong-typed commands declaration is VERIFIED rejected; invalid explicit paths cause PiCC-defined whole-plugin rejection. Marketplace-entry component overlays/strict are unsupported |
| `feature.plugins-content` | partial |  |  | selected installed-plugin skills, commands, agents, and hooks are folded into shared registries after whole-plugin fail-closed validation; the inventory separately observes declared component shapes without making them effective. PARTIAL: installed-state v2 serialization is fixture-derived, development and catalog roots remain inert, strict marketplace overlays and unsupported component execution are absent, and plugin MCP servers remain deferred (see feature.plugins-installed-selection, feature.plugins-manifest-metadata, feature.plugins-inventory, feature.plugins-development-trust, and feature.mcp-plugin-servers) |
| `feature.plugins-enablement` | partial |  |  | strict qualified name@marketplace enablement accepts literal booleans only, with source-aware key-wise precedence. Installed record, authentic enabledPlugins declaration, precedence-resolved effective state, immutable code, and optional data remain distinct; repository enablement cannot establish development or bundled-root trust. Existing explicit effective enablement survives install and update; otherwise initial install uses marketplace defaultEnabled, then manifest defaultEnabled, then true. PiCC-owned activation validates dependencies conservatively and never acquires or enables them automatically; imported dependency metadata remains observational. PARTIAL because strictKnownMarketplaces, blockedMarketplaces, allowManagedHooksOnly, and other enterprise lifecycle policies are not enforced |
| `feature.plugins-hooks` | partial |  |  | installed plugin hooks support default hooks.json, explicit JSON file, and inline declarations with qualified identity and lazy persistent-data creation. At trusted execution time, command and argument fields alone expand root/data/project variables from the canonical authorized root; matcher, condition, URL, and arbitrary raw fields remain literal. A wrong-typed hooks declaration is VERIFIED rejected; invalid explicit paths cause PiCC-defined whole-plugin rejection. Non-command handler tiers remain tracked separately |
| `feature.plugins-installed-selection` | partial |  |  | exact-version runtime selection separates PiCC-owned committed installation authority from imported Claude records and uses qualified name@marketplace identity, applicable scope, project, installed provenance, and eligible cache bases. User, project, and local PiCC-owned records are mutable only through exact selectors; Claude-owned and managed records remain read-only. Captured Claude installed-state v2 serialization is FIXTURE-DERIVED and undocumented, not PiCC's permanent API. CLAUDE_CODE_PLUGIN_CACHE_DIR and each documented path-delimited CLAUDE_CODE_PLUGIN_SEED_DIR <seed>/cache directory are eligible bases only when they resolve to existing accessible directories and an exact imported record selects content below them; PiCC does not import seed marketplaces or implement Claude's first-seed lifecycle behavior. Highest-applicable winner selection, canonical equivalent deduplication versus conflict rejection, qualified blocklist schema and global fail-closed handling, namespace/data-key collision rejection, and the policy that ambiguous, stale, catalog-only, blocked, malformed, or enabled-but-uninstalled state loads no fallback content are PiCC-defined. Environment cache and seed bases never authorize content alone |
| `feature.plugins-inventory` | partial |  |  | PiCC-defined bounded observational projection joins installed records, declarations, effective enablement, immutable code, optional data posture, metadata, components, diagnostics, pending recovery, and loaded-session state by qualified identity; marketplace registrations and policy stay global, selected-manifest and catalog dependencies retain distinct provenance, and renames remain catalog-only inert declarations. Fixed `/plugins` and `/doctor` consume the startup capture. Interactive `/plugin` keeps loaded-runtime truth captured while freshly assembling effective desired authority from the active checkout at each open. Standalone command consumers build one fresh command-scoped durable-state snapshot. Every passive view remains inert and acquisition-free. No stable JSON automation or exact Claude rendering parity is claimed |
| `feature.plugins-launcher-inventory` | partial |  |  | `picc plugin list` and `picc plugin details <qualified-name>` build one fresh command-scoped snapshot, while the same standalone router provides explicit marketplace, install, enable, disable, update, uninstall, and offline recovery commands without normal Pi extension, hook, MCP, or plugin-runtime startup. Output is bounded deterministic human-readable text, mutations require previews and confirmation, and neither stable JSON automation nor exact Claude launcher parity is claimed |
| `feature.plugins-manifest-metadata` | partial |  |  | a present validated kebab-case plugin manifest name owns the visible runtime component namespace; manifestless content uses the validated installed lifecycle name. The selected manifest's bounded same-read projection observes delivered name, description, version, author, homepage, repository, license, keywords, defaultEnabled, dependencies, and supported or unsupported component declaration shapes. Skills, commands, agents, and hooks declarations can affect runtime only after selection and validation; defaultEnabled participates only in initial PiCC-owned install when no effective declaration exists, PiCC-owned activation checks dependencies against installed effective plugins, imported dependency metadata remains observational, and renames remain catalog-only inert declarations. Broader recognized metadata validation, catalog component overlays, marketplace strict overlays, and automatic dependency lifecycle are unsupported |
| `feature.plugins-marketplace-observation` | partial |  |  | bounded observation includes PiCC-owned and existing Claude-owned, seed, and managed registrations, fixture-derived known_marketplaces.json serialization, catalogs, supported and unsupported component shapes, dependencies, renames, provenance, and policy matches. Catalogs and declarations never authorize executable content by themselves. Only exact selected PiCC-owned records are mutable; imported records remain read-only. Catalog refresh changes retained catalog snapshots but never installed executable generation membership by itself; dependency automation, rename migration, strict-overlay execution, and unsupported component execution remain absent |
| `feature.plugins-skills` | partial |  |  | installed plugin skills support manifestless/default layouts, root SKILL.md, and explicit skill directories, including a directory directly containing SKILL.md; individual files are not skill declarations. Installed qualified name@marketplace identity owns selection, variable/data context, and lazy persistent-data creation, while a present validated kebab-case manifest name owns the visible skill namespace; manifestless content uses the validated installed lifecycle name. A wrong-typed skills declaration is VERIFIED rejected; invalid explicit paths cause PiCC-defined whole-plugin rejection rather than component-only fallback |
| `feature.proactive-compaction-policy` | partial |  |  | PiCC HARDENING: configurable proactiveCompactPercent defaults to 90 (valid 50–95). Fresh successful tool-requesting assistant usage can queue threshold pressure; already-requested tools finish, and turn_end handles the complete batch. before_provider_request is the final idle-to-armed sample; when newly known pressure crosses the threshold, its admission abort blocks ordinary provider transport. agent_settled is the only boundary that may start one physical Pi compaction transaction if the checkpoint is still required, and only after no provider response or tool batch remains unresolved. Summary transport and retry behavior follows feature.compaction-summary-recovery. Admission is limited to main sessions and PiCC-created children using Pi's APIs: openai-completions, openai-responses, and openai-codex-responses. Main logical cancellation joins bounded Pi settlement; PiCC authenticates starts by an opaque Pi-preserved details-envelope identity, and only exact aborted terminal object identity followed by same-run selected-branch settlement permits retained-input reconciliation, with TUI steering/follow-up/draft order and no automatic replay. Only the TUI may remain reusable after complete custody. Print/JSON remain retrieve-and-relaunch partial outcomes with exit status 3. Live RPC post-compaction retained-replay cancellation is unsupported because Pi 0.83 abort does not reclaim its native queues: any authenticated cancellation outcome PiCC can present has a distinct confirmed restart-required terminal category, is restart-process with status 3, leaves controller admission closed, refuses in-process new/resume/fork/reload replacement, and requires an external fresh PiCC process plus fresh session rather than same-session resubmission; Pi may drain those queues before PiCC can present the outcome. Restoration/start failures say continuation was not confirmed to start, while later stages warn that first-run effects may exist and no second run is automatic. Child cancellation physically aborts through the SDK; confirmed child input becomes one canonical report shared by all consumers, while ambiguous records remain quarantined; confirmed shutdown makes one bounded best-effort persistence attempt before destructive retained cleanup, emits an exact verified locator on success or an explicit possible-loss warning on storage failure, and hook, stale-generation, and other post-commit failures retain category-appropriate safe outcomes. Success resumes through a synthetic persisted, model-visible continuation and reconciles queued input; confirmed pre-commit operational or hook exhaustion is manually recoverable only while the live or persisted session remains available, while post-commit restoration, replay, or continuation failure requires replacement and never recompacts the committed summary. UNCONFIRMED-HOST EXCEPTION: if a main-session callback or main-session resumed cancellation/join misses its bounded deadline, elapsed time does not establish host quiescence; admission, manual recovery, and in-process replacement remain closed. Recovery requires a fresh process and fresh session using externally retained input; the affected session must not be reopened. The settled-run check remains a non-resuming fallback. PiCC lifecycle output strips provider diagnostics; native physical agent_end / RPC compaction-error records remain Pi-owned and cannot be suppressed or redacted. Tier PARTIAL because proactive admission and continuation are PiCC-specific rather than verified Claude parity |
| `feature.read.images` | partial |  |  | image ingestion through Read — a read image file, and a raster image OUTPUT inside a .ipynb, is normalized through the Pi image pipeline (Pi's own defaults: ~2000px longest side, JPEG re-encode over a byte cap; described as PiCC's own normalization, NOT asserted byte-identical to Claude Code) and delivered as a real image content block ON A VISION-CAPABLE MODEL. Tier PARTIAL because a single entry has one tier while the behavior SPLITS on vision: on a non-vision model the same read degrades to a model-visible text note instead of an image block (never a silent drop or garbled text). Classification is byte-based (magic bytes), not extension-based. The image-FILE path is inherited from base Pi and preserved here; PiCC adds the notebook/binary routing around it — see tool.Read |
| `feature.selected-main-agent` ⚠ | partial |  | `agent.frontmatter.disallowedTools`, `agent.frontmatter.hooks`, `agent.frontmatter.initialPrompt`, `agent.frontmatter.mcpServers`, `agent.frontmatter.permissionMode`, `agent.frontmatter.tools`, `setting.agent` | A CLI --agent value or resolved agent setting selects a Claude-format custom agent as the main identity, with CLI > persisted selected-branch identity > setting precedence independent of PiCC's managed > nearest project > user > lower-precedence plugin definition precedence. Claude's separate CLI-defined --agents definitions are unsupported and do not participate. One immutable session snapshot replaces the prompt every turn and applies tool/catalog restrictions, supported model/effort, skills, memory, hooks, MCP, and an optional once-only initial user message. Selection and initialPrompt are proved synchronously on the live branch before provider admission, but a new persisted session becomes reopenable from disk only after Pi persists it, currently after the first assistant response. Once persisted, initialPrompt is not replayed and selection is re-resolved on resume. Fresh missing or invalid selection blocks provider work; missing or uncertain persisted evidence installs a prominent no-tools recovery identity. PARTIAL because selected-main skill preload/retention and memory application are PiCC-defined and unverified, as are prompt construction, selector persistence/collision behavior, active-tool reconciliation, model ordering/fallback, diagnostics, safe fallback, hook/MCP lifecycle, and terminal presentation; permissionMode is a safety-relevant no-op, and maxTurns/background/isolation/color have no selected-main behavior |
| `feature.session-scratchpad` | partial |  |  | per-session native-safe scratchpad: an eager per-session scratch dir (os.tmpdir()/picc-scratch-*, honoring Claude's CLAUDE_CODE_TMPDIR relocation knob) whose LITERAL resolved path is injected into the system prompt on ALL platforms with an imperative "always use this instead of /tmp" directive plus the "only use /tmp if the user explicitly requests it" escape hatch — mirroring Claude Code's injected-literal-path scratchpad contract (a literal path in the prompt, no env var, matching Claude). PARTIAL for two honest gaps: (a) the scratch PATH SHAPE differs from Claude's — Claude's Windows scratchpad is a backslash %LOCALAPPDATA%\...\claude\...\scratchpad path whereas PiCC injects the forward-slash C:/... form (via toNativeSafeTempForm) so the Bash tool and native Read resolve it to the same file; that separator difference is the deliberate fix, not an accident (on Unix it is os.tmpdir()/picc-scratch-* vs Claude's /tmp/claude/<session>/scratchpad — semantically equivalent, not byte-identical); (b) on the Windows shell↔native namespace split (pinned Git Bash) PiCC ADDITIONALLY emits a Windows-specific mktemp -p recipe note that Claude does not, an additive PiCC mitigation for the bare-/tmp drive-relative resolution trap. Skill-author-facing delivery is this injected prompt guidance — no env var and no path rewriting (see "Mechanical-fidelity decisions (load-bearing)" in doc/architecture.md) |
| `feature.tool-output-clip` | partial |  |  | tool-result clip backstop — a single tool result whose text block exceeds a generous, PiCC-configurable token budget (clipMaxTokens, default 20k tokens ≈ 80k chars, converted once via bytes/4) is bounded before it enters model context: head + tail kept, middle dropped, replaced by a distinctive model-visible `[PiCC clipped <N> characters …]` marker carrying a tool-appropriate recovery hint (Read → re-read a narrower offset/limit range; Grep → tighter pattern or smaller head_limit/offset; Bash/MCP proxy/MCP resource tool/subagent → narrow the command). MCP prompt results do not traverse tool_result and instead use a separate aggregate transformed-input bound before entering the user turn (see feature.mcp-prompts). The marker travels in-band on the canonical model-visible result; compact Grep/Glob human rendering summarizes that clipping state instead of displaying the clipped match body. This is PiCC HARDENING, NOT Claude parity and NOT byte-identical to any Claude behavior. DIRECTIONAL DIVERGENCE: the clip is a HIGH backstop ABOVE Claude's own bounding — Claude errors a Read at ~25k tokens (VERIFIED) and truncates Bash at ~30k chars (a less-firmly-sourced figure than the 25k-token Read error), whereas PiCC's default budget (~20k tokens/~80k chars per text block) sits above both, so mid-band outputs Claude would trim pass through PiCC UNTOUCHED; the intent is only to stop the pathological, window-blowing result, not to reproduce Claude's thresholds, and the marker/hint/count wording is PiCC-authored (INFERRED, never matched against Claude's marker strings). Coverage is the results Pi does NOT already bound — Claude-named tools, subagent/Task dispatch results, MCP outputs — and the clip fires at BOTH the main and subagent guard seams; only text blocks are ever sliced (image/data blocks pass through untouched). CONSUMER NOTE (parity Q5): when the clip fires the result is TRUNCATED — the middle is dropped and a marker spliced in — so a strict exact-token / JSON / YAML consumer of that (already-oversized) result must account for the truncation itself, not merely the added marker text: bytes from the middle are gone, and recovery is the tool-appropriate re-run hint. Built-in Read/Bash keep Pi's OWN 50 KB truncation (applied below this budget, before the tool-result chokepoint) and do NOT receive the head+tail+hint treatment — see tool.Read / tool.Bash. Tier PARTIAL: a deliberate hardening backstop rather than a reproduction of Claude's own output bounds. SEPARATE PRE-EXISTING DIVERGENCE, disclosed here but NOT fixed by this backstop: Pi head-truncates built-in Read at 50 KB (~12.5k tokens) where Claude errors at 25k tokens — a different bound than Claude's, left as a follow-up and not remedied here |
| `feature.hook-handler.agent` | degraded-noop |  |  | agent hook handlers degrade with a notice |
| `feature.hook-handler.mcp_tool` | degraded-noop | documented: Hooks reference — MCP tool hook fields (2026-07-31) | `feature.hook-handler.mcp_tool-blocking-enforcement`, `feature.mcp`, `feature.mcp-hook-matching` | mcp_tool hook handlers degrade with a notice. Claude requires an already-connected server and never initiates a connection or OAuth; plugin-scoped `server`, `tool`, and `input` fields are unavailable. Text output is interpreted like command-hook stdout; disconnected-server and isError results are non-blocking. The handler is available across hook events, with a documented SessionStart/Setup early-connection race; none of that handler contract runs in PiCC, while ordinary MCP tools themselves still run |
| `feature.hook-handler.mcp_tool-blocking-enforcement` ⚠ | degraded-noop | documented: Hooks reference — MCP tool hook fields (2026-07-31) | `feature.hook-handler.mcp_tool`, `feature.mcp`, `feature.mcp-hook-matching` | mcp_tool handlers configured on deny/block-capable hook events degrade with a notice, so PiCC cannot enforce valid deny/block output and the operation can proceed. For events where PiCC enforces command-hook blocking results, use a supported command hook; use Claude Code when WorktreeCreate creation-time enforcement or other unavailable enforcement is required |
| `feature.hook-handler.prompt` | degraded-noop |  |  | prompt hook handlers degrade with a notice |
| `feature.plan-mode` | degraded-noop |  |  | plan mode treated as guidance; no mode switch, no ExitPlanMode gate |
| `feature.telemetry-otel` | degraded-noop |  |  | telemetry/OTEL settings parsed, nothing exported |
| `feature.agent-teams` | not-supported |  |  | agent teams out of scope; names degrade safely |
| `feature.artifacts` | not-supported |  |  | Artifacts out of scope; names degrade safely |
| `feature.checkpointing-rewind` | not-supported |  |  | no rewind parity — relies on Pi's session model |
| `feature.computer-use` | not-supported |  |  | computer use out of scope; names degrade safely |
| `feature.cron` | not-supported |  |  | scheduled tasks out of scope; names degrade safely |
| `feature.lsp` | not-supported |  |  | LSP integration out of scope; names degrade safely |
| `feature.managed-policy-windows-registry` ⚠ | not-supported |  | `feature.managed-policy` | Windows registry managed-policy delivery is intentionally unsupported: HKLM and HKCU are neither queried nor probed. Registry-only policy is silently ignored and contributes no settings, diagnostics, enforcement, cleanup blocking, or fail-closed behavior. Use supported administrator-owned system managed settings files or ordered drop-ins when policy must apply |
| `feature.mcp-auto-background` | not-supported | documented: MCP reference — Automatic backgrounding of long tool calls (2026-07-31) | `feature.mcp-first-byte-timeout`, `feature.mcp-idle-timeout` | MCP tool-call auto-backgrounding is unavailable: CLAUDE_CODE_MCP_AUTO_BACKGROUND_MS, CLAUDE_CODE_DISABLE_BACKGROUND_TASKS, non-interactive CLAUDE_AUTO_BACKGROUND_TASKS opt-in, subagent/IDE/noninteractive exclusions, and elicitation-dialog deferral are not implemented for MCP calls. Calls run in the foreground, bounded by the resolved tool timeout |
| `feature.mcp-channels` | not-supported | documented: CLI reference — CLI flags (2026-07-31); documented: Claude Code channels — Enterprise controls (2026-07-31) | `feature.mcp`, `setting.allowedChannelPlugins`, `setting.channelsEnabled` | MCP channels and their `--channels` and `--dangerously-load-development-channels` loading controls are unavailable |
| `feature.mcp-child-session-env` | not-supported | documented: MCP reference — Option 3: Add a local stdio server (2026-07-31); observed: Claude Code 2.1.218 binary — observed stdio environment sanitization path | `feature.mcp` | PiCC does not remove inherited CLAUDE_CODE_CHILD_SESSION from stdio MCP environments; Claude removal is documented and owner-reported, while PiCC behavior remains unchanged |
| `feature.mcp-cli-invocation-controls` | not-supported | documented: CLI reference — CLI flags (2026-07-31) | `setting.disableSideloadFlags`, `setting.mcpServers` | Claude CLI invocation/loading controls `--mcp-config`, `--strict-mcp-config`, `--permission-prompt-tool`, `--bare`, and `--safe-mode` are unavailable; `--bare` skips MCP server discovery generally and `--safe-mode` disables MCP loading, while PiCC uses its normal project discovery and permission posture. These unsupported controls do not disable or narrow supported agent-inline declarations |
| `feature.mcp-connect-timeout-ms` | not-supported | documented: MCP reference — Push messages with channels (2026-07-31); observed: Claude Code 2.1.218 binary — observed 5000 ms default on one connect path | `feature.mcp-first-byte-timeout`, `feature.mcp-server-always-load` | PiCC ignores MCP_CONNECT_TIMEOUT_MS and uses MCP_TIMEOUT. Owner-reported Claude Code 2.1.218 binary observation covers one connect path with a 5000 ms default; broader semantics are not claimed |
| `feature.mcp-connectors` | not-supported | documented: MCP reference — Use MCP servers from claude.ai (2026-07-31) | `feature.mcp-claude-json-scopes`, `feature.mcp-oauth`, `setting.disableClaudeAiConnectors` | claude.ai connectors (account-level remote servers) — out of scope |
| `feature.mcp-elicitation` | not-supported | documented: MCP reference — Respond to MCP elicitation requests (2026-07-31) | `hook.event.Elicitation`, `hook.event.ElicitationResult` | MCP elicitation (server-initiated user prompts) is deferred; the current hook.event.Elicitation and hook.event.ElicitationResult surfaces are parsed and never fired. hook.event.mcp__elicitation is only a historical PiCC alias |
| `feature.mcp-first-byte-timeout` | not-supported | documented: MCP reference — Push messages with channels (2026-07-31) | `feature.mcp`, `feature.mcp-auto-background`, `feature.mcp-connect-timeout-ms` | Claude's first-response-byte timer defaults to 60 seconds for HTTP, SSE, and connectors, with documented asymmetric environment/per-server override and floor rules; stdio and WebSocket have no first-byte timer. PiCC instead uses aggregate hard wall-clock bounds, not first-byte timers |
| `feature.mcp-headers-helper` | not-supported | documented: MCP reference — Use dynamic headers for custom authentication (2026-07-31) | `feature.mcp-oauth`, `feature.mcp-remote-transports`, `feature.mcp-websocket` | headersHelper on MCP server entries is deferred; an explicit remote entry using it is skipped safely |
| `feature.mcp-idle-timeout` | not-supported | documented: MCP reference — Push messages with channels (2026-07-31) | `feature.mcp-auto-background`, `feature.mcp-remote-transports` | PiCC does not implement Claude's transport-specific idle timers: HTTP/SSE/WebSocket/connectors and stdio have different defaults; progress notifications reset or avoid idle expiry; IDE/SDK in-process calls are excluded; CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT=0 disables the timer; and a per-server timeout is an idle-timeout floor. Remote transport or server loss may still enter recovery or terminal failure |
| `feature.mcp-list-changed` | not-supported | documented: MCP reference — Dynamic tool updates (2026-07-31) | `feature.mcp-prompts`, `feature.mcp-resource-subscriptions`, `feature.mcp-resources` | tools/list_changed, prompts/list_changed, and resources/list_changed dynamic refresh are unsupported; same-definition catalogs remain immutable. Claude retains previous catalogs when a list_changed refresh fails, but PiCC never attempts that refresh. An explicit PiCC administration change can refresh a changed execution definition through host registration; this is not list_changed support (see tool.mcp__*, feature.mcp-prompts, and feature.mcp-resources) |
| `feature.mcp-max-result-size-chars` | not-supported | documented: MCP reference — MCP output limits and warnings (2026-07-31) | `feature.mcp-output-token-cap`, `feature.tool-output-clip` | tool _meta["anthropic/maxResultSizeChars"] is discarded; PiCC does not apply the per-tool text persistence threshold override (capped by Claude at 500,000 characters), and its generic clipping is not equivalent, does not alter Claude's fixed warning threshold, and does not move image content off token limits |
| `feature.mcp-oauth` | not-supported | documented: MCP reference — Authenticate from the command line (2026-07-31); documented: MCP reference — Authenticate with remote MCP servers (2026-07-31); documented: MCP reference — Use a fixed OAuth callback port (2026-07-31); documented: MCP reference — Use pre-configured OAuth credentials (2026-07-31) | `feature.mcp-connectors`, `feature.mcp-remote-transports` | `/mcp authenticate` is a PiCC-defined inert entry point that performs no login, logout, token storage, refresh, or runtime action. The `oauth` field is ignored and the server still runs when otherwise usable; OAuth behavior is absent, so users must not rely on `oauth` under PiCC. Missing behavior includes 401/403 auth detection; automatic token refresh, reconnect, and one retry; re-authenticate/Clear authentication and startup auth-needed paths; ToolSearch-enabled headless model visibility; HTTP/SSE-only credential applicability; login/logout and `login --no-browser`; `--client-id`, masked `--client-secret`, and MCP_CLIENT_SECRET; fixed callback port; preconfigured credentials, CIMD, dynamic registration, authServerMetadataUrl, pinned oauth.scopes precedence, offline_access, and insufficient_scope reauthentication. A static headers.Authorization value prevents Claude's OAuth fallback; configured static headers are PiCC's supported alternative where applicable |
| `feature.mcp-output-token-cap` | not-supported | documented: MCP reference — MCP output limits and warnings (2026-07-31) | `feature.mcp-max-result-size-chars`, `feature.tool-output-clip` | MAX_MCP_OUTPUT_TOKENS — not honored. The generic tool-output clip backstop bounds MCP proxy/resource-tool results (see feature.tool-output-clip), while transformed MCP prompt content uses its separate aggregate input bound (see feature.mcp-prompts) |
| `feature.mcp-plugin-servers` | not-supported | documented: MCP reference — Plugin-provided MCP servers (2026-07-31) | `feature.mcp`, `feature.plugins-content` | plugin-bundled MCP servers are deferred: PiCC discovers no plugin MCP configs, so it lacks their lifecycle and reload behavior, placeholder expansion/substitution, scoped `mcp__plugin_<plugin>_<server>__<tool>` tool and server naming, and transport-specific substitution/support behavior. Use Claude Code for plugin-provided MCP servers (feature.plugins-content carries the qualifying clause) |
| `feature.mcp-requires-user-interaction` ⚠ | not-supported | documented: MCP reference — Require approval for a specific tool (2026-07-31) | `tool.mcp__*` | tool _meta["anthropic/requiresUserInteraction"] is discarded, so PiCC does not enforce Claude's mandatory per-call human confirmation and users must not rely on this gate |
| `feature.mcp-resource-subscriptions` | not-supported | unverified: MCP reference — Use MCP resources (2026-07-31) | `feature.mcp-list-changed`, `feature.mcp-resources` | resource subscriptions and update notifications beyond resources/list_changed are unavailable; protocol existence alone does not establish Claude Code exposure |
| `feature.mcp-resource-templates` | not-supported | unverified: MCP reference — Use MCP resources (2026-07-31) | `feature.mcp-resources` | MCP resource templates (resources/templates/list and templated URI discovery) are unavailable; fixed resource tools expose only definition-bound resources/list snapshots and exact caller-supplied URI reads, while Claude Code exposure remains unverified |
| `feature.mcp-roots` | not-supported | documented: MCP reference — Option 3: Add a local stdio server (2026-07-31) | `feature.mcp` | MCP roots, roots/list, and roots/list_changed are unavailable; servers are not told workspace roots |
| `feature.mcp-runtime-enabled` | not-supported | documented: MCP reference — Managing your servers (2026-07-31); inferred: Private native state — enabledMcpServers persistence and list interpretation are not documented | `feature.mcp-control-status`, `feature.mcp-project-approval`, `feature.mcp-runtime-disabled` | the selected native project's `enabledMcpServers` list is recognized and reported but never activates Claude's default-off built-in servers. Its exact private-state persistence and list interpretation are inferred rather than documentation-verified; absent enablement blocks capability rather than permitting a restricted operation, so this gap is not safety-relevant |
| `feature.mcp-sampling` | not-supported | unverified: MCP reference — Scale with MCP tool search (2026-07-31) | `feature.mcp` | MCP server sampling requests are unavailable and Claude Code exposure remains unverified; servers cannot ask PiCC to run model generations, so this absence is not safety-relevant |
| `feature.mcp-server-always-load` | not-supported | documented: MCP reference — Scale with MCP tool search (2026-07-31) | `feature.mcp-connect-timeout-ms`, `feature.mcp-tool-search`, `tool.WaitForMcpServers` | server alwaysLoad is recognized and ignored; the server still runs, but receives neither the tool-search exemption nor Claude's startup barrier bounded by the standard 5-second connect timeout. Check `/mcp` readiness instead, and use Claude Code if the startup guarantee is required |
| `feature.mcp-server-instructions` | not-supported | documented: MCP reference — Scale with MCP tool search (2026-07-31) | `feature.mcp` | MCP server instructions and Claude's documented truncation handling are not forwarded into model context |
| `feature.mcp-server-mode` | not-supported | documented: MCP reference — Use Claude Code as an MCP server (2026-07-31) | `feature.mcp` | Claude Code host mode (`claude mcp serve`) is unavailable; PiCC is an MCP client only |
| `feature.mcp-server-role` | not-supported | unverified: MCP reference — Installing MCP servers (2026-07-31) | `setting.mcpServers` | server role is recognized and ignored; the server still runs, but current Claude behavior is unverified, so users must not rely on the field under PiCC |
| `feature.mcp-shell-prefix` | not-supported | documented: MCP reference — Option 3: Add a local stdio server (2026-07-31); observed: Claude Code 2.1.218 binary — observed stdio spawn prefix path | `feature.mcp` | PiCC does not honor CLAUDE_CODE_SHELL_PREFIX for stdio MCP spawning; owner-reported Claude Code 2.1.218 binary provenance reports prefixing on the observed spawn path |
| `feature.mcp-tool-always-load` | not-supported | documented: MCP reference — Scale with MCP tool search (2026-07-31); unverified: MCP reference — Scale with MCP tool search (server-startup-barrier coupling for the per-tool form is not specified) (2026-07-31) | `feature.mcp-server-always-load`, `feature.mcp-tool-search` | tool _meta["anthropic/alwaysLoad"] is discarded, so the tool is not forced into upfront loading; whether Claude also applies the server startup barrier for this per-tool form remains unverified |
| `feature.mcp-tool-search` | not-supported | documented: MCP reference — Scale with MCP tool search (2026-07-31) | `feature.mcp-model-failure-visibility`, `feature.mcp-server-always-load`, `feature.mcp-tool-always-load`, `tool.ToolSearch`, `tool.WaitForMcpServers` | Claude defaults to MCP tool search where the model/provider/deployment supports it, otherwise loads schemas upfront; ENABLE_TOOL_SEARCH, MAX_MCP_TOOLS_BEFORE_DEFER, search/deferred/alwaysLoad selection, and ToolSearch-versus-WaitForMcpServers fallback are not implemented. PiCC always sends full schemas upfront. Ordinary main-session failed-connection evidence remains human-visible only; selected-main setup failure denies admission, while dispatched-agent setup degradation reaches that child through bounded PiCC-defined warning context before its first request |
| `feature.mcp-url-without-type-validation` | not-supported | documented: MCP reference — Option 1: Add a remote HTTP server (2026-07-31) | `feature.mcp-remote-transports`, `setting.mcpServers` | Claude skips an MCP entry with `url` and no `type` as a configuration error. PiCC can instead treat an untyped entry with a valid `command` as stdio and ignore its URL; set an explicit `type` and do not rely on Claude's validation behavior under PiCC |
| `feature.mcp-websocket` | not-supported | documented: MCP reference — Option 4: Add a remote WebSocket server (2026-07-31) | `feature.mcp-cli-management`, `feature.mcp-remote-transports` | Claude's documented WebSocket (`type: "ws"`) transport is unavailable in PiCC. PiCC supports stdio, HTTP, and SSE alternatives; if the server is WebSocket-only, there is no unchanged-project PiCC path. Claude configures WebSocket through `.mcp.json` or `mcp add-json` with URL, static or helper headers, timeout, and alwaysLoad; it has no OAuth or `mcp add --transport ws` path |
| `feature.plugin-lifecycle-automation` | not-supported |  |  | unattended updates, release channels, stable headless JSON, dependency automation, enterprise distribution, and plugin authoring/evaluation commands are deliberately unsupported |
| `feature.plugins-development-trust` | not-supported |  |  | trusted development/sideload activation is not implemented; repository-bundled and development plugin roots remain inert, and settings or environment paths cannot authorize them |
| `feature.plugins-other-components` | not-supported |  |  | plugin-provided LSP, workflows, output styles, top-level and experimental themes and monitors, channels, and other component runtimes are deferred; manifests and catalogs retain bounded shape-only inventory evidence, while MCP has its own feature.mcp-plugin-servers entry |
| `feature.read.pdf` | not-supported |  |  | PDF reading — Claude Code reads PDFs at baseline (since ~v1.0.67); PiCC returns the binary error. Stated as deliberately BELOW the Claude baseline (NOT a claim that Claude also errors on PDF); actual PDF parsing is a deferred follow-up. The deficit is disclosed via the runtime Claude-style binary error ("...looks like a PDF, which PiCC does not support reading yet") and via the support-matrix table / this registry entry, and is cross-referenced from tool.Read — so the gap is discoverable rather than hidden inside Read's full tier. (NOTE: /doctor does not NAME this entry — it lists only project-triggered findings plus anonymous per-tier counts, so PDF surfaces there only within the not-supported count, never as a labelled row.) |
| `feature.remote-control` | not-supported |  |  | remote control out of scope; names degrade safely |

## Summary

The registry enumerates **246 capabilities** against baseline `claude-code-2.1.x (mid-2026)`: **59 full**, **98 partial**, **36 degraded-noop**, **53 not-supported**. 23 entries are safety-relevant — a divergence where a project's restriction is not enforced, marked ⚠ in this matrix. Unknown inputs outside this registry are not counted here: they are unassessed by definition and degrade safely at runtime.
