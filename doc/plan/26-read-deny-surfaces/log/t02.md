# t02 execution log — docs, CHANGELOG, and capability-registry

## What changed
- **`doc/user-guide.md` §6** — extended the deny-grammar bullet: after the existing grammar list, a
  `Read(<glob>)` deny is now stated to also gate `Grep`/`Glob`/`NotebookRead` on a matching path
  (`Grep`/`Glob` = documented Claude best-effort parity; `NotebookRead` = inferred defense-in-depth),
  and noted the one-directionality (a `Grep(<glob>)` rule does not gate `Read`). Added a nested
  *Honesty caveat* sub-bullet using the PINNED t01 facts: a scoped `deny: Read(secrets/**)` blocks
  `{path:"secrets"}` AND `{path:"secrets/x"}` (bare directory node IS covered); the genuine residual
  gap is a read with **no path** / `path:"."` (e.g. `Grep {}`) whose results can still surface
  matching content; only a bare `deny: Read` (context removal) forecloses that. Did NOT use the
  earlier-draft's wrong "naming the bare directory may not match" wording.
- **`doc/architecture.md`** — added a read-family sentence parallel to the existing edit-family
  description under `tools/`: `Read(…)` gates the file-read family one-directionally across
  Grep/Glob/NotebookRead; `Grep(…)`/`Glob(…)` does not gate `Read` — same shape as `Write` not
  gating `Edit`.
- **`CHANGELOG.md`** — new `### Changed — Read denies gate the whole read family (2026-07-16)` entry
  at top of `[Unreleased]`. Chose `### Changed` (matches a heading form still present in Unreleased)
  over `Added`, since this closes a parity/security gap in existing behavior. Included the honest
  no-path limit.

## Registry decision
- CHANGED the registry source clause for `setting.permissions.deny` (kept tier `full`) to add
  "; a Read(<glob>) deny also gates Grep/Glob/NotebookRead on a matching path, one-directionally
  (§6.1)". Rationale: feature.md acceptance requires the registry/matrix to accurately describe the
  read-family coverage; the prior generic note did not. Tier unchanged — the expansion makes the
  entry more faithful, not less.
- Ran `npm run gen:capabilities` (required because registry text changed): "Wrote
  doc\supported-features.md (150 capabilities)". Line 105 updated in lockstep. `git status` shows
  only the five intended files changed — no stray generated-file drift.

## Fixture decision (recorded per t02)
- Did NOT grow a parallel Grep/Glob e2e fixture. `examples/full-surface/.claude/settings.json`
  already ships `deny: ["Read(secrets/**)"]` and the guard→engine path is generic once
  `ruleToolMatches` expands; the t01 unit tests fully prove the contract. A duplicate fixture would
  be dead weight. Conscious choice, not an oversight.

## Verification
- `npm run typecheck`: clean.
- `npm test`: full suite green (see report). `test/registry.test.ts` freshness guard passes with the
  regenerated matrix.

## Deviations
- None. Did not touch `src/engine/permissions.ts` or any test (t01's surface).
