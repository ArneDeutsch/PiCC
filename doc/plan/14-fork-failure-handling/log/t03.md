# t03 execution log — registry/docs/CHANGELOG truthfulness

## Registry note (capability-registry.ts:174, skill.frontmatter.context)

Chosen wording (replaces the bare "context: fork runs the skill as a fresh-context subagent (§4.1)"):

> context: fork runs the skill as a fresh-context subagent; a fork that fails on a terminal
> error is a LOUD failure naming the cause with partial output preserved (parity with the
> Agent tool, 2.1.199); F14 threads the Esc signal to a model-invoked context: fork (the
> Skill-tool path) so it reports aborted, but a typed top-level /forked-skill expansion is
> NOT Esc-cancellable — a PiCC/Pi harness limitation (no abort signal at the input-hook
> stage), not Claude Code scoping Esc; fork dispatches are non-resumable (§4.1)

- Failure = partial preserved + cause named, framed as parity with the Agent tool.
- Esc-aborted scoped to the model-invoked Skill-tool caller; phrased as "F14 threads the Esc
  signal" (not "Esc cancels a nested fork" — nested cross-session delivery is Pi's, unverified).
- Typed /forked-skill non-cancel attributed to a PiCC/Pi harness limitation (no abort signal at
  the input-hook stage), explicitly NOT Claude Code scoping Esc.
- No unqualified "Esc → aborted". Added the non-resumable half-clause per the optional note.
- Version cited is 2.1.199 (the partial-output/loud-failure fix), not 2.1.200.

## doc/supported-features.md

Regenerated via `npm run gen:capabilities` (148 capabilities). Not hand-edited. Line 153 now
carries the new note. Matrix-freshness guard (test/registry.test.ts:403) passes — no drift.

## doc/architecture.md (Subagent error contract section)

Added a "One presentation for every dispatch (F14)" bullet: the context: fork path was the
last divergence and now conforms; t01 extracted the shared exported presentDispatchResult
helper that the Agent tool plus both fork consumers route through; scoped-Esc caveat kept
consistent with the registry (harness limitation, not Claude scoping).

## doc/user-guide.md:192 check — OUTCOME: scope clause ADDED

The line "Pressing Esc cancels a running foreground dispatch" is generic. A typed
/forked-skill IS a synchronous foreground dispatch but is not Esc-cancellable, so the generic
phrasing could be read as covering it (over-claim by omission). Added a one-clause scope note:
Esc covers Agent/Task and a model-invoked context: fork (Skill tool path), but NOT a typed
/forked-skill expansion (PiCC/Pi harness limitation: no abort signal at the input-hook stage).

## CHANGELOG.md

Added an entry under a new "### Fixed — context: fork failure preservation and Esc abort
(2026-07-14)" heading at the top of [Unreleased], matching the repo's dated-heading Fixed
style. Covers: loud failure + partial preservation (parity, 2.1.199), shared
presentDispatchResult helper, Esc-aborted scoped to the model-invoked Skill-tool fork, typed
/forked-skill harness limitation, non-resumable. Cites 2.1.199 (not 2.1.200).

## Verification

- npm run gen:capabilities — clean regen, no drift.
- npm run typecheck — green.
- npm test — Test Files 44 passed | 1 skipped; Tests 1046 passed | 16 skipped (matches baseline).

## Friction

None. presentDispatchResult confirmed exported at src/runtime/subagents.ts:1439 before citing it
in architecture.md. Stayed within the writable surface; no src/index.ts, subagents.ts, or test edits.
