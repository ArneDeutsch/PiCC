# t02 execution log — system-prompt scratchpad injection

- Added `shellNamespaceDiffersFromNative(platform?: NodeJS.Platform)` to
  `src/engine/shell-inject.ts` next to `resolveGitBashPath`: exact
  `(platform ?? process.platform) === "win32" && resolveGitBashPath() !== undefined`.
  Injectable platform param mirrors `toNativeSafeTempForm`. Uses cached
  `resolveGitBashPath` so it's free on the per-turn hot path. Doc comment records the
  deliberate exclusion of bare-bash/WSL.
- Threaded two optional `AssemblyInputs` fields in `src/runtime/context-assembly.ts`:
  `scratchDir?: string` and `windowsTempNote?: boolean`. No `engine/` import added
  there — both values are computed in `index.ts` (composition root) and passed at the
  `buildSystemPromptSuffix` call, mirroring `steeringText`/`autoMemory`. Did NOT copy
  `compatNotice` (dead wiring, never passed).
- Injection is a NEW pushed section (`buildScratchpadSection` helper), emitted only when
  `scratchDir` is set. `HARNESS_CONVENTIONS` const untouched. Section header
  `## Scratch directory`. Always-line names the LITERAL path with an "instead of `/tmp`"
  directive (Claude-faithful, not an env-var reference). Windows note appended only when
  `windowsTempNote === true`, covering all four wording constraints: mandatory quoted
  `mktemp -p "<scratchDir>"`; bind to scratch dir only / never `$TEMP`/`$TMP`/`cygpath`;
  the why-clause (bare `/tmp/...` resolved drive-relative by native tools vs. forward-slash
  drive-letter resolved identically by both); framed as preferred default, not an override
  of a skill's explicit path instructions.
- `src/index.ts`: extended the import to also pull `shellNamespaceDiffersFromNative`; passed
  `scratchDir` (the t01 eager variable) and `windowsTempNote: shellNamespaceDiffersFromNative()`
  at the `before_agent_start` call site.
- Placement decision (Left open): section pushed last, after the `compatNotice` section. Since
  it only emits when `scratchDir` is set, off-Windows non-scratchpad output is unchanged
  byte-for-byte regardless of placement.
- Wording decision (Left open): the Windows note references `mktemp -p "<scratchDir>"` with the
  literal path already in forward-slash `C:/…` form (t01 owns the value). Guidance does NOT
  reconstruct or re-slash the path, so a `C:/…` forward-slash path is safe — no dependency on
  the backslash-conversion caveat from t01's security note.
- Tests: added `shellNamespaceDiffersFromNative` block to `test/skills.test.ts` (linux/darwin
  false; win32 branch = `resolveGitBashPath()!==undefined` structural equality that holds on any
  host; default-platform delegation). Added three injection tests to `test/runtime-core.test.ts`
  context-assembly describe: literal-path + instead-of-/tmp on all platforms; Windows note
  conditional on the flag (all four constraints asserted) present/absent; and scratchDir-undefined
  produces byte-for-byte identical output even with the flag forced (locks off-Windows unchanged +
  Windows-note-absent). Existing `Claude Code compatibility conventions` assertions still green.
- Verification: `npm run typecheck` clean. `npm test` → 1249 passed / 16 skipped (baseline
  1243/16; +6 new). No new failures.
- No deviations from the spec.
