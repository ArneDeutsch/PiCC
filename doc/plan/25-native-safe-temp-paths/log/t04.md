# t04 execution log — capability registry entry + matrix regen + contract docs

- **Registry entry.** Added `feature.sessionScratchpad` to `FEATURE_ENTRIES` in
  `src/registry/capability-registry.ts` (placed immediately before `feature.managed-policy`),
  matching the neighboring `cap("feature", id, tier, note)` shape (no `safetyRelevant` — not
  safety-relevant). Tier **`partial`**, single-line note (no `\n`, so the invariants test passes).
  It is a `feature.*` entry per spec — NOT an overload of `tool.Bash`.
- **Honest gaps.** The `partial` justification names the two real fidelity gaps: (a) scratch PATH
  SHAPE differs (`os.tmpdir()/picc-scratch-*` vs Claude's `/tmp/claude/<session>/scratchpad`) —
  semantically equivalent, not byte-identical; (b) on the Windows shell↔native namespace split PiCC
  ADDITIONALLY emits a Windows-specific `mktemp -p` recipe note Claude does not (additive mitigation).
  Explicitly framed the literal-path/no-env-var contract as **parity** with Claude, so `partial` is
  NOT justified by the absence of an env var. Noted `CLAUDE_CODE_TMPDIR` is honored (Claude's
  relocation knob → not a gap).
- **`(§)` cross-ref decision (Left open).** Existing feature notes end with `(§X)` pointing into
  `doc/plan/picc-plan.md`; this feature has no picc-plan.md home. Chose to cite the docs home
  **explicitly and unambiguously as `(architecture.md §4)`** rather than a bare `(§4)` — a bare `(§4)`
  in the generated matrix would read as picc-plan §4 and mislead. `architecture.md §4` resolves
  (that's where the new contract bullet lives). Did not omit the pointer, since a resolving pointer is
  more useful and consistent with sibling entries' style.
- **Matrix regen.** Ran `npm run gen:capabilities` → regenerated `doc/supported-features.md`
  (now 151 capabilities; partial count +1). Verified idempotence by running it twice and comparing
  md5 — identical hash (`960124637df6…`). `test/registry.test.ts` green (51/51), including the
  in-process freshness guard that diffs the committed matrix against a fresh render.
- **architecture.md — new §4 bullet + three stale-passage fixes** (verified line numbers myself; they
  had shifted slightly from the spec's cited ranges but the passages were unambiguous):
  - §4 "Mechanical-fidelity decisions": added a "per-session native-safe scratchpad steers temp files
    off bare `/tmp` (#48)" bullet between the "UTF-8 subprocess env" and "Everything degrades" bullets.
    Covers the Git-Bash↔native string-namespace mismatch, the eager per-session scratch dir + literal
    path injected into the prompt (forward-slash drive-letter on Windows via `toNativeSafeTempForm`),
    the recipe (`mktemp -p "<scratchpad>"` quoted; never bare `/tmp`; never from `$TEMP`/`cygpath`),
    the Windows-only extra note gated on `shellNamespaceDiffersFromNative()`, and the deliberate
    rejection of path-rewriting "approach B". States explicitly that the **skill-author-facing**
    contract is t02's injected prompt guidance (`buildScratchpadSection`) and architecture.md is the
    contributor record.
  - `context-assembly.ts` bullet (§2 module map): added the `buildScratchpadSection` block to the
    enumeration of what `buildSystemPromptSuffix` appends.
  - `util/env.ts` bullet (§2 module map): added `toNativeSafeTempForm(p, platform?)`.
  - §3 flow: step 1 (extension load) now records eager scratch-dir creation + holding its literal
    path; step 3 (`before_agent_start`) now records the per-turn scratchpad section (+ Windows note).
- **CHANGELOG.** Added to `[Unreleased]` at the top: a `### Fixed — first-attempt cross-tool temp
  reads on Windows (2026-07-16)` (the #48 regression, behavior-focused) and a `### Added — per-session
  native-safe scratchpad surface (2026-07-16)`. Matched the `### <Category> — <title> (date)` shape;
  today = 2026-07-16. (Serial-conflict caution: if these collide on merge, regenerate/rebase the two
  entries into the current `[Unreleased]` head.)
- **Did not hand-edit** `doc/supported-features.md` — regenerated only.
- **Verification.** `npm run typecheck` clean. `test/registry.test.ts` 51/51. Full `npm test` →
  1250 passed / 16 skipped (matches the ~1250/16 baseline; no new failures). No deviations from spec.
