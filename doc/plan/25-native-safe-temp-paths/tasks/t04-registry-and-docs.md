# t04: Capability registry entry + capability matrix regen + contract docs

## Goal

The new session-scratchpad surface is recorded truthfully in the capability registry, the
generated matrix is regenerated and in sync, the architecture doc's now-stale passages are
brought current, and the cross-tool temp-path contract is documented for harness
contributors — while acknowledging that the primary skill-author-facing delivery of the
contract is t02's injected system-prompt guidance, not this contributor doc.

## Context & seams

- **Registry entry.** In `src/registry/capability-registry.ts` add one entry —
  `feature.sessionScratchpad` (a `feature.*` entry is the right precedent: no
  harness-exported/prompt-injected surface like `CLAUDE_PROJECT_DIR` overloads `tool.Bash`
  today). Tier **`partial`**, with an HONEST justification of the *actual* fidelity gaps
  (not a scoping aside):
  - PiCC mirrors Claude Code's injected-literal-path scratchpad contract (system-prompt
    prose + "use instead of /tmp"), including all-platform injection.
  - Genuine `partial` gaps to name: (a) the scratch **path shape differs** from Claude's
    (`os.tmpdir()/picc-scratch-*` vs Claude's `/tmp/claude/<session>/scratchpad`) —
    semantically equivalent, not identical; (b) on Windows PiCC adds a namespace note that
    Claude does not emit (a PiCC mitigation, additive). Do NOT justify `partial` by "doesn't
    export an env var" — Claude has no such export either, so that is parity, not a gap.
    (t01 does honor `CLAUDE_CODE_TMPDIR`, Claude's relocation knob, so that is not a gap.)
  Match the exact `cap(...)` shape/required fields of neighboring entries.
- **Registry `(§…)` note cross-ref — decide deliberately.** Every existing note ends with a
  pointer into `doc/plan/picc-plan.md` (e.g. `(§4.8)`). This feature has no `picc-plan.md`
  home, and the note is copied verbatim into the generated `supported-features.md`, so a
  dangling `(§X)` becomes a visible orphaned ref in a shipped doc. Either omit the section
  pointer or cite this feature folder / a stable architecture.md §4 anchor — do not emit a
  ref that resolves nowhere.
- **Regen.** Run `npm run gen:capabilities` (→ `node scripts/gen-capability-matrix.mjs`) and
  commit the regenerated `doc/supported-features.md` (do not hand-edit it; the Summary counts
  and headers update automatically). `test/registry.test.ts` asserts registry↔matrix sync —
  a missing regen fails it. Verify idempotence (running it twice yields no diff).
- **Architecture doc — home + stale-passage fixes.** The canonical home is
  **`doc/architecture.md` §4 "Mechanical-fidelity decisions (load-bearing)"**, immediately
  alongside the existing "Git Bash is pinned on Windows" (`:341-345`) and "UTF-8 subprocess
  env" (`:347-350`) bullets. Add a contract bullet there. `doc/supported-features.md` is
  generated and only carries the registry one-liner — it is NOT the prose home. Also update
  the THREE passages that go stale when t01/t02 land (all in the same task):
  - `architecture.md:192-194` — the `util/env.ts` bullet (add `toNativeSafeTempForm`).
  - `architecture.md:99-104` — the `context-assembly.ts` enumeration of what
    `buildSystemPromptSuffix` appends (add the scratchpad section).
  - `architecture.md:199-213` — §3 flow: activation-time scratch-dir creation and the
    per-turn injected scratchpad section.
  Content of the new §4 bullet: the Windows Git-Bash↔native string-namespace mismatch, the
  per-session scratchpad (literal path injected into the prompt, forward-slash drive-letter
  on Windows), and the recipe (`mktemp -p "<scratchpad>"`, quoted; never bare `/tmp`; never
  compute from `$TEMP`). Note explicitly that the **skill-author-facing** contract is the
  injected prompt guidance (t02) — architecture.md is the contributor record; the #48
  "documented so future skills don't reproduce it" AC is met primarily by t02, this doc
  secondarily.
- **CHANGELOG.** Add to `[Unreleased]` (`CHANGELOG.md:7`), behavior-focused (not registry
  bookkeeping), matching the `### <Category> — <title> (date)` shape: a **`### Fixed`** for
  the Windows first-attempt-Read regression (#48), plus a **`### Added`** for the new
  per-session scratchpad surface. Carry the serial-conflict caution (regenerate/rebase the
  entry if it collides on merge).

## Writable surface

- `src/registry/capability-registry.ts`
- `doc/supported-features.md` (regenerated by `npm run gen:capabilities` — never hand-edit)
- `doc/architecture.md` (§4 new bullet + the three stale passages: `:99-104`, `:192-194`,
  `:199-213`)
- `CHANGELOG.md`
- `test/registry.test.ts` only if a new entry legitimately requires a fixture/count update
- No other production code.

## Approach constraints

- Tier is **`partial`** with the honest (a)/(b) gaps above — do not overclaim `full`, do not
  justify `partial` by the (non-)env-var.
- No env-var recipe anywhere in docs; teach the literal-path / "your scratchpad directory"
  portable form (works under real Claude Code too).
- Never hand-edit generated matrix files; always via `npm run gen:capabilities`.

## Left open

- Exact `(§)` cross-ref target (omit vs. cite feature vs. architecture anchor) — pick one
  that resolves; note the choice in the log.

## Testing

- `test/registry.test.ts` green (registry ↔ generated artifacts in sync).
- Verify `npm run gen:capabilities` is idempotent before committing.

## Acceptance criteria
- [ ] A `partial`-tier `feature.sessionScratchpad` entry with the honest (a)/(b) gap
      justification and a non-orphaned `(§)`/cross-ref.
- [ ] `npm run gen:capabilities` run; generated artifacts committed and in sync
      (`test/registry.test.ts` green).
- [ ] `doc/architecture.md` §4 documents the contract; the three stale passages
      (`:99-104`, `:192-194`, `:199-213`) are updated.
- [ ] CHANGELOG `### Fixed` (+ `### Added`) behavior entry added.
- [ ] typecheck and full suite green.

## Depends on
t02
