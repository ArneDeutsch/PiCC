# F02 observations

- 2026-07-12: Project agents defined mid-session are not dispatchable until the Claude Code session reloads them (`/reload-plugins` fixed it) — worth a note in the skill or docs for future sessions.
- 2026-07-12: Baseline `npm test` is flaky in a fresh worktree: 2 of 6 runs failed with a file-level error (0 failing tests), unreproducible afterwards; suspected port/CPU collision with the parallel 01-mcp-core session running its own suites. Gate: no *new* failures, retry once on file-level flake. Deserves a root-cause look someday (fixed mock-server ports?).
- 2026-07-12: Phase 1 scouting reports (claude-parity, coder) were strong enough to skip a separate Phase 4 fan-out — two deep investigations up front beat four shallow ones later. Process note for review.md.
- 2026-07-12: Plan review round 1 (6 reviewers) — all NEEDS-WORK, all plan-fixable. Standouts: agent ID was placed in `details`, which Pi never shows the model (whole SendMessage promise unreachable — UX walkthrough caught it); resume-as-specified would have bypassed the entire permission stack (security); the headline regression was tested only against fakes of the exact behavior fakes got wrong last time (tester). Process lesson: the six-lens fan-out earns its cost — five of six found at least one MUST-FIX, and they barely overlapped.
- 2026-07-12: Pre-existing bug found during review (not this feature's scope): comment at `src/index.ts:579-580` claims subagents can only poll/stop their own tasks, but the shared registry lets them TaskOutput/TaskStop ANY session task, incl. siblings. Claude Code hides TaskOutput from subagents. Candidate follow-up.
- 2026-07-12: mock-openai.ts cannot simulate error responses at all — a test-infrastructure gap that made the empty-success bug invisible to e2e. t01 fixes it; noted here because other features will want error-path e2e too.
- 2026-07-12 (t01): Bug found, out of t01 scope — `src/index.ts` context:fork consumers (~:658, ~:922) throw on any `!ok`, losing partial output for failed forks; only the Agent tool got the 2.1.200 partial+note mapping. Route to a later task or follow-up.
- 2026-07-12 (t01): Spec error — the pinned throw wording and "error = raw capped errorMessage" conflicted on the shared error channel; implementer resolved it well (wording set at classification time), but specs should allocate exact strings to one owner.
- 2026-07-12 (t01): Repo facts — `npm run typecheck` excludes `test/`, so type-level contract pins need the compile-in-test mechanism (TS 7.0.2-native has no `createProgram` JS API); a test-tsconfig typecheck gate would simplify future pins. mock-openai sticky-error turns can starve later script turns if used without `when` predicates.
- 2026-07-12 (t01): Review round: security PASS / coder PASS / tester NEEDS-WORK — tester's two MUST-FIX test gaps (catch-all path, worktree-entry abort) were real; 11 findings fixed in one pass; abort-race consistency decided as "signal wins on every settle path". Suite 773 → 818.
- 2026-07-12 (t02): **The implementer stall mystery solved by the tester review**: fake-sdk.ts's top-level Pi import + builtin-agents' vi.mock factory = circular mock deadlock — the full suite HUNG forever at that file. The implementer wasn't just impatient; its gate genuinely never completed, and each self-wake launched another doomed run (8 concurrent suites, ~20 shells, killed by coordinator). Lessons: (a) fix in flight; (b) dispatch prompts now mandate synchronous gates, forbid no-op polling, and require pasting the suite summary line as proof; (c) "acceptance checked without a completed run" is the exact failure the proof-line rule prevents.
- 2026-07-12 (t02): Process — the t02 implementer twice ended its turn "waiting" on a background test run instead of finishing synchronously; its report never arrived, but the execution log + working tree carried everything (the state-on-disk design paying off). Fix for the skill later: dispatch prompts should say "run your gates synchronously; never end your turn waiting on a background process." Also a live demo of the feature's value: the coordinator recovered the stalled agent via SendMessage-resume — exactly what t04 gives PiCC.
- 2026-07-12 (t01): **Baseline flake root-caused**: `builtin-agents.test.ts` afterAll `rmSync` of a just-vacated cwd → transient Windows EPERM under full-suite load (the file documented the phenomenon for its inner dir but not its own teardown). Fixed with `maxRetries/retryDelay`. This probably closes the pre-feature "unknown file-level flake" — watch whether it recurs.
- 2026-07-12 (t02): **Parity MUST-FIX caught only by the claude-parity reviewer (round 2)**: the round-1 fix list had an item ("SubagentStop payload carries the subagent's own transcript_path") that was itself a parity BUG — Claude Code keeps `transcript_path` = the MAIN transcript inside a subagent (verified vs the hooks reference); only `agent_id`/`agent_type` are subagent-only additions. Three reviewers (parity/coder/tester) independently flagged the same code-vs-log contradiction, but only parity knew the *correct* direction. Lesson: a review finding that says "the code contradicts the log" is ambiguous until someone checks which one is right against the source of truth — here, reverting toward Claude Code, not reconciling the log to the code. Reversed in round 2; t02 spec seam + acceptance criterion amended.
- 2026-07-12 (t02): **The item-6 miss shows a plan-review gap**: nobody in the round-1 fan-out verified the transcript_path-to-hooks idea against Claude Code before it was accepted as a fix — it read as an obvious "hooks should see their own transcript" improvement. Process note: any change to hook *payload semantics* is a parity surface and must go through claude-parity, even when it looks like a local quality fix.
- 2026-07-12 (t02): Deferred to later tasks (recorded so they aren't lost): (t04) resolver "newest-match wins" + a plantable `.subagents` dir means resume-by-ID must not treat resolved on-disk transcripts as more trusted than any session restore (security); (t04) whether a *failed* (terminal-API-error) subagent is actually SendMessage-resumable in Claude Code is unverified — t02 already ships the "resumable" trailer on failed-with-partial runs, so t04 must confirm or drop it; (t07) verify Explore/Plan really are non-resumable in Claude Code, that plugin subagents' `agent_type` should be the plugin-scoped id (PiCC emits bare `agent.name` — check `src/claude/agents.ts`), and document the `agent-<12hex>` id format (Claude uses a UUID) and the `<base>.subagents/<stamp>_<id>.jsonl` location scheme (differs from Claude's per-session `subagents/` dir but preserves discoverable-from-session).
- 2026-07-13 (t03): **DEFERRED FOLLOW-UP (touches t01 seam) — abort/failure not visually distinct in the foreground.** Aborted and failed-with-no-output dispatches THROW (t01's loud-failure contract), so Pi renders them in its own red error box and `renderResult`'s outcome badge never fires — an Esc/TaskStop abort looks identical to an API crash (only the message wording differs), and the `■ aborted` badge is unreachable from the foreground path. Feature acceptance ("reports as aborted, not failed") is met at the TEXT/model level and for background TaskOutput status, but not the foreground visual badge. User decided (2026-07-13) to DEFER: ship t03, keep the badge branch as forward-compatible exhaustive enum handling (unit-tested directly), and open a follow-up to rework the t01 throw seam so aborted/failed-no-output return an error-shaped result carrying `outcome` (preserving "model sees an error"). **Candidate follow-up task.**
- 2026-07-13 (t03): Review fan-out (security/coder/tester/user-experience) — coder + tester PASS, security + UX NEEDS-WORK, all fixable in t03 except the above. Real catches: two display paths bypassed the tail sanitizer (activity-line tool name; renderCall/final renderResult raw text) — Pi does NOT sanitize component render output at the frame writer (verified), so the tail condenser's sanitizer is the ONLY guard and every display path must route through it; the final render double-printed "resumable via SendMessage" and leaked the raw `---` model trailer to the human; a turn-capped SUCCESS rendered as clean `● completed` (the `(truncated)` badge branch was unreachable). Process note: the primary mandated defense (sanitizing replayed subagent tool output) was solid — the misses were the SECONDARY display strings, exactly where a "the hard part is done" implementer stops looking.
- 2026-07-13 (t03): Emission-rate vs memory bound — the progress condenser bounds MEMORY (single activity line, capped tail) but emits one `onUpdate` per streaming assistant token (dedupe only catches identical snapshots, which streaming never produces). Fine in interactive mode (Pi coalesces renders) but chatty in print/RPC. Throttling was a Left-open decision; not fixed beyond clearing the activity line on turn_end (no timers — condenser stays deterministic for tests). Watch if print/stream-json consumers find it noisy.
- 2026-07-12 (t02): **Suite-hang deadlock fixed by the coordinator to unblock, then the rest delegated** — `fake-sdk.ts` no longer statically imports the Pi module (which `builtin-agents`' vi.mock factory mocks while awaiting the helper → deadlock); the real `SessionManager` is now injected via `useRealSessionManager()`. Confirmed the Pi package is NOT CJS-requireable (`ERR_PACKAGE_PATH_NOT_EXPORTED`), so the reviewer's `createRequire` fallback would have failed — injection was the only viable fix.
