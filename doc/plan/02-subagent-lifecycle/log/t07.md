# t07 execution log — registry truthfulness, docs, CHANGELOG

Read first, in order: feature.md, t07 spec, observations.md (full), log/t01–t06,
and the shipped code (subagents.ts, subagent-registry.ts, background-tasks.ts,
subagent-progress.ts, util/subagent-transcripts.ts, index.ts, claude/agents.ts,
registry/capability-registry.ts). Every claim below was cross-checked against the
SHIPPED behavior of t01–t06, not the plan text.

## CRITICAL CORRECTION — verified against code
The t07 spec line 26 ("payloads now carry agent_id + the subagent's own transcript
path") is STALE. Verified in `src/runtime/subagents.ts`:
- `injectIdentity` (`:688`) adds `agent_id` + `agent_type: agent.name` to EVERY
  in-dispatch hook payload; it does NOT inject `transcript_path`.
- `fireSubagentStop` (`:707`) fires the payload with NO `transcript_path`, so the
  HookRunner's own constructed default (= the MAIN session transcript) stands.
- The code comments (`:685-687`, `:710-714`) explicitly document this as the t02
  review-round-2 Claude-parity correction.
Registry SubagentStart/SubagentStop entries state EXACTLY this: "payload carries
agent_id + agent_type … while transcript_path stays the MAIN session transcript
(Claude Code parity)." NOT "the subagent's own transcript path."

## Registry entries added/changed (tier + one-line justification)

- **tool.Agent: full → PARTIAL.** Dispatch/loud-failure/agent-id/progress/verbatim
  are all fully implemented, but the default dispatch DIRECTION diverges from the
  pinned baseline: PiCC defaults FOREGROUND while Claude 2.1.198 runs subagents
  background-by-default, so an implicit-concurrency fan-out runs serially. Undersell
  tiebreaker + a matrix reader must not infer concurrent-by-default parity. Note
  names the failure semantics (2.1.199/2.1.200), agent ids, progress, and the gap.
- **tool.Task: full → PARTIAL.** Alias of Agent — inherits the same semantics and
  the same default-foreground divergence; kept consistent with tool.Agent.
- **tool.SendMessage: ADDED, partial.** Its absence was untruthful by omission.
  Gaps named: no cross-restart resume (registry is process-lifetime), steering
  reaches only BACKGROUND dispatches, idle-parent delivery next-turn-only,
  fork/agentOverride non-resumable; subagent↔subagent / agent-teams out of scope.
- **agent.frontmatter.background: ADDED, full.** Honored since t05 (parsed in
  `claude/agents.ts`, routed via `isBackgroundAgent`). The FIELD itself is fully
  honored (a `background:true` agent runs background); the broader background
  limitations live on feature.background-agents, so `full` on the field is correct
  (same shape as agent.frontmatter.isolation → full while worktrees carry caveats).
- **feature.background-agents: partial (note rewritten).** Was "no push, stop
  cooperative." Now records settlement PUSH (t05) + loud failed-status (t01) and
  names the gaps: default-foreground vs Claude 2.1.198, no idle-parent wake,
  bounded/excerpted notices, no remote/cloud, cooperative stop.
- **tool.TaskOutput: full (note updated).** Added failed-status reporting (never an
  empty success) + agent-id trailer + usage line. Stays full.
- **tool.TaskStop: partial (note updated).** Labels the "stopped → result discarded"
  contract as PiCC-defined (Claude leaves TaskStop's post-stop semantics
  undocumented). Stays partial.
- **hook.event.SubagentStart / SubagentStop: full (notes updated).** agent_id +
  agent_type; transcript_path = MAIN (see CRITICAL CORRECTION). Tier unchanged.
- **hook.event.Notification: degraded-noop (note updated).** Records that background
  settlement does NOT fire an `agent_completed` Notification (t05 left it unwired) —
  the coordinator learns via the pushed settlement message + the SubagentStop hook.
  "Parsed, never fired" survives. Verified against t05 log + observations.
- **setting.cleanupPeriodDays: full → PARTIAL.** t02 shipped NO subagent-transcript
  reaper; only orphaned worktrees are reaped. Note names the `<base>.subagents/` gap.

Intra-2.1.x version sensitivity woven into notes where an entry depends on it:
2.1.198 (background-by-default; course-correction) on Agent/Task/background/frontmatter,
2.1.199/2.1.200 (failed-status + partial-note) on Agent, 2.1.205 (running-flip steer)
on SendMessage.

Registry does NOT track control commands (no `command.*` kind exists — grep-confirmed),
so `/usage` gets no registry entry; it is documented in the user guide instead, framed
as a PiCC-additive subagent-scoped surface, explicitly NOT Claude `/usage`/`/cost`
parity (per t06 observations).

## Matrix
`npm run gen:capabilities` regenerated `doc/supported-features.md` (148 capabilities;
+2 net: SendMessage, agent.frontmatter.background). NEVER hand-edited. Re-running gen
produced no further diff (determinism/no-drift confirmed). Command exit 0.

## Docs
- `doc/architecture.md`: added the **Subagent error contract** subsection in §4
  (completed/failed/aborted classification, loud named failure, partial-output
  preservation, foreground-throw vs background-status, retry unchanged); expanded the
  `subagents.ts` module bullet and added module bullets for `subagent-registry.ts`,
  `subagent-progress.ts`, `util/subagent-transcripts.ts`; updated the
  `background-tasks.ts` bullet (failed-status, settlement push); updated data-flow
  step 6 (error contract, background:true, settlement push, SendMessage).
- `doc/user-guide.md`: overview bullet + §4 dispatch bullet updated; **Observing
  subagents** subsection added (transcript location `<mainSessionFileBase>.subagents/
  <stamp>_<agentId>.jsonl`, live progress + Esc, `/usage` subagent-scoped breakdown,
  SendMessage resume/steer with the four honest limits); `/usage` row added to the
  commands table; §7 summary split into Full / Partial (background dispatch +
  SendMessage moved to Partial with the default-foreground gap named).
- `README.md`: Subagents feature bullet rewritten (loud failures, transcripts, live
  progress, /usage, settlement push, SendMessage); control-surface line adds /usage.
- `CHANGELOG.md` [Unreleased]: HEADLINE = the empty-success bug fix under "Fixed",
  referencing the 2026-07-12 dogfooding incident; then "Added" (transcripts, agent
  ids + progress, SendMessage, settlement push + background frontmatter, usage +
  /usage) and "Changed — registry truthfulness."

## Stale claims found + fixed
- `src/index.ts:626` comment "so a subagent can poll/stop the tasks it started
  itself" — FALSE (the shared registry lets a granted subagent reach ANY session
  task, incl. siblings'/parent's; Claude hides TaskOutput from subagents). Made the
  comment TRUTHFUL and flagged the behavior as a candidate follow-up (scoping task
  visibility per dispatcher). COMMENT-ONLY change, no behavior change. index.ts is
  outside the spec's writable surface, but the coordinator's dispatch explicitly
  carved out this comment fix (instruction 4).
- Living docs sweep (README/architecture/user-guide/supported-features) for "no way
  to observe", "one-shot dispatch", "silently dropped", "empty success": no STALE
  occurrences remain — every "empty success" hit is now a truthful description of the
  fix. Historical dated review docs (`doc/review/2026-07-12-*.md`) were deliberately
  NOT rewritten — they are point-in-time records (e.g. the audit's "no SendMessage/
  resume (one-shot dispatch documented)" was accurate on 2026-07-12); falsifying a
  dated record would be worse than leaving it. Noted here instead.

## Tests
- `test/registry.test.ts`: removed Agent/Task from the "core tool surface … full"
  loop and cleanupPeriodDays from the "honored … stay full" list; added 8 focused
  tests — Agent/Task partial + failure/default-direction note content, SendMessage
  partial + gap wording, TaskOutput full / TaskStop partial + PiCC-defined wording,
  SubagentStart/Stop agent_id+agent_type + MAIN transcript, Notification
  agent_completed-not-fired, agent.frontmatter.background full, background-agents
  settlement-push/idle/2.1.198 gaps, cleanupPeriodDays partial + `.subagents/` gap.
- Doc-freshness test: deliberately NOT added. The gen script shells out to a
  `node --import tsx` child; a regenerate-and-diff test would add child-process +
  CRLF-normalization flakiness on Windows to a suite whose baseline is green-but-
  flaky. The spec said "consider," and the coordinator regenerates+commits the file;
  determinism is already proven above. Candidate for a later, isolation-safe version.

## Could NOT make truthfully / surprising
- `/usage` is registered via `pi.registerCommand` (works in the interactive TUI) but
  is MISSING from the `input`-handler control-command interceptor regex
  (`src/index.ts:970` lists doctor|compat|quota|skills|agents, not usage), so typing
  `/usage` in PRINT/non-interactive mode falls through to the model rather than being
  intercepted. This is a t06 residue, outside my writable surface. Docs describe
  /usage as a control command (true in interactive mode, the documented default
  flow); the print-mode-interception gap is flagged here as a candidate follow-up. I
  did NOT change the regex (behavior change, out of scope).
- Residual parity item from t02 observations NOT resolved by me: plugin subagents'
  `agent_type` in hook payloads is the bare `agent.name` (injectIdentity uses
  `agent.name`), which may need to be the plugin-scoped id for exact Claude parity.
  My SubagentStart/Stop registry notes say "agent_type" generically and do NOT claim
  plugin-scoping, so they stay truthful; the underlying payload nuance is a candidate
  follow-up (touches subagents.ts/agents.ts, outside t07's registry-truthfulness
  core).
- tool.Agent/tool.Task downgrade full→partial was the highest-stakes tier call.
  Resolved toward partial on the mandated "when in doubt, undersell" rule: a reader
  seeing Agent=full would wrongly infer background-by-default concurrency parity — the
  exact untruthful-by-tier class this feature exists to fix.

## Review-polish pass (t07 FIX 1–5)
- **FIX 1 (architecture.md):** added a sentence to the §4 Subagent-error-contract
  "Foreground vs background" bullet naming the default-direction divergence — PiCC
  defaults FOREGROUND vs Claude 2.1.198 background-by-default (implicit fan-out runs
  serially unless run_in_background/background:true), the single most consequential
  subagent parity gap; cross-links feature.background-agents.
- **FIX 2 (registry tool.TaskOutput note):** appended the shared-registry
  cross-visibility clause — the session-wide background-task registry lets a
  TaskOutput-granted subagent reach ANY session task, whereas Claude hides TaskOutput
  from subagents (a project-intended restriction PiCC does not enforce). Tier stays
  `full` (the tool itself works; the gap is named in the note). Extended
  test/registry.test.ts to assert the "session-wide" + "hides TaskOutput from
  subagents" substrings.
- **FIX 3 (/usage print-mode gap):** added `usage` to the input-handler
  control-command interceptor alternation (src/index.ts ~:974) so `/usage` is
  intercepted in print/non-interactive mode alongside doctor|compat|quota|skills|agents
  (previously it fell through to the model); fixed the two stale command-list comments
  (~:970 and ~:1175) to include `usage`. Extended test/control-commands.test.ts: a new
  `/usage`-via-input-event interception test + added `usage` to the registered-command
  loop.
- **FIX 4 (registry tool.SendMessage note):** moved the "(2.1.205)" version tag off
  the version-agnostic steer clause onto the resume STATUS-FLIP clause (a resumed
  failed/completed agent flips back to running is the 2.1.205 semantic). Note-only.
- **FIX 5 (NITs):** CHANGELOG.md agent-id precision `agent-<hex>` → `agent-<12 hex>`
  (code mints exactly 12 hex, matching the validator error string); user-guide
  foreground-abort caveat — Esc on a foreground dispatch renders in an error frame
  worded as aborted, the distinct abort badge being a background/next-turn surface.
- **Matrix:** re-ran `npm run gen:capabilities` after the registry edits; a SECOND
  regeneration produced an identical file (hash-verified idempotent, no drift). Never
  hand-edited.

## Gates
- `npm run typecheck`: exit 0.
- Full `npm test`: **Test Files 39 passed | 1 skipped (40); Tests 948 passed | 16
  skipped (964)** — baseline 940 passed | 16 skipped (956); +8 registry tests, no new
  failures, no flake, no retry needed.
- Polish pass (FIX 1–5) re-run: `npm run typecheck` exit 0; full `npm test` →
  **Test Files 39 passed | 1 skipped (40); Tests 949 passed | 16 skipped (965)**
  (+1 vs the prior 948: the new `/usage` input-event interception test). No new
  failures, no flake.

## Feature-close fixes (7-reviewer close fan-out)

Applied the close-review fix set on top of the committed t01–t07 (HEAD 67771d7).

- **FIX 1 (MUST — settlement notices consumed before delivery):** re-architected the
  drain to PEEK-then-COMMIT. `SubagentRegistry.isSettledNoticeArmed()` (new, pure) lets
  `drainSettlementNotices(isArmed, commit, hasRegistryRecord?)` SELECT notices without
  flipping any dedup gate; it now returns `SettlementNotice[]` (`{ content, commit }`).
  `deliverSettlementNotices` (index.ts) delivers each notice in its OWN try/catch and
  calls `commit()` — `consumeSettledNotice` (registry path) or `settlementNoticeDelivered=true`
  (drain-fallback path) — ONLY after `pi.sendMessage` returns. A throw leaves the notice
  un-committed → it re-fires next drain; a throw on one never blocks the others.
  Resume-newest-wins is preserved by an in-drain `selected` Set (newest reverse-order
  record claims the agent id; older ones skip), replacing the implicit single-consume
  gate. Exactly-once preserved (commit flips the gate; idempotent). Tests: new unit
  "a delivery throw on one notice leaves it un-committed → re-fires next drain" and new
  integration "a pi.sendMessage throw on one notice still delivers the others…";
  existing exactly-once-across-turns, resume re-arm (sendmessage.test.ts), and
  newest-wins (`.reverse()`) tests stay green.
- **FIX 2 (SHOULD — background→foreground fallback had no diagnostic):** the
  foreground-fallback `note` now keys on the EFFECTIVE `wantsBackground` (param OR
  `background:true` frontmatter) instead of `params.run_in_background`, so a frontmatter-
  background agent forced foreground also surfaces the degrade. New test in
  background-tasks.test.ts. (Wording keeps the "run_in_background" substring so the
  non-writable runtime-core note assertion stays green.)
- **FIX 3 (SHOULD — TaskOutput vocab "stopped"→"aborted"):** the stopped TaskOutput text
  now leads with "was aborted" to match the settlement-notice/`/usage`/user-guide
  vocabulary; retains a "stopped before completing" clause (which keeps the non-writable
  subagent-outcomes assertion green). Adjusted the three background-tasks.test.ts
  assertions to "was aborted".
- **FIX 4 (SHOULD — sanitize model/project-controlled identifiers to the terminal):**
  `renderUsageReport` now passes `record.agentName` through `sanitizeLine`; the TaskOutput
  result text passes `task.label` through `sanitizeLine`. New tests: `/usage` (integration)
  and TaskOutput (unit) render a control-byte name/label sanitized. (`/agents` catalog sink
  left untouched — out of scope.)
- **FIX 5 (SHOULD — soften SubagentStart/Stop plugin agent_type parity claim):** both
  hook-event notes now state agent_type is the agent's bare frontmatter name and that
  PiCC does NOT apply plugin-scoped naming (Claude's exact plugin-scoped id unverified);
  tier stays `full`. Registry test asserts the "plugin" nuance clause. Matrix regenerated.
- **FIX 6 (SHOULD — README foreground-default divergence):** added a half-clause to the
  Subagents bullet noting dispatch defaults FOREGROUND while Claude 2.1.198 is
  background-by-default (fan-outs run serially unless background is requested).
- **FIX 7 (SHOULD — guard the triplicated usage interface):** added a compile-time
  `_SameShape` drift guard in subagents.ts (type-only `import type` of `UsageLike` /
  `SubagentUsage`) — mutual `keyof` containment + mutual assignability, so adding/removing/
  retyping a field in any of the three shapes fails `tsc`. Verified non-vacuous (a scratch
  drift errors TS2322; a match is clean).
- **FIX 8 (SHOULD — assert usage on the RESUME path):** the sendmessage resume
  offline-integration test now scripts `stats` and, after the resumed run settles, asserts
  the background record usage AND `registry.get(agentId).usage` are populated — proving the
  dispatch→persist→settle→RESUME→usage chain.
- **FIX 9 (SHOULD — matrix-freshness guard):** refactored gen-capability-matrix.mjs to
  EXPORT a pure `renderCapabilityMatrix(entries, baseline)` (CLI now gated behind a
  main-module check; importing the module neither spawns nor writes). New registry.test.ts
  guard regenerates IN-PROCESS from `CAPABILITY_REGISTRY`/`CLAUDE_BASELINE` and diffs the
  committed doc (both CRLF-normalized) — un-fakeable, no child process.
- **FIX 10 (SHOULD — clarify resolveSubagentTranscript):** added a doc comment: it is the
  exported ID→transcript discovery API for external tooling/humans, exercised by
  e2e/unit tests; production RESUME deliberately uses the registry-stored path (SEC#2),
  so it has no runtime caller by design — do not dead-code delete.
- **NIT (coder-3, stuck running record):** wrapped the agent hook-config parsing (between
  the minimal `register()` and the main try/finally) in a try/catch that `markSettled`s the
  record and fails the dispatch loudly, so a parse throw can never strand a "running"
  registry record. Placement keeps the ack-before-register window intact (still before the
  semaphore await).
- **NIT (coder-2, followUp):** added "declared for the pi-contract pin; runtime uses
  steer()/reopen" comments on both `PiSession.followUp?` and `SteerableSession.followUp?`.
- **NIT (docs, full-surface row):** softened the README repo-layout row to "Larger fixture
  exercising a broad slice of the feature surface".
- **NIT (adversarial-3, suppress agent-id in settlement notice for non-resumable):**
  SKIPPED as risky — the background task record carries no `oneShot` flag, so the only
  available discriminator is `resumable`, and suppressing the id for non-resumable records
  would (a) drop a correlation id the coordinator saw at dispatch for FAILED/early-guard
  runs and (b) break ~4 existing settlement-notice assertions (and the drain-fallback test)
  that expect the id to appear. Judged a contract change, not a cheap nit; left the id in.

**Matrix:** re-ran `npm run gen:capabilities` after the FIX 5 registry edit; a SECOND
regeneration produced an identical file (idempotent, no drift). Never hand-edited.

## Gates (feature-close fixes)
- `npm run typecheck`: exit 0.
- Full `npm test`: **Test Files 39 passed | 1 skipped (40); Tests 955 passed | 16
  skipped (971)** — +6 vs the prior 949 (the six new close-fix tests). No new failures,
  no flake, no retry needed. Tolerated pre-existing flake: builtin-agents teardown EPERM
  on Windows (did not occur this run).
