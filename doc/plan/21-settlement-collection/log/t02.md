- Wired terminal `TaskOutput` returns to call the owner-safe `markCollected(taskId)` only after the complete response object is built; running polls and all earlier throws leave delivery pending. A failed terminal transition fails closed rather than returning an uncollected terminal record.
- Treated completed (including empty/cut-off), failed (with or without partial output), and stopped records identically for collection. Repeated and concurrent collectors are idempotent; post-notice retrieval leaves `notified` unchanged.
- Added the production sender's synchronous final `isValid()` check immediately before `pi.sendMessage` and commit. Chose a test-only synchronous `beforeSettlementSend` seam to deterministically collect after selection without adding an await or timer.
- Changed only stopped settlement guidance: it explicitly says no result was retained and TaskOutput can report the outcome but cannot retrieve discarded output.
- Extended the existing real-SessionManager persisted-resume fixture. Real `SendMessage` creates three tested generations: uncollected delivery, collected suppression, and newest-generation delivery despite late original collection.
- Headline offline integration traverses the registered coordinator `Agent` and `TaskOutput` tools with an injected fake SDK, then fires the real `before_agent_start` handler twice.
- No deviations from the task spec. No timers or network access added.
- Verification: focused `npx vitest run test/background-tasks.test.ts test/integration-extension.test.ts test/sendmessage.test.ts` passed (3 files, 175 tests); `npm run typecheck` passed; full `npm test` passed (53 files, 1235 passed, 1 skipped).
- FIX: Changed the headline integration to call suite `wire()` without an SDK option, then inject the fake SDK through `onWired` internals via `subagentRuntime.setSdkForTest()` before registered Agent dispatch. Removed the suite-local SDK option; registered Agent, TaskOutput, and the real next-turn hook remain the traversed path.
- FIX: Split empty completion into a non-vacuous case asserting exact empty text and `details.status === "completed"`.
- FIX: Refined stopped-only guidance to distinguish the retained stopped outcome from discarded final output. Transcript availability is now conditional on `transcriptPath`, with tests for both path-present and path-absent notices and assertions against impossible retrieval wording.
- FIX: Updated stale integration-suite and settlement-delivery comments to describe mixed real/seeded coverage, eligible uncollected current generations, task-local delivery state plus agent readiness, and the pre-send validity check.
- FIX verification: focused tests passed (3 files, 176 tests); `npm run typecheck` passed; full `npm test` passed (53 files, 1236 passed, 1 skipped). No deviations or friction.
