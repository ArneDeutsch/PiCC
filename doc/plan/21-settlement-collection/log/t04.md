- Chose a concise cut-off header suffix: `subagent run cut off at its output limit`. Guidance says TaskOutput/the transcript expose retained output, but a continuation was never produced and requires `SendMessage` resume when available or re-dispatch.
- Kept run cut-off and notice bounding independent: a second footer says `Notice excerpt truncated` only when the bounded notice omits retained output. Existing untrusted framing, sanitization, caps, and lifecycle state are unchanged.
- Pinned both cut-off cases (retained output fits vs. notice excerpt also truncated) and the TaskOutput description's terminal-collection/running-poll contract in focused tests.
- Reserved “collection” for successful terminal TaskOutput returns in README/CHANGELOG; TaskStop now says it stops a run. Removed the blanket `Task*` claim from the user guide's Full tier, named TaskOutput/TaskStop partial, corrected the identity-only lifecycle paragraph, and changed the resume comment to describe the agent readiness gate plus task-local/newest checks.
- Deliberately did not add a real-Pi live e2e, as specified; existing lifecycle behavior was not changed.
- No deviations or implementation friction. Existing coordinator-owned edits to `feature.md` and the untracked t04 task spec were left untouched.
- Verification: focused `npx vitest run test/background-tasks.test.ts` passed (1 file, 113 tests); `npm run typecheck` passed; full `npm test` passed (53 files, 1239 passed, 1 skipped); final `git diff --check` passed with only the existing LF→CRLF warning for coordinator-owned `feature.md`.
- FIX: Gated run-cut-off header and recovery guidance to result-bearing outcomes. A `stopped` record with stale `truncated: true` now remains the user-visible aborted, outcome-only notice and receives no output-limit claim.
- FIX: Added the stopped+truncated regression; strengthened the short cut-off case to require retained output and the long case to require the `[…]` marker, a sub-2,000-character notice, and exclusion of the complete 4,000-character payload.
- FIX: Standardized the collection section on the user-visible `aborted outcome`, with `stopped` identified only as the internal task status.
- FIX verification: focused `npx vitest run test/background-tasks.test.ts` passed (1 file, 114 tests); `npm run typecheck` passed; full `npm test` passed (53 files, 1240 passed, 1 skipped).
- Final wording polish: the outcome-only notice now says `aborted outcome (internal task status: stopped)` so runtime wording matches the user-visible outcome vocabulary and the guide.
