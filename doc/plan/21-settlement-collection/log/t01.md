- Chose task-local `pending | collected | notified` delivery state, initialized on every registry-created task; removed the fallback-only delivered flag in favor of the same state for all paths.
- Added owner-safe `markCollected(taskId)` to registry views. It is idempotent for terminal tasks, leaves notified tasks notified, and rejects running polls/unknown or foreign tasks.
- Added `SettlementNotice.isValid()` as the final synchronous pre-send seam. It verifies pending state, newest-generation ownership, terminal status, and the external armed gate where applicable.
- Newest records claim a shared agent id before eligibility checks, so running, collected, and notified resumes cannot fall through to older records.
- Left the agent-level settled gate intact for readiness/resume; collected registry-miss and registered records use the task-local state as authoritative suppression.
- No deviations from the task scope. The existing production delivery loop is outside t01's writable surface; this task supplies the final validity seam for the later lifecycle/contract tasks to invoke immediately before send.
- Verification: focused `npx vitest run test/background-tasks.test.ts` passed (1 file, 93 tests); `npm run typecheck` passed; full `npm test` passed (53 files, 1217 passed, 1 skipped).
- FIX review: replaced delivery-time reverse scans with an authoritative O(1) `agentId -> newest task` index. Per-identity generation links restore the prior owner when a settled result changes ids; corrected identities are inserted by task start generation so settlement timing cannot change newest-wins semantics.
- FIX review: strengthened supersession, fallback retry, terminal outcome, and post-notice real TaskOutput tests. All final no-fallthrough assertions use always-armed callbacks so task-local state/index behavior—not a consumed agent gate—is what makes drains empty; a settled newer running generation is also proven independently eligible.
- Kept `settlementDelivery` optional for compatibility with legitimate structural test/diagnostic records such as the existing `baseTask()` fixture; registry-created records always initialize it and absence remains explicitly interpreted as pending.
- Updated the `BackgroundTaskView` shape comment from five to six members.
- FIX verification: focused `npx vitest run test/background-tasks.test.ts` passed (1 file, 97 tests); `npm run typecheck` passed; full `npm test` passed (53 files, 1221 passed, 1 skipped).
