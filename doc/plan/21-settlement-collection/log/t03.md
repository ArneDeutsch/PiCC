- **Superseded initial decision:** initially kept `tool.TaskOutput` at `full` and `feature.background-agents` at `partial`; review and user approval later corrected the current conclusion to `tool.TaskOutput: partial` for its pre-existing schema gap. The lifecycle documentation treats terminal TaskOutput return as delivery, running polls as notice-preserving, post-notice retrieval as non-rearming, and eligible/uncollected/current/newest-generation limits rather than claiming global single delivery.
- Chose to place the full Claude divergence evidence note in the user guide, architecture, and capability registry; the root README and full-surface command use concise directional wording. Reporter-observed Claude Code 2.1.x redundant post-retrieval notification behavior is explicitly not presented as normative parity because public notification-consumption semantics are unspecified.
- Audited all assigned settlement-related registry rows: `tool.Agent`, `tool.Task`, `tool.TaskOutput`, `tool.SendMessage`, `hook.event.Notification`, and `feature.background-agents`. Preserved next-turn timing, one-shot print-mode loss, newest-generation supersession, bounded/untrusted notices, no idle-parent wake-up, no `agent_completed` Notification hook, stopped outcome-only notices, and cut-off-as-all-available-output wording.
- Updated the shared Agent/Task model-facing description and schema guidance so a later notice is conditional; pinned that language and the registry tiers/evidence/limitations in tests.
- Updated README, user guide, architecture, both full-surface background-task artifacts, and the Unreleased Fixed changelog entry. Regenerated `doc/supported-features.md`; two consecutive final generations produced identical SHA-256 `b65f2af5bd7389a335b72bb2e3e0d5936df665ecaaa53136f2738dba12611ab7` (the generator reports a write on every run).
- Friction: focused tests initially exposed two wording mismatches in new/existing registry assertions; corrected the assertion capitalization and restored the established exact `parallel-agent cap` limitation phrase. Generated-doc status also emits the repository's LF→CRLF working-copy warning; `git diff --check` remains clean.
- No deviations from the task spec and no runtime behavior changes.
- Verification: focused `npx vitest run test/background-tasks.test.ts test/registry.test.ts` passed (2 files, 161 tests); `npm run typecheck` passed; full `npm test` passed (53 files, 1236 passed, 1 skipped).

## FIX pass (approved review findings)

- Applied the user-approved tier correction: `tool.TaskOutput` is now `partial`. This is a pre-existing schema-parity gap, separate from F21 lifecycle behavior: reporter evidence anthropics/claude-code#21343 shows Claude Code 2.1.20 using `block:true`, and anthropics/claude-code#76335 shows 2.1.206 `local_agent` using `block:true` plus `timeout`, while PiCC exposes `wait`. Official tools docs list TaskOutput and its deprecation but do not publish its parameter schema.
- Scoped timing evidence as reporter observation rather than contract: anthropics/claude-code#21343 (2.1.20 background agents) and anthropics/claude-code#24752 describe late notification during an active conversation, but neither public docs nor reports establish exact notification-consumption or mid-turn/next-turn semantics. PiCC's terminal-collection suppression remains explicitly described as UX hardening, not verified parity.
- Defined an eligible current task on first use as the latest generation for its agent that remains uncollected and unnotified. Pinned the one-bounded-next-interactive-turn rule, running-poll preservation, and terminal-collection suppression in both Agent/Task description surfaces and tests.
- Kept the collect-before-rely/finalize action in always-injected guidance, removed the false claim that every uncollected result is lost, and called out bounded next-turn notices plus one-shot print-mode termination. The user guide now gives immediate print-mode choices: await TaskOutput, use `run_in_background:false`, or force foreground.
- Qualified stale contributor comments and still-Unreleased changelog promises. Clarified that `SubagentStop` fires independently of the conditional notice, reflowed the architecture SendMessage sentence, and made the full-surface integration test explicitly pin fixture guidance rather than claim behavioral proof.
- No runtime lifecycle behavior was changed. The only friction in this FIX pass was two focused-test failures caused by an assertion variable typo and one remaining old `full` tier assertion; both test-only issues were corrected.
- Final capability generation was run twice: both outputs and diffs were identical, SHA-256 `e86ec6d9c9ac1b10fa6837a071634f4410e571b6d87ff7828d753e4cee09cc09`. The generator still reports a write each run and Git emits the existing LF→CRLF warning.
- FIX verification: focused `npx vitest run test/background-tasks.test.ts test/registry.test.ts test/runtime-core.test.ts test/integration-extension.test.ts` passed (4 files, 276 tests); `npm run typecheck` passed; full `npm test` passed (53 files, 1236 passed, 1 skipped).

## Final FIX pass

- Recast the older still-Unreleased 2026-07-13 registry entry as historical and explicitly superseded by F15: foreground-default was true then, while current PiCC is background-by-default.
- Replaced this task's bare evidence references with unambiguous `anthropics/claude-code#…` references throughout source documentation, generated documentation, tests, and this log; unrelated and PiCC issue references were left unchanged.
- Marked the opening `tool.TaskOutput: full` decision as superseded by the review/user-approved current `partial` conclusion.
- Strengthened registry assertions for one bounded settlement notice on the coordinator's `NEXT` turn and for the qualified, reporter-observed active-conversation timing evidence. Strengthened the `run_in_background` schema-description assertion so the one-bounded notice is conditional on the latest generation remaining uncollected and unnotified.
- No runtime lifecycle behavior changes and no deviations from the revised spec.
- Generated capabilities twice with byte-identical output; both SHA-256 values were `33ee2ed0696aa3b138d2403d850739621d9a7d4d9846102543a866d60daaf77d`.
- Final verification: focused `npx vitest run test/background-tasks.test.ts test/registry.test.ts test/runtime-core.test.ts test/integration-extension.test.ts` passed (4 files, 276 tests); `npm run typecheck` passed; full `npm test` passed (53 files, 1236 passed, 1 skipped); `git diff --check` passed (only existing LF→CRLF warnings were emitted for the revised task spec and generated matrix).
