# t03 execution log — Prose docs, CHANGELOG, full-surface example

## Edits made

- **README.md** (~L54): feature-overview clause now states nesting is off by default
  (main-session-only), an explicit opt-in via `subagents.maxDepth: 2..5`. One clause, not full
  semantics (per spec altitude).
- **doc/user-guide.md**:
  - §7 Full-tier line (~L386): "nested subagent dispatch (default depth cap 5)" → main-session-only /
    default 1 + opt-in via 2..5.
  - §7 Partial-tier per-depth prose (~L406): noted depth ≥ 2 fan-out only happens when `maxDepth` is
    raised to 2..5.
  - **New subsection** "Subagent dispatch controls (`.claude/settings.json`)" placed at the end of §4
    (just before §5), chosen over the §5 harness-config block because these are *project* settings,
    not the project-external `~/.picc/config.json` surface §5 documents. Covers: the three controls
    (table), PiCC-extension framing, depth semantics (main = depth 0, its subagents = depth 1,
    default maxDepth 1 blocks depth 2), scope/precedence (project overrides user), per-agent
    `tools:`/`disallowedTools:` narrowing, explicit contrast of the two "off" states
    (enabled:false/disableSubagents = no delegation vs maxDepth:1 = keeps depth-1, blocks nesting),
    and a migration snippet with the 2-vs-5 caveat.
- **doc/architecture.md**:
  - ~L111: "enforces the depth cap (default 5)" → default 1 = main-session-only, raise to 2..5.
  - Step-6 dispatch narrative (~L233): rewrote "Nested dispatch is depth-capped; the same guard runs
    inside every subagent session" so it no longer implies subagents routinely nest — nesting is off
    by default; provisioning + guard only engage when maxDepth is raised.
  - Nested-background-bound block (~L317): prepended that recursion is off by default (a second,
    larger divergence) and depth ≥ 2 only occurs on opt-in.
- **doc/design/pi-integration.md** (~L87-88): replaced the stale fossil ("default 1 nesting level
  beyond orchestrator = depth 2 total ⇒ configurable") with the new default: 0 nesting levels beyond
  the orchestrator's direct subagents = depth 1 total; reconciled with architecture/user-guide.
- **CHANGELOG.md**: new dated `### Changed — main-session-only subagent dispatch by default
  (2026-07-16)` at the TOP of [Unreleased], above the description-based-feature-naming entry.
  Includes the 5→1 default, migration snippet (`"subagents": { "maxDepth": 2 }`, 2-vs-5 explicit),
  deliberate-divergence + PiCC-extension note, and the two-off-states contrast. Older released
  entries (incl. the "Claude-faithful" historical note at ~L362 and the [0.1.0] entries) left
  untouched per spec.
- **examples/full-surface/README.md** (L25): "Nested subagents (depth 2)" row now states nesting
  works because of the fixture's own explicit `subagents.maxDepth: 2` opt-in, not by default.

## Decisions on "Left open"

- Placed the new controls documentation as a §4 subsection (near the dispatch narrative) rather than
  in the §5 config section, because §5 is scoped to project-external config and these are project
  `.claude/settings.json` keys.
- Architecture nested-background note kept concise: one added sentence framing recursion-off-by-
  default as the larger divergence, without re-deriving the per-depth-budget math.

## Terminology

Mirrored the shipped t02 registry wording: "main-session-only", "PiCC extension with no
Claude-settings equivalent", "raise subagents.maxDepth to 2..5", "Claude Code's five-level nesting is
fixed and not configurable".

## Verification

- Grep of README/doc/CHANGELOG/examples for `depth cap`/`default 5`/`5 nesting`/`Claude parity`
  (excluding doc/plan|research|review and generated supported-features.md): remaining hits are the
  corrected edits, unrelated "Claude parity" prose (background settlement UX), and historical
  released CHANGELOG entries — no stale subagent-depth claim remains.
- `npm run typecheck` + `npm test` — see report (prose-only changes; confirmed no snapshot regressed).
</content>
</invoke>
