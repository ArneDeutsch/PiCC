# t01 execution log — flip default to main-session-only + enforcement tests

## Production change (the only one)
- `src/discovery/settings.ts`: `createDefaultSettings().subagentMaxDepth` `5 → 1`,
  and rewrote the adjacent comment from the "Claude Code allows up to 5" framing to
  the deliberate PiCC divergence (main-session-only by default; opt into nesting via
  `subagents.maxDepth: 2..5`, a PiCC extension key). No enforcement logic touched in
  `src/index.ts` or `src/runtime/subagents.ts` — confirmed by reading them that the
  gate is driven purely by `subagentMaxDepth` (buildSubagentSystemPrompt catalog gate,
  the customToolsFor Agent/Task provisioning gate, and the runtime backstop at
  `opts.depth > maxDepth`). Nothing to escalate — enforcement is correct as-is.

## Tests
- `test/discovery.test.ts`: default assertion `subagentMaxDepth` `5 → 1`, comment fixed.
  Left the explicit-override / both-spellings / re-enable test (recognized-toggles)
  untouched — it still covers explicit `maxDepth: 5`, `enabled: false`, `disableSubagents`.
- New file `test/main-session-only-default.test.ts` (chose one focused file over
  scattering — the "Left open" call; keeps the AC#3/#4 offline-integration + the unit
  guard together with a shared narrative). Contents:
  - **Unit (AC#4 core + mirror):** `makeSubagentRuntime(..., { maxDepth: 1 })` +
    `dispatch({ depth: 2 })` → `ok:false`, error contains "depth"; positive mirror
    `{ maxDepth: 2 }` + `dispatch({ depth: 2 })` → `ok:true` (opt-in restores one generation).
  - **Offline-integration, default project (bare mkdtemp, no settings.json ⇒ maxDepth 1),
    real `picc()` + fake SDK via the `{ sdk }` test seam:**
    - Positive control (AC#2): main session has `Agent`+`Task`, `before_agent_start`
      prompt contains "Available subagents".
    - AC#3: dispatch `general-purpose` (inherits ALL tools) foreground; its created
      session `customTools` contains `SlashCommand` (proves broad inheritance/provisioning
      ran — non-vacuousness) but NOT `Agent`/`Task`; its `systemPromptOverride()` omits
      "Available subagents".
  - **Offline-integration, context:fork alternate path (AC#4), purpose-built
    default-settings fixture:** a `context: fork` skill (`fork-x`, `agent: researcher`)
    + a `slash-agent` inheriting all tools (⇒ granted SlashCommand). The dispatched
    depth-1 subagent invokes `/fork-x` via its SlashCommand instance → `forkDispatch`
    at depth 2 → runtime guard refuses. Assert: caught error contains "exceeds the
    configured maximum"; `registry.list()` has a depth-1 `slash-agent` record
    (positive control — the dispatch really ran) and NO `depth === 2` record (register
    happens after the guard, so a refused depth-2 dispatch never registers).

## AC#5 by composition (no vacuous clone — as instructed)
Did NOT clone `sendmessage.test.ts` resume test, since its fake `customToolsFor` is a
recorder that emits tools regardless of depth/maxDepth (a resumed-tools assertion there
would pass vacuously). Instead AC#5 = composition of two REAL facts already under test:
1. My AC#3 test proves the *real* `customToolsFor` (the `picc()` closure) gates a
   depth-1 subagent to no `Agent`/`Task` under the default.
2. Existing `test/sendmessage.test.ts` "SECURITY (MUST-FIX #1): the resumed dispatch
   re-applies the full enforcement stack …" proves resume re-invokes `customToolsFor`
   with **preserved `depth=1`** (`SYS:reviewer:depth=1`) and **identical `customTools`**
   (`c1.customTools` names `toEqual` `c0.customTools` names).
Together: a resumed depth-1 subagent re-runs the same real gate at the same depth →
it cannot regain `Agent`/`Task`. That is AC#5.

## AC#6 (explicit opt-in) reliance
The runtime-level positive mirror (`maxDepth: 2` admits `depth: 2`) is the new
unit-level guard here. Behaviorally, `examples/full-surface` (pins `maxDepth: 2`) and
`test/fork-nested-guard.test.ts` (raises to 3) remain the end-to-end proof that
`maxDepth: 2..5` restores nesting — not weakened by this task. Both stayed green under
the new default (they set explicit settings, so the default flip does not reach them).

## Non-vacuousness verification (did, then reverted)
Temporarily flipped the default back to 5 and re-ran the new file: the AC#3 absence
assertion failed (`toolNames` included `Agent`) and the AC#4 test failed (no `forkError`
— the depth-2 fork succeeded). Reverted to 1; all green again. This confirms the absence
assertions detect the depth gate specifically, not a missing grant.

## Friction / surprises
- `git checkout` to undo the temporary sed was (correctly) blocked by the sandbox
  classifier because it would also discard the real deliverable edit; used the Edit tool
  to flip 5↔1 instead. Clean.
- Used the `{ sdk }`/`onWired` in-process test seam (like `slashcommand-fork`) rather
  than the module-level `vi.mock` (like `builtin-agents`) — no global SDK mock needed,
  and `onWired` hands back the real `subagentRegistry` for the depth-record assertions.
- New mkdtemp+chdir fixtures use the Windows-safe cleanup idiom (try/catch around
  `rmSync` with retries) via a local `rmQuiet` helper.

## Results
- `npm run typecheck`: clean.
- Full suite `npm test`: 1242 passed / 16 skipped / 0 failed (baseline 1237/16/0; +5 new).
