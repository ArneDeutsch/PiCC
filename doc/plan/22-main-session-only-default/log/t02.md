# t02 execution log

- Rewrote `setting.subagentMaxDepth` note: dropped "default 5" and "Claude parity";
  now states default 1 = MAIN-SESSION-ONLY, subagents.maxDepth is a PiCC extension
  with no Claude-settings equivalent, raise to 2..5 to opt in, and that Claude's
  five-level nesting is fixed (matching a ceiling is not settings parity). Tier kept `full`.
- Added PiCC-extension framing to `setting.subagentsEnabled` (folded in the
  `disableSubagents` alias disclosure — reads cleanly, so I did the optional bit) and
  `setting.subagentConcurrency`. Both name the `subagents.*` key and say "no
  Claude-settings equivalent". Tiers kept `full`.
- `tool.Agent` and `tool.Task`: APPEND-ONLY. Appended a "MAIN-SESSION-ONLY BY DEFAULT"
  sentence after the existing "see tool.Agent.fork" clause (converted the trailing
  " — see tool.Agent.fork (§4.3)" into " — see tool.Agent.fork. <new sentence> (§4.3)").
  No existing wording reworded; all ~16 pinned substrings in test/registry.test.ts
  (~199-222) untouched, so no test edits needed there.
- `feature.background-agents`: replaced the single "Nested (depth ≥ 2) fan-out uses
  per-depth budgets..." sentence with a version that first states nesting is OFF BY
  DEFAULT (subagents.maxDepth 1 = main-session-only) and depth ≥ 2 only on operator
  opt-in, then repeats the exact asserted substrings verbatim: "per-depth budgets",
  "maxDepth × concurrency", "Claude's single global (~10) parallel-agent cap". All three
  asserted substrings (test ~397-399) preserved; no test edits needed.
- No test/registry.test.ts assertions were changed — every edit was append/preserve.
- Ran `npm run gen:capabilities` — doc/supported-features.md regenerated (6 insertions /
  6 deletions, exactly the 4 target rows: tool.Agent, tool.Task, feature.background-agents,
  and the three setting.subagent* rows). No hand edits.
- typecheck: clean. Full suite: 1242 passed, 16 skipped, 0 failed (53 files passed, 1 skipped).
- Surprise: t01 had already flipped createDefaultSettings (discovery.test expects
  subagentMaxDepth 1), so the registry statement is now truthful against the shipped default.

## Coordinator review fixes (Phase 7 triage)
- claude-parity SHOULD: dropped the "ordinary" qualifier on `setting.subagentsEnabled` (it under-claimed — `subagentsEnabled:false` disables ALL dispatch incl. fork, since the Agent/Task/SendMessage block is skipped); reworded to say so.
- claude-parity NIT: fixed off-by-one — "opt into that many nested generations" -> "allow that many levels below the main session" on `setting.subagentMaxDepth`.
- Re-ran `npm run gen:capabilities`; matrix back in sync.
