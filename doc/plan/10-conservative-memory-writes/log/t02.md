# t02 execution log

## Tier decisions
- All three memory registry entries downgraded `full` → `partial` (setting.memory L157,
  agent.frontmatter.memory L211, feature.agent-memory L241). Coherent: the same
  conservative-write divergence is now advertised at one tier across all three.
- Each note follows the existing `PARTIAL:` house style (tool.Agent/tool.TaskStop): loading
  facts stated as full parity, then a `PARTIAL:` clause disclosing that PiCC writes only on
  explicit request while Claude Code also writes proactively, plus the CLAUDE.md opt-in.
- setting.memory note used the spec's recommended wording verbatim. The other two mirror it,
  adapted (per-agent "subagent writes/updates…"; feature entry as the umbrella).

## Note wording — stale-phrase check (human-verify, no automated test)
- Grepped the registry for `write-back|persistence|proactive`: only the three edited notes
  match, and each uses "proactively" solely to describe *Claude Code's* behaviour while
  explicitly stating PiCC is conservative. No note implies PiCC writes proactively. The stale
  "write-back conventions" / "persistence guidance" phrasings are gone.

## Generated matrix
- Ran `npm run gen:capabilities` (148 capabilities); committed doc/supported-features.md
  unedited. Matrix rows 113/165/185 now read `partial` with the new notes. Matrix-sync test green.

## User guide
- Reworded the Memory artifact-table row (L132): "with conservative write-back — memory is
  written only when you explicitly ask it to remember something (see note below)".
- Added a blockquote callout right after the artifact table explaining the conservative default,
  the divergence rationale, and a copy-pasteable `## Memory` CLAUDE.md opt-in snippet.
- §7: moved "auto memory (`MEMORY.md`)" and the `memory:` scopes mention OUT of the **Full**
  paragraph and INTO the **Partial** paragraph, appended after the `maxTurns` sentence, phrased
  as loading full parity / conservative writes by default / opt into eager via CLAUDE.md.
- Left loading-only lines (L15, CLAUDE_CODE_DISABLE_AUTO_MEMORY ~L281) untouched.

## CHANGELOG
- Reworded the existing `[Unreleased]` `### Added` "Auto memory" bullet (did NOT add a separate
  `### Changed` entry, per spec). Dropped "write-back conventions"; now states conservative
  explicit-request writes, loading unchanged, no setting added, CLAUDE.md opt-in. Grep confirms
  no `[Unreleased]` bullet still says "write-back conventions".

## Verification
- `npm run typecheck`: clean.
- `npm test`: 41 files passed / 1 skipped; 1017 tests passed / 16 skipped. Tier-count and
  doctor-report tests green (dynamic counts absorbed the full→partial shift).

## Surprising / notes
- Nothing surprising. Did not touch architecture.md or README.md (spec: their memory passages
  are loading/support claims, still accurate).
