# t01 execution log

- Replaced the single git bullet in `HARNESS_CONVENTIONS` (`src/runtime/context-assembly.ts`)
  with one `Commits:` bullet honoring constraints 1-5.
- Shipped the spec's "Recommended wording" verbatim — it already satisfies all five
  constraints, so no "Left open" polish was needed:
  - trigger scope (user OR skill/project instruction) — constraint 1;
  - read status/diff + recent git log, "match … style where it is richer" — constraint 2;
  - why-over-what body gated to "a non-trivial change" — constraint 3;
  - `--no-verify` sentence preserved verbatim as trailing sentence — constraint 4;
  - no Co-Authored-By / trailer / attribution mention — constraint 5.
- Added the F19 anti-regression assertion pair (`/recent git log/i`,
  `/why the change was made/i`) in `test/runtime-core.test.ts`, right after the F15 pair
  inside the existing "builds a suffix containing instructions, rules, skills, agents and
  steering" test, mirroring the comment-plus-`toMatch` style.
- Nothing surprising in the code; block is a plain template literal, all assertions are
  substring/regex as the spec notes.
