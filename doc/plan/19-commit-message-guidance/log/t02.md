# t02 — Truthfulness surface (registry, matrix, docs) — execution log

## Registry entry (src/registry/capability-registry.ts)
- Added one entry, placed immediately after `feature.agent-memory` (the closest sibling:
  both are `partial` prompt-injection features with an inline PARTIAL disclosure).
- Exact note text shipped:
  > always-on, every-turn nudge in the conventions block (rebuilt each turn, survives
  > compaction): when asked to commit, first read the changes (git status/diff) and recent
  > git log, match this repo's commit-message style where it is richer, and — for a
  > non-trivial change — write a short why-over-what body; the --no-verify prohibition is
  > preserved. PARTIAL: guidance only, outcome model-dependent (a prompt nudge, not a
  > deterministic output shape); NOT full Claude Code commit parity — omits the HEREDOC
  > commit form, the attribution trailer, and parallel git status/diff/log batching; commit
  > attribution stays governed by setting.includeCoAuthoredBy
- Wording tracks what t01 actually shipped in HARNESS_CONVENTIONS (the `Commits:` bullet).
- **§ref decision:** omitted. Existing feature entries carry §refs, but no design-doc
  section cleanly covers the conventions block / commit guidance; per the task's guidance I
  omitted rather than invented one.
- Did NOT touch `setting.includeCoAuthoredBy` / `setting.attribution` (referenced them by
  name in the note instead).

## Matrix (doc/supported-features.md)
- Regenerated with `npm run gen:capabilities` (149 capabilities). Not hand-edited. New row
  present at feature.commit-message-guidance.

## CHANGELOG.md
- New `### Added — richer git commit messages by default (2026-07-15)` at the top of
  `## [Unreleased]`, before the background-by-default entry.
- Includes the "why you'd care" clause (closes the visible quality gap between Claude
  Code-authored and PiCC-authored commits on the same repo), the searchable phrase "commit
  message", honesty register ("approximates", not "matches"), `--no-verify` preserved,
  steering named as the lever.

## doc/user-guide.md
- Extended the `steering` bullet in `### Harness configuration`: steering is appended AFTER
  the conventions block, so a contrary steering entry (or a project commit rule) overrides
  the built-in behavior (text can't be deleted, but behavior can be overridden). Added a
  copy-pasteable `"openai/*"` example: "Keep commit messages to a one-line subject; no body
  unless I ask." Avoided a bare "cannot disable".

## README.md
- Added a brief `- **Git commits**` bullet to the "What it does" capability list (not a new
  section). Contains "commit message", honesty register (nudges/approximating, not full
  ceremony).

## Verification
- `npm run typecheck`: clean.
- `npm test`: 1087 passed / 16 skipped (46 files passed, 1 skipped) — matches the green
  baseline. test/registry.test.ts stayed green; its tier counts are computed dynamically
  (no hardcoded totals to update), and the matrix-freshness guard passed against the
  regenerated doc.

## Deviations / surprises
- None. All count assertions in test/registry.test.ts are dynamic
  (`CAPABILITY_REGISTRY.filter(...)`), so the new entry required no test edits.
