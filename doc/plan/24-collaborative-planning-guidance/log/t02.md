# t02 execution log

- Added `feature.collaborative-planning` (`partial`) directly above
  `feature.managed-policy` (right after `feature.commit-message-guidance`) in
  `FEATURE_ENTRIES`. Followed the F19 note precedent: single line, semicolons, no `\n`.
  Note states always-on/every-turn/rebuilt/survives-compaction, what it steers, and the
  PARTIAL rationale (guidance only, model-dependent, not a mode/state machine, no
  plan-mode gate) plus the explicit "a planning posture, not plan mode; plan-mode tool
  surface stays degraded-noop (see feature.plan-mode)" disclaimer. Does not imply
  feature.plan-mode works.
- Regenerated the matrix via `npm run gen:capabilities` (151 capabilities). New
  `feature.collaborative-planning | partial` row present at doc/supported-features.md:188.
  Did not hand-edit the generated file.
- CHANGELOG: inserted a new `### Added — collaborative planning posture by default
  (2026-07-16)` block at the very top of `[Unreleased]`, above the description-based
  feature-naming entry. F19-styled: bold lead sentence, applies-to-every-project +
  best-effort/model-dependent framing, closes with the steering-override pointer to the
  user guide. Inserted as a whole block (not interleaved).
- User guide: added a collaborative-planning paragraph + a second worked steering example
  in the Harness-configuration steering section, reusing the existing config paths and the
  worked-example idiom, using the term "steering" throughout.
- Wrote evaluation.md — maintainer-facing reference for the three issue #51 scenarios
  (ambiguous planning w/ two visible choices; clear detailed ticket; confirmed
  implementation), what to observe, and that the maintainer runs them live in picc with
  GPT-5.6 Sol and rates them separately. Not a test.
- README: no change needed (defers steering to user guide, matrix to supported-features).

## Deviations / friction
- None. Matrix-freshness test (registry.test.ts:532) was expected to fail pre-regen; it
  passes after regeneration.
