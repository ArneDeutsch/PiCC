# t01 execution log

- Added `COLLABORATIVE_PLANNING_MAX_WORDS = 120` and `COLLABORATIVE_PLANNING_GUIDANCE`
  (both exported) ABOVE the `HARNESS_CONVENTIONS` declaration in
  `src/runtime/context-assembly.ts` (TDZ requirement — the const is interpolated
  into that template literal, so a lower declaration would ReferenceError at load).
- Composed `HARNESS_CONVENTIONS` to append `${COLLABORATIVE_PLANNING_GUIDANCE}` as
  trailing bullets after the existing Commits bullet, keeping the nudge inside the
  conventions block. `HARNESS_CONVENTIONS` stays module-private / first section.
- **Wording deviation from the spec's transcribed draft:** the spec draft counted
  121 words by the budget test's own tokenizer (not the claimed 119) — over the 120
  ceiling. Applied two minor polish cuts the spec explicitly permits: "in the repo
  first" -> "in the repo", and "has genuinely happened" -> "has happened". Final
  count: **119 words, 774 chars** (CRLF-normalized). All guardrails preserved (no
  time budget, question-guard co-located with "ask", Planning:/Implementation:
  prefixes, sequenced skill gate, concrete verbs).
- Presence/subagent-reach grep phrase: `ask only when blocked` (newline-free,
  load-bearing, from the Implementation bullet — one of the spec's suggested
  phrases).
- Tests added:
  - `test/runtime-core.test.ts`: presence+placement assertion appended to the
    "builds a suffix containing…" test (nudge after conventions header, before next
    `\n## `); new `it(...)` budget guard on the exported const (words >=60 and
    <=MAX; MAX==120; chars <=900).
  - `test/builtin-agents.test.ts`: `toMatch(/ask only when blocked/)` in the Plan
    (skipProjectContext) dispatch test — required subagent-reach pin.
- No `src/index.ts` change and no new `AssemblyInputs` field, as the spec noted —
  both call sites go through `buildSystemPromptSuffix`.
