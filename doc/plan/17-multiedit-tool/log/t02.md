# t02 execution log — wire MultiEdit, retire stub, retier registry

## Wiring edits (all four landed as specified)
1. `src/index.ts`: imported `createMultiEditTool` from `./runtime/tools/multi-edit.js`; added
   `createMultiEditTool(get) as unknown as Record<string, unknown>` to `buildCwdBoundTools`'s
   returned array (after the Glob entry, before the taskBundle spread). This reaches the main
   session (registerTool loop) and subagents (granted-gated). Did NOT touch the pi.registerTool
   built-in-override block or PI_TO_CLAUDE/CLAUDE_TO_PI — MultiEdit has no Pi built-in, name stays
   verbatim.
2. `src/index.ts`: added literal `"MultiEdit"` to `allKnownToolNames()` (before the DEGRADED_TOOLS
   spread), so a subagent `tools:[MultiEdit]` grant is no longer treated as unknown now that it left
   DEGRADED_TOOLS.
3. `src/runtime/tools/degrade-stubs.ts`: removed the `MultiEdit` entry from `DEGRADED_TOOLS`; added
   a bottom-comment note (house style, matching the SlashCommand/TaskOutput lines) that MultiEdit is
   real now (F17).
4. `src/runtime/tool-map.ts`: added `"MultiEdit"` to the `["Read","Write","Edit"]` list in
   `touchedFilePath()` so on-touch nested-CLAUDE.md / path-scoped injection fires (MultiEdit uses
   `file_path`, already handled by `input.file_path ?? input.path`).

No `permissions.ts` change — `FILE_EDIT_TOOLS` and `pathSpecifierMatches` already cover MultiEdit
(verified, left untouched per spec).

## Registry retier
`src/registry/capability-registry.ts` `tool.MultiEdit`: `degraded-noop` -> `full`. Note text written
(leads with the baseline-removed hedge; keeps `§` marker):

> real implementation of the historical Claude Code MultiEdit — NOTE Claude Code 2.1.x removed the
> tool (about v2.0.8), so `full` means faithful to the pre-removal contract, a superset of the pinned
> baseline kept as an older-project compatibility courtesy, NOT 'matches current Claude Code':
> batched, strictly exact-string edits (no fuzzy fallback, unlike PiCC's Edit) applied sequentially
> to one running buffer (each edit sees the prior edit's result), atomic (any miss rejects the whole
> batch, file left untouched), per-edit replace_all with unique-else-error, and an empty old_string
> on the first edit of a new file creates it (§4.8)

Regenerated `doc/supported-features.md` via `npm run gen:capabilities` (148 capabilities). Not
hand-edited.

## Tests
- `test/runtime-core.test.ts`: added `expect(touchedFilePath("MultiEdit", { file_path: "x.ts" })).toBe("x.ts")`
  in the existing touchedFilePath case.
- `test/registry.test.ts`: added `"MultiEdit"` to the core full-tier list; added the two drift
  assertions (tier full + `stubNames.has("MultiEdit") === false`) alongside the SlashCommand block.
- `test/integration-extension.test.ts`: added `"MultiEdit"` to the tool-surface registration list;
  added a new end-to-end nested-injection test that builds a FRESH `fakePi()` + `picc()` instance
  (pristine injection-dedup state, cwd already = fixture dir from beforeAll) and fires a `MultiEdit`
  tool_call on `src/main.rs` as that instance's first `src/` touch, asserting `FS-NESTED-SRC-CLAUDE-MD`
  and `FS-RULE-RUST-PATHSCOPED` inject. No deny-block integration test added (per spec; decision proven
  in permissions-hardening.test.ts). No fixture edit.

### Decisions ("Left open")
- Registry note wording: used the spec's suggested text, tightened the hedge ("NOT 'matches current
  Claude Code'") and made empty-old_string "on the first edit of a new file" for precision.
- Did NOT add a separate `allKnownToolNames()` guard test — the integration registration assertion
  already covers the observable effect (as the spec allowed).
- Fresh-instance test: rather than the `onWired`-seam `wire()` helper (which is settlement-specific),
  used a plain fresh `fakePi()` + `picc()` since the test only needs pristine injection-dedup state,
  not the internals seam. Kept the 500ms async-IIFE beat for parity with the beforeAll wiring. No
  trouble — passed first run.

## Verification
- `npm run typecheck`: green.
- Targeted (registry + runtime-core + integration-extension): 162 passed.
- Confirm-green (permissions-hardening + agents + tools-parity): 123 passed, 1 skipped — untouched.
- `npm test` (full): 1110 passed | 16 skipped (baseline ~1109 + the one new integration test). No new
  failures.
