# t03 execution log — capability registry truthfulness + CHANGELOG

## Key decisions

- **tool.TaskOutput** (tier kept `full`): removed the inverted clause ("the
  background-task registry is session-wide, so a subagent granted TaskOutput can
  reach ANY session task … whereas Claude Code hides TaskOutput from subagents
  entirely (a project-intended restriction PiCC does not enforce…)"). Replaced
  with the honest scoped behavior: subagents INHERIT TaskOutput (only the five
  named tools are withheld; "hidden TaskOutput" is filed Claude bug #15098/#23154,
  not its contract); a subagent's TaskOutput reaches only tasks it dispatched;
  coordinator reaches every session task; foreign/unknown task_id refused cleanly
  without leaking existence/label/status/output; explicit HONEST HARDENING note —
  the per-dispatcher guard matches Claude's fresh-context isolation in practice and
  is stricter than Claude only on the #15098 coordinator-passed-id edge, NOT a
  blanket "non-divergent" claim.

- **tool.TaskStop** (tier kept `partial`): left the existing task_id-only-vs-agent-
  id/name partial untouched (F13 non-goal). Appended the same scoped-behavior +
  #15098 hardening note, cross-referencing tool.TaskOutput ("This is the identical
  per-dispatcher guard as tool.TaskOutput … see tool.TaskOutput").

- **feature.background-agents**: no false claim existed; added one truthful clause
  ("Subagent TaskOutput/TaskStop are scoped to the subagent's own dispatched tasks
  while the coordinator retains full session-wide reach (F13; see tool.TaskOutput
  for the #15098 hardening note)."). No "Claude hides TaskOutput" language added.

- **test/registry.test.ts** (:235-238 region): rewrote the comment; replaced the
  now-false positive assertions `toContain("session-wide")` /
  `toContain("hides TaskOutput from subagents")` with
  `toContain("only tasks it dispatched")`, `toContain("#15098")`,
  `not.toContain("hides TaskOutput from subagents")`,
  `not.toContain("session-wide, so a subagent")`. Also added two positive
  assertions on the TaskStop note (`only tasks it dispatched`, `#15098`) after the
  existing discard-contract assertions — TaskStop's kept partial-string assertions
  were left intact.

- **doc/architecture.md**: added one line to the `background-tasks.ts` bullet —
  subagent TaskOutput/TaskStop scoped to the dispatcher's own tasks via
  `registry.scopedTo(ownerId)` (F13); coordinator retains full session-wide reach.

- **CHANGELOG.md**: added a `### Fixed` entry under `[Unreleased]` (dated
  2026-07-14). Chose `Fixed` over `Changed` because this closes a cross-dispatcher
  isolation gap and a truthfulness bug in the registry.

- **doc/supported-features.md**: regenerated via `npm run gen:capabilities` (never
  hand-edited); 148 capabilities.

## Friction

- Wording emphasis: first draft used "ONLY tasks it dispatched" (uppercase) in
  both notes, but the assertion checks lowercase "only tasks it dispatched" (the
  spec's suggested phrase). One test run failed on the case mismatch; resolved by
  lowercasing "only" in both notes rather than weakening the assertion. Regenerated
  the matrix and re-ran.

## Results

- `npm run typecheck`: clean.
- `npm test`: 41 files passed | 1 skipped; 1028 tests passed | 16 skipped. Green,
  including the registry-matrix freshness sync test.

## Scope

- No src/runtime or src/index behavior changes. Stayed within the writable surface:
  capability-registry.ts, doc/supported-features.md (regenerated), registry.test.ts,
  architecture.md, CHANGELOG.md.
