# t05 — CONTRIBUTING + PR template + implement-feature creation-side guidance + CHANGELOG

## What was done
- **CONTRIBUTING.md** — added a `### Manual verification` subsection under "Pull requests", after the
  registry/doc-updates line. States the two artifacts (PR-description *guidance* vs. the
  *manual-verification comment* evidence), one-sentence before/after rationale, the applicability
  escape ("no manual verification needed: `<reason>`") with the docs-only / fully-auto-tested carve-out
  AND the skill/harness/prose-is-not-docs distinction, and a cross-platform worked example launching
  `node ../../bin/picc.mjs` from `examples/hello-claude`.
- **.github/pull_request_template.md** (new) — Summary; Automated checks checklist (`npm run
  typecheck`, `npm test`, with the CI Node 22/24 note); Manual verification guidance (Start your
  review here + expected observable outcome) with the applicability escape in an HTML comment; and a
  Manual-verification comment acknowledgement checkbox.
- **implement-feature/references/handoff.md** — proportionate refine of the "PR body" paragraph and the
  "Start your review here" skeleton only: added the applicability judgement + escape, threaded (not
  contradicted) with the existing "no runnable UI → picc executing the changed behaviour" doctrine,
  cross-referenced CONTRIBUTING/PR-template. Did not restructure the hand-off. No `/verify` skill cited.
- **CHANGELOG.md** — two `[Unreleased]` entries dated 2026-07-15 at the top: `### Added — evaluate
  skill` and `### Changed — implement-feature proposal-gating & verification guidance`, house format,
  bold-lead bullets with honest limits (redirect is behavioral, pending live smoke test).
- **test/evaluate-skill.test.ts** — new `verification-contract repo artifacts (t05)` describe block:
  PR template exists + carries guidance/escape/comment-ack; CONTRIBUTING carries both artifacts +
  escape + worked-example markers. Reads raw with `\r\n`→`\n` normalization (files are not loaded via
  loadSkillBody and `.gitattributes` does not force LF on `.md`).

## Key decisions ("Left open" items)
- **Canonical escape phrase:** chose **"no manual verification needed: `<reason>`"** (pr-eval's exact
  wording) across all three surfaces rather than the task's illustrative "no manual step needed", so
  CONTRIBUTING / PR template / pr-eval / handoff use one string.
- **Worked-example fixture:** `examples/hello-claude` (simplest fixture; both examples exist in the
  worktree). Launch is `cd examples/hello-claude && node ../../bin/picc.mjs` — verified picc.mjs
  resolves its extension relative to itself and runs in cwd, so the relative `../../bin` path works on
  Windows Git Bash and Linux alike.
- **PR template escape placement:** put the escape inside an HTML comment guiding the author, so the
  rendered template stays clean while the instruction is present in the file (and testable).

## Results
- `npm run typecheck`: clean.
- `npm test` (full suite): **47 files passed | 1 skipped; 1117 tests passed | 16 skipped**. No new
  failures. The evaluate-skill file alone: 28 passed. implement-feature reference-link integrity stayed
  green after the handoff.md edit.

## Notes / friction
- Did NOT touch `src/`, `doc/supported-features.md`, or the capability registry; did not run
  `gen:capabilities` (no capability added) — per spec.
