# t04 — Docs, capability-registry truthfulness, and fixture coverage (execution log)

## What changed

- **`src/registry/capability-registry.ts`**
  - `tool.TaskOutput` (tier stays `full`): extended the note to describe the new observability
    render — reuse of the shared subagent renderer (F04); a live view while awaiting a running task
    (self-identifying `Task(task-N) · Agent(<type>)` header + muted `agent-<id>` subline, rolling
    activity tail, current-activity line); the same call resolving to an outcome badge (completed/
    failed/aborted) + transcript path + per-subagent usage footer on settle; a poll (`wait:false`)
    showing status + last activity inside the same frame (never a bare unlabelled chip). Made the
    fidelity point explicit: the `agent-<id>` identity is shown at EVERY surface INCLUDING
    non-resumable one-shot builtins, with no false "resumable via SendMessage" invite (that invite
    renders only when the task is actually resumable). Kept the display-only/byte-identical
    model-contract note. Prior failed-status / session-wide-registry prose retained.
  - `feature.background-agents` (tier stays `partial`): added a truthful positive clause (a
    background task is observable while a TaskOutput call is awaiting it — the same live render +
    settle badge a foreground dispatch shows, every background surface naming its agent by type +
    `agent-<id>`) and, to GAPS, the requested divergence: **no always-on Agent View dashboard
    (Claude Code's `claude agents` shell command, 2.1.139+)** — background progress is observable
    only while a TaskOutput call is awaiting it, never an unattended panel. No `/tasks` slash command
    invented; the Claude surface is named as the `claude agents` shell command. No new capability id.

- **`doc/supported-features.md`** — regenerated via `npm run gen:capabilities` (it is a
  generated-from-registry file; the two rows above are its only diff). No hand-written prose exists
  in this doc, so there was no separate stale prose to fix; the old TaskOutput/background-agents
  wording is superseded by the regenerated rows.

- **`CHANGELOG.md` `[Unreleased]`** — added `### Added — background-task observability (2026-07-13)`
  at the top of Unreleased: TaskOutput streams like a foreground agent (live tail/activity → settle
  badge + transcript + usage; poll shows status inside the identifying frame); every background
  surface names its agent (type + `agent-<id>`), including non-resumable one-shot builtins, with the
  SendMessage invite still gated on resumability; display-only with the byte-identical verbatim model
  contract and unchanged settlement push, honoring the same width-clamp/sanitize guarantees.

- **`examples/full-surface/`** — added an executable background-dispatch + TaskOutput path (the
  fixture previously had only the word "background" in `fork-research/SKILL.md` prose):
  - `.claude/agents/async-researcher.md` — a `background: true` agent (also exercises the
    `agent.frontmatter.background` field, previously uncovered by the fixture), tools `Read,
    WebFetch, WebSearch`, replies `DONE-BG-RESEARCH`.
  - `.claude/commands/bg-research.md` — dispatches `async-researcher` via the Agent tool with
    `run_in_background: true` and retrieves it with `TaskOutput(task_id: "task-1")`; carries canary
    `FS-BG-TASKOUTPUT`.
  - `README.md` coverage map — new row documenting the background dispatch + TaskOutput path and its
    canary. Kept minimal and self-consistent with the existing fixture; structurally valid with no
    network/live model required (a static surface like the other agents/commands).

## Left-open decisions

- Chose a **`background: true` agent + a slash command** (over embedding the flow in an existing
  skill) so the addition mirrors the fixture's existing agent/command structure and covers both the
  `background:true` frontmatter and an explicit `run_in_background:true` + `TaskOutput` retrieval path.
- Did **not** touch `doc/architecture.md` — the `subagent-render.ts` module boundary is already
  covered by t01–t03 records; adding a note here was left open ("only if it improves accuracy") and
  is not needed for this docs/registry/fixture task.

## Verification

- `npm run gen:capabilities`: wrote `doc/supported-features.md` (148 capabilities); the diff is
  exactly the two edited rows (`tool.TaskOutput`, `feature.background-agents`). A second run produces
  no further diff — registry and matrix are consistent (idempotent).
- `npm run typecheck`: clean (`tsc -p tsconfig.json --noEmit`, no output).
- `npm test` (full suite): **984 passed | 16 skipped** — matches the t03 baseline exactly, no new
  failures. The in-process registry/matrix consistency test is part of the green suite.

## Deviations

- None. All edits stayed inside the task's writable surface (`capability-registry.ts`, the generated
  matrix, `CHANGELOG.md`, `examples/full-surface/**`). No `doc/supported-features.md` hand-edit (only
  regenerated). No git commit/push (coordinator owns commits).

## Review follow-up (coordinator) — two completeness gaps

Coordinator authorized two additions to the writable surface and asked to close them.

1. **`doc/user-guide.md` — "Observing subagents" section.** The durable guide documented live
   progress only for foreground/running dispatches and listed `TaskOutput` as just "polled/awaited".
   Added a new "Background tasks are observable too" bullet (matching the section's tone/length):
   an awaiting `TaskOutput` streams the background task's LIVE activity (rolling tail +
   current-activity line) like a foreground subagent, then settles in the SAME call to an outcome
   badge + transcript + per-subagent usage footer; a `wait:false` poll shows status + last activity
   in the same identifying frame; every background surface (start message, live/awaiting, poll,
   settled) names the task + agent as `Task(task-N) · Agent(<type>) · agent-<id>`, shown even for
   non-resumable one-shot builtins, with the "resumable via SendMessage" hint only when the task is
   actually resumable; display-only (verbatim result unchanged); honest boundary stated — no
   always-on dashboard, observable only while a `TaskOutput` call is awaiting it. Wording kept
   consistent with the CHANGELOG/registry entries.

2. **`examples/full-surface/README.md` header truthfulness + executable coverage.** The header says
   "each canary string is asserted by tests" but the new `FS-BG-TASKOUTPUT` canary was unasserted.
   Preferred adding the assertion over softening the header: added an integration test
   (`test/integration-extension.test.ts`, in the "skill activation" describe) —
   *"background dispatch + TaskOutput path is exercisable (F04)"* — that asserts (a) the
   `async-researcher` `background: true` agent reaches the routing catalog, and (b) `/bg-research
   <topic>` expands into the user turn via the `input` event carrying `FS-BG-TASKOUTPUT`,
   `run_in_background`, `TaskOutput`, and the substituted `$ARGUMENTS`. One iteration: the agent
   renders as `async-researcher (read-only):` (read-only tools), so the assertion uses the same
   `/- name( \(read-only\))?:/` pattern the neighboring catalog assertions use.

### Verification (post-review)

- `npm run typecheck`: clean.
- `npm test` (full suite): **985 passed | 16 skipped** (baseline 984 + 1 new integration test).
  Green. `npm run gen:capabilities` remains idempotent (no further matrix diff).

## Feature-close review fixes (post-t04)

- **Removed dead `noteActivity`** — verified zero production callers via grep (only the
  definition + comment references inside `noteProgress` remained; both former onProgress callers
  were repointed to `noteProgress` in t02). Deleted the `noteActivity` method from
  `src/runtime/background-tasks.ts` and its now-orphaned unit test ("noteActivity surfaces live
  activity…") from `test/background-tasks.test.ts`. `noteProgress`/`subscribeProgress` untouched.
- **User-guide overclaim fix** (`doc/user-guide.md`) — the background-surface paragraph no longer
  pins the single literal `Task(task-N) · Agent(<type>) · agent-<id>` to *all* surfaces (inaccurate
  for the START message). Now states the identity components (task id, agent type, `agent-<id>`) are
  shown at every surface, and notes the start block leads with `Agent(<type>) → background as
  task-N`, while live/poll/settled render the `Task(...) · Agent(...) · agent-<id>` form.
- **Esc/background note** (`doc/user-guide.md`) — added a sentence near the existing Esc mention:
  pressing Esc while *awaiting* a background task only detaches the live view; the task keeps running
  (retrieve again with `TaskOutput`) — Esc does not stop a background task.
- typecheck green; `npm test` → 984 passed | 16 skipped (was 985; the one removed test accounts for
  the delta).
