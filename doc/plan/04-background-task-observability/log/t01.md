# t01 — Extract shared subagent renderer

- Docs live under the worktree path (not main repo); initial Read of main-repo paths 404'd — read from worktree instead.
- Moved the render cluster from `src/runtime/subagents.ts` (block "t03 live-progress + result rendering helpers", lines ~1435–1707) into new `src/runtime/subagent-render.ts`, byte-for-byte:
  - private helpers: `themedFg`, `themedBold`, `pushWrapped`, `pushColored`, `clampLines`, `sanitizeInline`, `firstLine`, `formatUsageLine`, `outcomeBadgeLine`, `stripAgentTrailerForDisplay`
  - exported: `renderAgentCall`, `renderAgentResult` (public contract for later tasks)
  - kept the header contract comment and all security/width invariant comments verbatim.
- `progressActivityLine` deliberately LEFT in `subagents.ts` (t02 relocates it). It sat mid-block; kept it with its doc comment.
- New module imports: `truncateToWidth`/`visibleWidth`/`wrapTextWithAnsi` (pi-tui); `sanitizeProgressText`/`formatUsageCompact`/`type ProgressSnapshot` (./subagent-progress.js); `isAgentId` (../util/subagent-transcripts.js). No import from subagents.ts (no cycle).
  - Did NOT import `renderProgressText` into the new module: the moved block never uses it (it's used only in the un-moved execute branch). Spec listed it but "Left open" marks re-export as optional; kept imports minimal to avoid an unused import.
- `subagents.ts` import fixes: removed the now-unused pi-tui import line (all three symbols were only used by moved helpers); dropped `formatUsageCompact` and `sanitizeProgressText` from the subagent-progress import (moved-only); kept `renderProgressText`, `SubagentProgressCondenser`, `ProgressSnapshot`. Kept `isAgentId` (still used at dispatch guard, line ~545). Added `import { renderAgentCall, renderAgentResult } from "./subagent-render.js"`.
- `createAgentToolDefinition`'s `renderCall`/`renderResult` delegate to the imported functions exactly as before — untouched.
- No test imported the moved helpers directly (all go through `createAgentToolDefinition`), so no test import repointing was needed.
