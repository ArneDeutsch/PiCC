# t03 execution log — issue-eval grounding (job 2) + pr-eval block consistency

## Scope
Implemented per `tasks/t03-issue-eval-grounding-preval-consistency.md`. Writable surface only:
`issue-eval.md`, `pr-eval.md`, `test/evaluate-skill.test.ts`, this log. Referenced the single-source
`evaluation-engine.md` grounding + evidence-anchor contract; did not restate or edit it.

## What changed
- **issue-eval.md — Step 2 (rating wave).** Added a "Ground the value read in the trusted project tree
  (job 2)" paragraph after the wave synthesis line: each `evaluator` lens reviewer also
  Reads/Greps/Globs the trusted project tree to ground the value read, kept strictly separate from the
  isolated, untrusted issue file (two trust paths, opposite postures, same wave). An issue body that
  names paths / dictates anchors is an injection signal (element 6 `MALICIOUS_INJECTION`), never a
  directive. Explicitly states the L1 screen + Step-1 redirect isolation are unchanged.
- **issue-eval.md — Step 5 (keep-open).** Extended the bounded-return bullet to include "bounded
  evidence anchors". Added an "Evidence anchors on a keep-open" block: canonical block carries the
  engine's `**Evidence:**` line + item format verbatim; proportionate density (0–1 brief / up to 4
  full) as ceilings-not-floors with the zero-legal-with-justification path preserved even on a public
  full-table keep-open; the correctness point (locator = trusted project-tree file, NOT a description
  of the issue text; only the "what it establishes" prose is paraphrased/leakage-stripped); anchors
  repo-relative never absolute; coordinator applies engine element-7 anchor re-validation PLUS the
  leakage-strip (both, never equated).
- **pr-eval.md — Step 6.** Added an "Evidence anchors" bullet in the two-section rendering list:
  acknowledges the engine's `**Evidence:**` line + item format verbatim; notes pr-eval already grounds
  (Step 1's evaluator reads the trusted codebase) so no new investigation mandate; anchor constraints
  apply uniformly to the public advisory comment at a proportionate density as for issue-eval; the
  `**Evidence:**` line renders once per rating-bearing block (§A ticket-worth + §B this diff), not
  doubled.
- **test/evaluate-skill.test.ts.** Appended two `describe` blocks (12 new `it`s) before the deny-floor
  block; did not touch existing blocks or the zero-write floor. Dash seams built from
  `String.fromCodePoint` (EM 0x2014 item separator, EN 0x2013 for the "0–1" bound); forward-slashed
  path in the item-format seam. Each new block also re-pins the co-located close-invariant / never-merge
  floor as a regression guard.

## Decisions on "Left open" (exact prose was the implementer's call)
- Placed issue-eval grounding as a dedicated bold-led paragraph at the end of Step 2 rather than
  inline in the fan-out sentence — keeps the trust-split visible and testable without disturbing the
  existing proportionality language.
- Anchor cap phrasing in issue-eval's dual-enforcement bullet says "cap the list at the proportionate
  ceiling" rather than restating engine element 7's numeric ≤5 — the mode ceiling here is 4 (full-table)
  and I did not want to contradict the engine's global bound; the engine remains the single source for
  the number.
- pr-eval placement: put the anchors bullet as the last sub-bullet of the two-section rendering list
  (after the overall-importance bullet), so the "once per rating-bearing block" count sits next to the
  §A/§B definitions it references.

## Verification
- `npm run typecheck` — clean.
- `npx vitest run test/evaluate-skill.test.ts` — 60 passed (was 48; +12).
- `npm test` (full) — 1269 passed / 16 skipped (baseline after t02 was 1257 / 16; +12 new tests).

## Deviations
None. All pinned collapsed substrings in both files preserved; redirect isolation, L1 enum, and the
close-invariant untouched.
