# t02 execution log — proposal-gate grounding (the F21 zero-tool-call fix)

## What changed

- `.claude/skills/evaluate/references/proposal-gate.md`
  - **Lightweight path** re-titled and re-scoped: heading now "fewer reviewers, never less
    grounding". Added an explicit "**Light means _fewer reviewers_, never _less grounding_**"
    frame plus the F21 callout (a value score returned in seconds with zero tool calls). The
    single-`evaluator` dispatch now **requires it to investigate the project first**
    (architecture/source/tests/docs/in-repo tracking via Read/Grep/Glob) **before** it scores
    the seven criteria; its input is reframed to "the proposal plus project evidence." A
    prose-only score is permitted **only** with the engine's explicit one-line no-evidence
    justification (cited to evaluation-engine.md, not restated).
  - **Second pass** trigger widened to "genuinely borderline _or higher-stakes_"; added "**no
    time quota and no mandatory committee for trivial**" and "**inherits the same grounding
    requirement**". Preserved the "second `evaluator` pass" / "never a Bash-capable
    `generalist`" / "structurally shell-free" invariants.
  - Added a "**Grounding is the evaluator's filesystem job**" paragraph: investigation is
    performed by the evaluator via Read/Grep/Glob; the coordinator adds no new gh/fetch/dispatch;
    the fixed action envelope is unchanged; "existing issue/plan tracking" means in-repo
    doc/plan/CHANGELOG/review.md, not a live GitHub query.
  - **Bounded structured return** gains a third field, "**bounded evidence anchors**", in the
    engine's `**Evidence:**` shape with the verbatim item format (em-dash U+2014, forward-slashed).
    The coordinator paragraph now applies the **anchor re-validation of engine element 7**
    (allow-list re-check, `..`/outside-repo/secret-file rejection, whole-item content-byte strip,
    repo-root normalization, ≤5 cap truncating over-count, never-re-open) and states it is
    **strictly stronger than** — not equal to — the per-criterion leakage-strip; the coordinator
    applies **both**.
  - **Rendered assessment** enumeration now includes the `**Evidence:**` block as a sibling
    below the overall-importance line ("**not** an eighth rubric row"), carrying the verbatim
    item format.
  - **Phase-8 density** note added to the Phase-8 filed-findings bullet: the surfaced assessment
    supports a **lean pick-list** (disposition + decision-flipping anchors) with the **full anchor
    set** in the filed `## Evaluation` body; the exact anchor budget is deferred to
    ticket-integration.md (t04's single home) — the 1–2 count is intentionally NOT restated here.

- `test/evaluate-skill.test.ts` — APPENDED one new `describe` block
  ("proposal-gate grounding + evidence anchors (evaluate-skill t02)") with 6 `it` blocks. No
  existing block touched. Dash-bearing seam (`EM = String.fromCodePoint(0x2014)`) matches the
  engine's anchor item format verbatim. Pins the OBLIGATIONS: light-≠-ungrounded (investigate
  before scoring + reframed input + filesystem-only grounding), prose-only-only-with-justification,
  second-pass higher-stakes/no-quota/grounded, anchors in the bounded return AND the rendered
  block, element-7 re-validation strictly-stronger-not-equated, and the Phase-8 lean-pick-list
  guidance.

## Key decisions (Left-open resolutions)

- Kept the light-path heading plain (no markdown emphasis) so the "fewer reviewers, never less
  grounding" obligation is a stable collapsed test seam; the emphasized restatement lives in the
  body prose.
- Phase-8 count: chose to state the lean-pick-list vs full-set split and explicitly defer the
  numeric anchor budget to ticket-integration.md, honoring the seam's "t04 is its single home"
  over the acceptance line's parenthetical "≤1–2" — satisfies the acceptance substance without
  double-owning the count.
- Referenced element 7 re-validation as strictly stronger than the leakage-strip and required
  BOTH, per the engine's element 7b warning against equating them.

## Verification

- `npm run typecheck` — green.
- `test/evaluate-skill.test.ts` — 48 passed (was 42; +6 new).
- `npm test` (full) — 1257 passed / 16 skipped, no failures (baseline given: 1249/16).

## Notes / friction

- All previously-pinned collapsed substrings preserved (zero github writes, shell-free, sandbox
  agent, second `evaluator` pass, generalist, bounded structured, remain in `review.md`, per-item
  user choice preserved, subtracts clear slop never, only annotates, never suppresses,
  ## evaluation, `gh issue close` is **never**) — additions only, no deletions.
- No `src/` change (non-goal honored). evaluation-engine.md and evaluator.md referenced, not edited.
