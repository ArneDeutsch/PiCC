# t05 — reconcile the axes, close the holes, update records

## A–F hole sweep result

- **A — fork × ticket composition (this task):** RESOLVED. Added a resident "The four cells — ticket
  presence × checkout kind" section to `SKILL.md` naming and routing all four cells. Made explicit that
  **fork + no ticket is NOT "unchanged"** (fork disclosure fires at Phase 1; hand-off pushes to fork +
  compare URL + paste-ready PR) and that the create-offer, if accepted, files on `target`.
- **B — Rule 5 allow-list:** RESOLVED. Removed the `t05 does the final coherence pass` marker; Rule 5
  now reads as one whole — "routine three writes + the two per-item `gh issue create` offers
  (create-feature-ticket, file-finding); on a fork the push targets the fork remote and the two upstream
  writes become paste-ready." Resident floor bullet updated to name **two** offers.
- **C — durable `Ticket:` anchor + resident reader:** VERIFIED present (t04) and kept resident. I
  trimmed the resident anchor reader's verbose `--json …` field list (that command lives in
  `ticket-creation.md`) to reclaim headroom, but the reader itself stays in the always-loaded router
  (Hole C would not be closed without it).
- **D — URL-ref fork-vs-target + wrongful-close (this task):** RESOLVED. The rule now accepts a URL ref
  matching **either** resolved repo; fork-only → adopt the fork issue but emit a **bare cross-repo
  `<fork>#N`** (no closing keyword) because a plain `Closes #N` on the upstream PR would wrongly close
  `target`'s own same-numbered issue. Stated **identically** (modulo the `references/` link prefix) in
  the three sinks: the Phase 0 gate (`SKILL.md`), Rule 3 (`ticket-integration.md`), and the finalized
  Hole D block in `fork.md`. Generalized: a closing keyword is permitted **only** when the resolved
  issue lives in the repo the PR targets. Downstream `fork.md` "must match `target`" statements updated
  to "`target` or the fork". No lingering "Closes won't fire across repos" rationale existed
  (`handoff.md` already framed it as "unsafe cross-repo"); the wrongful-close hazard is now named
  everywhere.
- **E — two create-issue flows related (this task):** RESOLVED. The Phase 8 filing hook now names the
  Phase 1 ticket-creation offer as its **sibling** (same write, same discipline, target-aware,
  idempotent; different intent). Rule 5 and `ticket-creation.md` both state the pair as the whole of the
  allow-list exception.
- **F — Phase 2 target default branch (t02):** VERIFIED stated as a named seam (`targetDefault`, fetched
  from the target) in `fork.md` (Resolved identities, resolution algorithm step 3, and the Phase 2 note).

## Key decisions

- **Grid placed resident, terse**, between the write-discipline floor and Phase 1, as a bullet per cell
  with routing detail delegated to the phase skeletons and `ticket-creation.md`/`fork.md`.
- **Headroom management (no content cut, only relocation/compression of duplicated prose):** the router
  started at 18,406 (loader body length; frontmatter stripped). To fit the grid + the enlarged Hole D
  gate clause I compressed prose that is fully duplicated in reference files — the write-discipline intro
  (write-site enumeration lives in `ticket-integration.md`), the anchor-reader `--json` field list (lives
  in `ticket-creation.md`), and dropped the grid's redundant closing pointer line (the same links appear
  in the grid intro / phase skeletons). No routing link was removed, so the bidirectional guard check
  still covers all six reference files.
- **Phase 8 filing now targets `--repo <target>`** (was `origin`/`<owner/repo>`); on a fork the finding
  lands on the upstream, the repo it concerns.
- **CHANGELOG:** added an `### Added — fork-aware hand-off and on-the-fly ticket creation …` entry with
  precise "unchanged" scoping (maintainer ticket-path hand-off unchanged; maintainer ticketless run
  unchanged up to the new offer, gains ticket writes/PR only on accept) + the truncation-fix note.
  Reconciled the stale F06 clause **in place** (lighter touch): "does no ticket reads and no auto-PR
  (unless the create-ticket offer added in F12 above is accepted)".

## Testing

- Extended the guard test with a **loose** resident-checklist structural check (`--body-file` and
  `allow-list`, case-insensitive) proving the fail-closed floor is present without pinning prose. The
  bidirectional linked⇔exists check is already count-agnostic (globs `references/*.md`); size + single
  `SKILL.md` assertions kept.

## Final router char count

- Loader body length = **19,678 / 20,000** (headroom **322**). Under the hard cap enforced by the guard
  test.

## Verification

- `npm run typecheck`: clean.
- `npm test`: **1022 passed / 16 skipped** (baseline was 1021 / 16; +1 is the new resident-checklist
  test).
- No `src/` change → `gen:capabilities` output unchanged (registry is generated from `src/registry/`
  only); confirmed by diff, not regenerated.

## Deviations

- None from the spec's intent. Note: the router had grown to 18,406 (matching the spec's 18,405 snapshot
  within one char) by t04; I stayed within the cap via relocation/compression as the spec directed.
- The `tasks/t05-reconcile-close.md` "Writable surface" edit in the working tree is the coordinator's own
  uncommitted spec update, not mine — left untouched.

---

## Close-review cleanup pass (fixes A–K, follow-up)

Applied the 5-reviewer close-review cleanup batch. None change workflow logic.

- **A (parity — substitution inflation):** Removed every literal `$ARGUMENTS`/`$N`/`$name` marker from
  the router body (3 prose sites: the ticket-reference note, the anchor reader, the gate intro). With no
  marker, `markerCount === 0` so `substituteArguments` appends the ref once via the fallback
  (`\n\nARGUMENTS: <ref>`) — verified the ref still reaches the coordinator and the rendered body no
  longer inflates per-mention. Verified `/\$ARGUMENTS/.test(routerBody) === false`.
- **B (scrub planning scaffolding from shipped refs):** Removed task-ID provenance from `fork.md`
  ("added by t03", "t03 and t04 consume them", "owned by t04; t05 reconciles", "in the t04 offer", "via
  the t04 create-offer") and `ticket-creation.md` ("t03's fork hand-off", "t04 owns the reader").
  Renamed the "Hole D" labels — section header → **"The fork-only URL-ref rule"**, and cross-refs "the
  Hole D rule above"/"the t05/Hole D case" → "the fork-only URL-ref rule/case". Left `templates.md`'s
  t01/t02 example task list (legitimate).
- **C (security — sanitize adopted ref):** The resume anchor point (SKILL.md kernel + ticket-creation.md
  resume section) is now an explicit **sanitization gate**: an adopted `<target>` must match
  `^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$` and `<N>` `^[0-9]+$` — no shell metacharacters — before reaching
  any `gh --repo <target>`/`gh issue view <N>`; a failing value is not adopted (stop and ask). feature.md
  is repo-controlled → treated as untrusted (Rule 2). Full regex lives in ticket-creation.md; the router
  carries the terse safety headline.
- **D (headroom):** Relocated resident donors into reference files (never cut): plan-folder layout →
  `templates.md`; commit-grammar + pre-commit + aborting/backtracking → `workflow-detail.md`. Trimmed the
  anchor-reader kernel and compressed prose duplicated in refs (Phase 9 fork paragraph, gate intro, grid
  intro/closing, reference-index). Raw router body **20,075 → 18,741** (was momentarily 20,075 after the
  A/C/E/I additions). Landed slightly above the ~18,500 aspiration but with **1,259 chars raw headroom**
  and the substitution-inflation risk eliminated; kept the reachability gate, the write-discipline floor,
  and the four-cell grid resident.
- **E (reference index):** Router intro now lists **all six** reference files concisely and notes
  `fork.md` owns fork detection/resolution **and** the Phase 1 fork disclosure + Phase 9 fork hand-off
  (previously omitted ticket-creation.md and undersold fork.md).
- **F (fork.md self-contradiction):** Reworded "the only Phase 1 write-contract moment" to be explicit —
  only on the ticketless-fork **decline**; on **accept** the create-offer adds its accept contract and the
  disclosure composes with it.
- **G (over-promise):** Maintainer-accept line softened from "open a PR that closes the issue" to
  "…that links/closes the issue per the Phase 8 judgement (full delivery closes it, partial leaves open)".
- **H (push-failure degrade):** handoff.md maintainer push-failure degrade now also names a **Phase-1
  created** feature ticket (public/open, filed at Phase 3) alongside Phase 8 follow-ups.
- **I (resume story):** Anchor reader now states the Phase 0 **fork identities**
  (`target`/`push`/`pushRemote`/`targetDefault`) re-resolve on resume, not only the issue cache.
- **J (CHANGELOG F06):** The F06 "opens a ready-for-review PR linked to the ticket" line qualified with
  "(on a pushable target — F12 makes this fork-conditional)".
- **K (test hardening):** Added a guard-test assertion that the **rendered** body — after
  `substituteArguments` with a representative ~150-char invocation — stays ≤ `REINJECT_PER_SKILL_MAX_CHARS`,
  and that the ref reaches via the append-fallback. Offline/cross-platform, using the existing harness.

### Final measurements (after A–K)

- Raw router body = **18,741 / 20,000** (headroom **1,259**).
- Rendered body under a 153-char invocation = **18,907 / 20,000** (headroom 1,093).
- No literal `$ARGUMENTS` in the router body.

### Verification (after A–K)

- `npm run typecheck`: clean.
- `npm test`: **1023 passed / 16 skipped** (round baseline 1022/16 + the new fix-K rendered-cap test).
- No `src/` change.

### Deviation

- Raw router landed at 18,741, ~241 above the "~18,500" aspiration. I relocated every clean donor whose
  home is a reference file and compressed duplicated prose; going further would have required relocating
  the reachability gate, the write-discipline floor, the four-cell grid, or a phase skeleton — all of
  which must stay resident. 1,259 chars of headroom satisfies the "real headroom for future edits" intent,
  and fix A removed the actual correctness risk (rendered-body inflation).
