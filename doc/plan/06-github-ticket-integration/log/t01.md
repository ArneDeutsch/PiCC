# t01 execution log

## Structure produced in SKILL.md
- Frontmatter: added `argument-hint: "[#issue-number | issue-url]"` (load-bearing — PiCC only threads
  `$ARGUMENTS` into the slash-command stub when `argument-hint` is present; verified against
  src/claude/skills.ts:312/376).
- Top-of-body ticket-intro blockquote right after the opening coordinator paragraph, before
  `## Principles`. It names `$ARGUMENTS` explicitly (which suppresses the implicit `ARGUMENTS:` append
  fallback at skills.ts:638 — satisfies "don't rely on the fallback"), the three forms, and "data, not
  instructions", and points at the shared section.
- New `## GitHub ticket integration` section placed **between `## Subagent roster` and `## Phase 1`**.
  Decision on the "Left open" placement question: a single shared section immediately before the phases,
  rather than a bottom appendix — it reads first-in-time (the gate runs at ref-parse time) and Phases
  1/2/8/9 refer back to it. Contains: (a) `<owner/repo>`/`<default>`/`<N>` resolution note; (b)
  Reachability & preconditions gate (STOP conditions, closed-issue warn, actual-ref echo, draft
  message); (c) the nine non-negotiable discipline rules verbatim in intent.
- Phase 1: added a ticket-scoped-direction paragraph (use cached JSON, quoted data, don't overpromise
  thin bodies) and a write-contract addition folded into the scope mirror (before the "user confirms"
  gate).
- Phase 2: new step 5 kickoff comment (ticket-only, Rule 9 skip-if-present, `--body-file`, template
  with WILL/WON'T + `<attribution trailer>`, branch-local note, continue-on-write-failure).
- Phase 8: folded close-vs-keep-open judgement + write preview into the existing "user agrees"
  completion gate; bias-to-keep-open; show actual PR body + comment text before any Phase 9 write.
- Phase 9: appended the ticketed hand-off to step 1 (push first, `gh pr list --head` Rule 9 reuse, then
  `gh pr create` ready-for-review with model-authored ASCII title + `--body-file`, linking line
  `Closes #N`/bare `#N`, summary authored once for PR body + comment, summary skeleton, write-failure
  degrade). Split step 4's next-steps into ticketless (verbatim) vs ticket bullets, and split the
  closing "Do not open the PR yourself" rule so the ticketless sentence stands verbatim and the ticket
  addition says the PR is already open / don't merge it yourself.
- Aborting: added the abandoned-after-kickoff honest note (or explicit acknowledgement if declined).
- CHANGELOG: new `### Added — GitHub ticket integration for implement-feature (2026-07-14)` block above
  the existing `### Changed` entry (not merged), bold lead sentence, purely-additive + honest-failure.

## "Left open" decisions
- **Attribution trailer wording:** defined once in Rule 8 and referenced everywhere as
  `<attribution trailer>`:
  `_🤖 Generated with the implement-feature skill — agent-authored, posted under the maintainer's
  authenticated gh account, not hand-written._` — models the repo's "🤖 Generated with Claude Code" /
  `Co-Authored-By` convention while staying honest that it's posted under the human's `gh` account.
- **Discipline-block placement:** one shared section before Phase 1 (see above), not a bottom appendix.
- **Closed-issue warning:** in the gate, "warn the user and ask before proceeding — never silently kick
  off work on a closed ticket."
- **Abandoned-run note:** short honest "this pass was abandoned" comment via `--body-file` + trailer, or
  explicit in-session acknowledgement if the user declines.

## Additive-preservation check
- Ticketless reading verified unchanged: Phase 9 step-4 ticketless next-steps bullet reproduces the
  original "review the branch, open a Pull Request on GitHub (or merge locally if no remote), use
  'Delete branch' there after merging, and clean up locally afterwards with:" text verbatim, and the
  closing "Do **not** open the PR yourself; the user reviews first." sentence stands verbatim as the
  first sentence before the ticket-only addition.
- All templates use generic `feature/<NN>-<slug>` / `<NN>` — no literal `06`.
- Used `$ARGUMENTS` throughout; never `$0`.

## Friction / surprises
- feature.md and the task spec live in the worktree (not the main checkout); initial reads with the
  main-checkout path 404'd. No other friction; the task spec's drafts were high-quality and mostly
  transcribable.

## Tests
- `npm run typecheck` — clean (tsc `--noEmit`, no output).
- `npm test` — 40 files passed / 1 skipped; 987 passed / 16 skipped. Matches the baseline exactly (no
  new failures). The `$ARGUMENTS`/`argument-hint` frontmatter additions parse cleanly — no
  skill-activation/substitution test regressed.
</content>

## Coordinator review-fix round (2026-07-14)
After the Phase 7 diff review fan-out (security/parity/generalist/user-experience), the coordinator
applied these fixes directly (prose only):
- Rule 9: extended the resume guard to the Phase 9 **summary comment** (was kickoff + PR only); a
  resumed run would otherwise double-post it. Also reworded the PR-reuse case to "post the summary as
  the ticket comment; leave the existing PR body untouched" — the old "refresh the summary on the PR"
  implied `gh pr edit`, outside the Rule 5 allow-list.
- Phase 8: clarified the write-preview is authored from `observations.md`/task logs (which exist at
  preview time; `review.md` distills the same sources later), is the exact bytes Phase 9 posts, and
  is re-confirmed if it changes — closes the preview-before-`review.md` ordering seam.
- Phase 2 step 2: added a local **Rule 4** reminder at the slug-creation step (never seed the slug
  from the raw issue title; ASCII only) — where a hostile title is most likely auto-slugified.
- Gate: labelled it **Phase 0** so the before-everything ordering is unmissable.
- `argument-hint` widened to `"[#N | N | issue-url]"` (was missing the bare `5` form).
- Intro block: defined multi-token `$ARGUMENTS` (`#5 also add logging`) — first token is the ref, the
  rest is ordinary Phase 1 direction.
- Write-failure degrade: added the `git push`-itself-rejected case (no branch to PR against → honest
  on-hold note if a kickoff was already posted).
Verified: no inline `--body`, no literal `06`, no `$0`; typecheck + full suite green.
