# t01 execution log

Authored by the coordinator directly (docs-only, precise wording grounded in the Phase 1
investigation the coordinator holds; subject to independent claude-parity + docs review of the diff).

## Sites corrected
- `src/registry/capability-registry.ts` — `tool.Agent` note: replaced the flat "verbatim final
  message" with the qualified contract (verbatim, EXCEPT a resumable dispatch appends a delimited
  in-band identity/resume trailer; Claude-faithful; TUI strips it; strict consumers must account).
  `tool.Task` (points at Agent) and `tool.TaskOutput` (already says identity is "outside the verbatim
  body") left unchanged — neither makes the false "no wrapper" claim.
- `doc/design/pi-integration.md` — §3.2 table row (`returned **verbatim**`) and §3.4 return-value
  clause (`**verbatim** (no wrapper)`) both qualified.
- `doc/architecture.md` — three sites: the subagent-runtime bullet (the crux "skills parse locked YAML
  … a hard contract" claim), the request-lifecycle step 6, and the error-contract "completed" bullet.
- `CHANGELOG.md` — new `### Fixed` entry under `[Unreleased]` (2026-07-16), framed as a
  docs-truthfulness fix / #46 downsize, behavior unchanged.
- `doc/supported-features.md` — regenerated via `npm run gen:capabilities` (1 line changed, the
  tool.Agent row); never hand-edited.

## Decisions on "Left open"
- Did NOT touch `CONTRIBUTING.md:54` ("verbatim subagent return" in a terse mechanics list) — the
  detailed contract lives in architecture.md/registry; qualifying a one-word list item would hurt
  readability without adding truth. (Flag for reviewers.)
- Left `tool.TaskOutput`/`tool.Task` registry entries as-is (already accurate).

## Verification
- `npm run typecheck`: clean.
- `npm run test:unit`: 1216 passed / 16 skipped — includes the capability-matrix **freshness guard**
  (`test/registry.test.ts`), so the regenerated matrix is confirmed in sync with the registry.
- Behavioral tests already encode the real contract (e.g. `test/subagent-transcripts.test.ts`
  "RESUMABLE agent = verbatim message + delimited ID trailer" / "no trailer for one-shot builtins") —
  no test changes needed.

## Review round (claude-parity + docs, on the diff)
Both reviewers independently caught one real miss + two truthfulness nits; all applied:
- **SHOULD-FIX** — `architecture.md` "**Verbatim subagent return.**" *principle* bullet (~:264) was
  missed by the initial sweep (a **case-sensitive** grep for `verbatim` skipped the capitalized
  heading). It made the strongest unqualified "no wrapping … skills parse it directly" claim and
  contradicted its own corrected sibling. Fixed with the same qualifier as `:113`.
- **NIT** — "the same resume handle" over-claimed *format* parity with Claude (Claude does it *in
  kind*). Softened to "the same **kind of** resume handle" in `CHANGELOG.md` + `pi-integration.md`.
- **NIT** — dropped "byte-exact" (over-precise: the background `TaskOutput` path appends a `usage:`
  line even for one-shot) in favour of "verbatim" at `architecture.md:113`, matching every other site.
- Confirmed leave-as-is (both reviewers agreed): `README.md:53`, `CONTRIBUTING.md:54`,
  `doc/user-guide.md` — terse/human-audience "verbatim" is genuinely true there (the TUI strips the
  trailer, so a human *does* see verbatim), and they're outside/below this task's contract altitude.

## Notes / surprises
- Fixed a stray soft-hyphen (`\xC2\xAD`) accidentally introduced in one architecture.md edit; swept
  the edited files clean afterward.
- Lesson: sweep for such claims **case-insensitively** — a titled principle ("**Verbatim** …") hides
  from a lowercase grep.
