# t02 execution log — wire NotebookRead + retier

## Wiring (src/index.ts)
- Import added at line 41: `import { createNotebookReadTool } from "./runtime/tools/notebook-tools.js";`
  (placed right after the search-tools import, near the other cwd-bound tool factories).
- `createNotebookReadTool(get)` added to the `buildCwdBoundTools` return array (line ~529),
  right after `createGlobTool(get)` and before the taskBundle spread — NOT into the
  `DEGRADED_TOOLS.map(...)` line. This provisions it to the main session and, gated by
  grant, to subagents via `customToolsFor`.
- `allKnownToolNames()` literal added between `"Glob"` and `"WebFetch"`:
  ```
  "Grep",
  "Glob",
  "NotebookRead",   // <-- added
  "WebFetch",
  ```
  Critical: without this literal, once NotebookRead leaves DEGRADED_TOOLS, `gateTools`
  treats a `tools: [NotebookRead]` grant / `NotebookRead(...)` rule as unknown and drops it.

## Permission switch (src/engine/permissions.ts)
- Added `case "NotebookRead":` to the `matchesRule` path-tool group so it routes to
  `pathSpecifierMatches`:
  ```
  case "NotebookEdit":
  case "NotebookRead":   // <-- added
  case "Glob":
  case "Grep":
    return pathSpecifierMatches(specifier, safeCall, pathOpts);
  ```
  Infra already present: `notebook_path` in CANONICAL_INPUT_FIELDS, pathSpecifierMatches
  reads `call.input["notebook_path"]`. Kept OUT of FILE_EDIT_TOOLS (read-only).

## Stub removal (src/runtime/tools/degrade-stubs.ts)
- Removed the `NotebookRead` entry from DEGRADED_TOOLS. NotebookEdit left untouched.

## Registry retier (src/registry/capability-registry.ts)
- Removed the degraded-noop `tool.NotebookRead` line from the degrade-stub block.
- Added the `partial` entry in the real-tool group, right after `tool.Glob` (near
  Read/Grep), one line, no newlines — the exact note string from the t02 spec.

## Matrix regenerated
- `npm run gen:capabilities` rewrote doc/supported-features.md (148 capabilities).
  The freshness guard (registry.test.ts) passes, confirming no hand-edit drift.

## Drift-guard test updates
- test/tools-parity.test.ts: removed "NotebookRead" from the degraded-names arrayContaining.
- test/registry.test.ts sync test: added positive assertions
  `lookupCapability("tool.NotebookRead")?.tier === "partial"` and
  `stubNames.has("NotebookRead") === false`.
- test/integration-extension.test.ts: added "NotebookRead" to the registered-surface list.
- test/registry.test.ts ~731: updated the now-stale "denying a degraded tool" comment to
  say NotebookRead is a real `partial` tool (assertion unchanged, still green).
- Did NOT add NotebookRead to the full-tools list (~183-193) — it's partial.

## New tests
- test/notebook-read-dispatch.test.ts (new file, mirrors slashcommand-fork.test.ts):
  "NotebookRead subagent-dispatch wiring (F18)" —
    - "provisions the main session with a real NotebookRead tool"
    - "grants a real NotebookRead tool to a subagent that inherits all tools"
  Dispatches future-agent (inherits all tools) foreground and asserts a NotebookRead
  customTool reached the created subagent session.
- test/permissions-hardening.test.ts: added
  "a path-scoped NotebookRead rule routes through pathSpecifierMatches (F18)" —
  NotebookRead(nb/**) matches notebook_path nb/a.ipynb, does NOT match other/a.ipynb.

## Proof the dispatch test guards the literal
- Temporarily removed the `"NotebookRead"` literal from allKnownToolNames and ran only
  test/notebook-read-dispatch.test.ts: the "grants a real NotebookRead tool to a subagent"
  case FAILED (no NotebookRead customTool reached the subagent — gateTools filtered the
  ungated name). Restored the literal; full suite green again.

## Results
- `npm run typecheck`: clean.
- `npm run test:unit`: 47 passed | 1 skipped files; 1088 passed | 16 skipped tests
  (baseline 1085 + 3 new). Freshness guard green.

## Surprising / notes
- future-agent (full-surface fixture) has no `tools:` frontmatter → inherits ALL tools,
  so it was the natural vehicle for the dispatch-wiring test (same one slashcommand-fork
  uses). No new fixture needed.
- No deviations from the spec.
