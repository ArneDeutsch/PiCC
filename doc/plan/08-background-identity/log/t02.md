- Read the feature/task specs and t01 execution log before editing; stayed within t02's writable surface.
- Left open: placed a concise dated identity item in Unreleased Changed and expanded the existing Observing subagents section rather than adding a new documentation section.
- Left open: used surface-specific capability notes so TaskStop pins every stop result, SendMessage pins new-task/same-agent resume correlation, and background-agents ties those to pushed settlement notices.
- Documented the compact `Task(task-N) · Agent(<type>) · agent-<id>` vocabulary without promising identical framing or exact Claude Code wording. Kept the displayed/requested-label caveat explicit because fallback or case-insensitive resolution can differ.
- Scoped the prior absolute “every background surface” prose to the start and TaskOutput views it actually enumerated; TaskOutput behavior and its capability entry remain unchanged.
- Recorded that the wording is model-visible while schemas, lifecycle/stop behavior, settlement delivery, structured results, framing, and limits are unchanged; retained TaskStop's PiCC-defined post-stop caveat and all existing capability tiers/parity gaps.
- `npm run gen:capabilities` succeeded: regenerated 148 capabilities. The generated diff changes only the three requested capability-note rows; totals remain 82 full, 14 partial, 43 degraded-noop, and 9 not-supported.
- Friction: the generator emitted the existing Windows LF-to-CRLF working-copy warning; freshness still passed in the full suite.
- Verification: `npm run typecheck` passed; `npm test` passed 42/42 files, 1026 tests passed and 1 skipped (1027 total).
- Deviations: none.
- Review fix: corrected the documented type sources: TaskStop/settlement use the background task record's stored display type; fresh records normally store the requested/display label, while resumed records and acknowledgments use the clean resolved registry name. Stable agent id is the practical cross-resume key and task id is the per-run key.
- Review fix: all three affected capability notes call the identity wording model-visible, PiCC-defined, and not verified as exact Claude wording. SendMessage separates Claude-supported resume/steer behavior from PiCC's acknowledgment wording.
- Review fix: replaced emphatic `NEW`/`SAME` wording with ordinary prose and strengthened semantic registry assertions for each type source plus all requested existing limitations.
- Review regeneration: `npm run gen:capabilities` wrote 148 capabilities; tiers/counts remain unchanged.
- Review verification: `npm run typecheck` passed; full `npm test` passed 42/42 files, 1026 tests passed and 1 skipped (1027 total).
- Review friction: `git diff --check` reported only the existing Windows LF-to-CRLF warning for the generated capability document; no whitespace errors.
- Close fix: recorded the exact record-based type-source behavior in the plan, release notes, and user guide; broader canonical-type plumbing remains deferred by maintainer decision. Kept the reviewed validated/bounded formatter unchanged and added it to the architecture module map.
- Close fix: shortened the three capability notes to lead with behavior and gaps, leaving detailed type-source comparisons in the user guide while retaining tiers and the PiCC-defined/not-exact-Claude wording caveat.
- Close fix: documented without runtime changes that PiCC TaskStop accepts only `task_id` (Claude 2.1.198+ also accepts agent id/name) and PiCC allows `SendMessage` resume after TaskStop (the Claude Code 2.1.x reference refuses stopped-agent resume). Registry tests now assert these durable semantics rather than exhaustive prose.
- Close-fix scope adjustment: added `doc/architecture.md` to the task record after close review judged the new shared formatter a contributor-facing runtime seam; no requested-vs-resolved plumbing changes, formatter simplification, or expanded examples.
- Close-fix generation: `npm run gen:capabilities` wrote 148 capabilities; totals remain 82 full, 14 partial, 43 degraded-noop, and 9 not-supported.
- Close-fix verification: focused registry tests passed 49/49; `npm run typecheck` passed; full `npm test` passed 42/42 files, 1026 tests passed and 1 skipped (1027 total).
- Close-fix friction: `git diff --check` reported only Windows LF-to-CRLF working-copy warnings; no whitespace errors.
