- Read the feature and task specs before implementation; stayed within the listed writable surface.
- Left open: used direct focused assertions rather than a shared test helper; chose an em-dash between the canonical tuple and each compact action/outcome suffix.
- Added a dependency-light identity formatter with fixed non-minted fallbacks, strict task/agent-id validation, terminal/control/format sanitization, reserved-delimiter encoding, minted-token neutralization, and an atomic 120-character type cap.
- Preserved the established type sources: background records use `agentType ?? agentName ?? "subagent"`; resume uses the registry's resolved `agentName`.
- Settlement retrieval guidance reuses the normalized task id; structured details and lifecycle mechanics were left unchanged.
- Requested-vs-resolved displayed-type mismatch remains intentionally deferred as specified.
- Friction: the first focused run exposed two test-expectation mistakes (BEL sanitizes to a separating space; the calculated maximum tuple length is 174); corrected the assertions without changing runtime behavior.
- Verification: `npm run typecheck` passed; focused identity/runtime suite passed 124/124; full `npm test` passed 1022 with 1 skipped (42 files).
- Review fix: strengthened cap coverage with inputs that place a surrogate pair and each reserved encoding (`%25`, `%28`, `%29`, `%C2%B7`) across the 120-character boundary; made the multi-escape middle-dot encoding atomic so `%C2` cannot survive alone.
- Review fix: extended the deterministic real-SessionManager resume test with one exact-case `reviewer` lifecycle record. Targeted TaskStop, settlement, and resume now correlate the same displayed type/stable agent id, while resume proves it mints a different task id.
- Review fix: added explicit output/transcript/path/diagnostic sentinels. TaskStop and resume acknowledgments disclose none; settlement retains only its contracted output/transcript values and omits unrelated path/diagnostic values.
- Security suggestion decision: retained legitimate natural-language displayed agent names. Structural delimiter encoding keeps them inside `Agent(...)`; a lexical prose filter would reject valid project agent names without reliably identifying attacks.
- Review verification: focused formatter/TaskStop/SendMessage suite passed 91/91; `npm run typecheck` passed; full `npm test` passed 1026 with 1 skipped (42 files).
- Final review tightened resume non-disclosure coverage to assert the acknowledgment omits the actual prior prompt and reply, not only synthetic unrelated sentinels.
