- Read feature.md, the task spec, the resident router, all six active reference files, current structural tests, contributor guidance, and the current changelog before editing.
- Left open: keep the existing phase-oriented organization; use `<feature-slug>` for repository identity, `<title>` for the stable public display title, and `<task-number>` / `<task-slug>` for task-local placeholders.
- Existing reference filenames differ from the task context's generic description, but the active bundle does contain exactly six references and all are in scope.
- Left open: organized the new contract around a resume-classification section plus Phase 1 identity authoring and Phase 2 validation/preflight; kept legacy-number prose narrowly confined to resume handling.
- Added CRLF-normalized, per-file structural checks rather than a concatenated bundle scan; forbidden-marker scans cover only the active skill and CONTRIBUTING, not historical plans/changelog.
- Targeted test initially had one assertion wording mismatch (`fail closed` vs `fails closed`); corrected the semantic assertion with no product change.
- No deviations from the task spec.
- Verification: final targeted `npx vitest run test/implement-feature-skill.test.ts` passed (1 file, 15 tests); `npm run typecheck` passed; final `npm test` passed (53 files, 1211 passed, 1 skipped).
- Friction: one full-suite run made concurrently with typecheck hit the pre-existing timing threshold in `hook-runner-parallel.test.ts` (3105 ms vs 2300 ms); that test passed alone (9/9), and the full suite then passed when rerun alone.
- Fix-mode review correction: distinguished absent-ref first push from an established self-owned exact-upstream repush on both maintainer and fork paths; resumed/CI-fix pushes require live-run knowledge or explicit resume confirmation, no case sibling, and equal/ancestor history, with foreign/diverged refs stopped and force forbidden.
- Fix-mode review correction: documented the complete EnterWorktree race honestly (unregistered-directory deletion, harness-branch adoption, seeding/hooks, misleading created result) and standardized observable-failure reporting as exact path/branch/status followed by a no-further-write stop.
- Fix-mode decision: disk-reconstructed resumes now have a public-write trust gate covering recovered scope, phase, identity, ticket target/reference, remaining write contract, and freshly resolved anchor/target agreement.
- Fix-mode decision: delimited a concrete legacy override using the current F20 forms and carried it through paths/headings/commits/branch/push/PR/CI/abort/cleanup; only that delimited section is excluded from obsolete new-run scans.
- Fix-mode review correction: freeze the stable display title at build go, restrict it to feature/review headings plus agent-created issue and PR titles, preserve given ticket titles, and print it in ticketless maintainer handoff.
- Fix-mode tests: localized race tuples, ownership checks on both push paths, quoted title command sites, concrete fork URL branch, all commit forms, robust ordering, legacy boundaries/forms, and retained GitHub/task-number markers.
- No deviations from the updated task spec; the coordinator-updated task spec itself was already modified on entry and was not touched.
- Fix-mode verification: targeted `npx vitest run test/implement-feature-skill.test.ts` passed (1 file, 17 tests); `npm run typecheck` passed; `npm test` passed (53 files, 1213 passed, 1 skipped).
- Close-review correction: descriptive resumes now validate the recovered frozen title, require exact feature/review heading agreement and exact plan/review commit-title agreement, allow task/fix commits by slug prefix, and show the title verbatim at the explicit trust gate.
- Close-review correction: maintainer fetch/base/upstream/push instructions use resolved `<pushRemote>`; literal `origin` is confined to the explicit no-`gh` git-only degrade, while fork routing remains unchanged.
- Close-review correction: collision replacements are re-authored, validated, fully rechecked, and fully reannounced with the residual simultaneous/disconnected race disclosure; pre-push collision stops now use the full “nothing is lost” recovery framing without force.
- Close-review decision: title authorship forbids direct copy/interpolation/mechanical transformation but does not reject incidental lexical overlap; ticket dedup, create, and synthesized cache all use the exact frozen `<Title>` byte-for-byte.
- Close-review tests: added localized assertions for resume title/heading/commit agreement, announcement order and no extra prompt, replacement reannouncement/race disclosure, pushRemote routing, pre-push recovery, exact Title seams, canonical issue references, case-insensitive obsolete scans, and the Unreleased changelog contract.
- Close-review friction: the added resident summary initially exceeded the re-injection cap; compressed the router while retaining full procedure in references. Structural tests also exposed line-break-sensitive assertions, which were changed to whitespace-tolerant matching.
- Close-review deviation: per coordinator instruction, did not run the manual PiCC smoke test.
- Close-review verification: targeted `npx vitest run test/implement-feature-skill.test.ts` passed (1 file, 20 tests); `npm run typecheck` passed; `npm test` passed (53 files, 1216 passed, 1 skipped).
- Manual smoke observation (coordinator-run): a real Windows Git Bash + PiCC print-mode ticketless run reached Phase 3 with the intended descriptive slug, matching worktree and `feature/<feature-slug>` branch, and matching plan heading; after the user's `go`, however, it invoked tools before showing the title/slug/branch/plan and residual-race announcement as user-visible prose. The dispatch did not provide the disposable artifact's literal names or cleanup status; integration/public-write paths remained unverified here.
- Smoke correction: made the post-go announcement a hard presentation gate before every Phase 2 tool call, fixed field order to title → slug → branch → plan → race disclosure, explicitly allowed tools later in the same response without waiting for a reply, and added localized router/workflow structural assertions for wording and order.
- Smoke-fix verification: targeted `npx vitest run test/implement-feature-skill.test.ts` passed (1 file, 20 tests); `npm run typecheck` passed; `npm test` passed (53 files, 1216 passed, 1 skipped).
- Successful second manual smoke (coordinator-run): Windows 11, Git Bash, PiCC print mode, `gpt-5.4-mini`, ticketless with no remote or GitHub writes. After `go`, PiCC first read the required workflow/template references, then visibly announced `Title: Smoke Greeting Artifact`, `Slug: smoke-greeting-artifact`, `Branch: feature/smoke-greeting-artifact`, `Plan: doc/plan/smoke-greeting-artifact/`, and the residual-race disclosure before the first workspace/preflight command and `EnterWorktree`; no further reply was requested.
- Second-smoke artifacts were exact: worktree `smoke-greeting-artifact`, branch `feature/smoke-greeting-artifact`, plan `doc/plan/smoke-greeting-artifact/`, and heading `# smoke-greeting-artifact: Smoke Greeting Artifact`. The run stopped after Phase 3; its disposable worktree and refs were subsequently cleaned up. Handoff, fork, and resume paths remain unverified by this smoke.
- Final smoke-alignment decision: required Phase 2 reference reads are the sole allowed pre-announcement tools; the user-visible announcement gates all workspace/preflight/mutating commands and `EnterWorktree`, needs no reply, and may share its response with later tools.
- Final smoke-alignment verification: targeted `npx vitest run test/implement-feature-skill.test.ts` passed (1 file, 20 tests); `npm run typecheck` passed; `npm test` passed (53 files, 1216 passed, 1 skipped).
