# t03 — Registry + docs truthfulness (execution log)

Docs-and-registry-wording only; no runtime change. Full suite green at the t02
baseline (1038 passed / 16 skipped); typecheck clean.

## Registry (`src/registry/capability-registry.ts`)

- **`tool.Agent`** — was: `PARTIAL: PiCC defaults FOREGROUND whereas Claude 2.1.198
  runs subagents background-by-default … runs serially`. Now: dispatch is
  `BACKGROUND-BY-DEFAULT (Claude 2.1.198)` — omitted `run_in_background` returns a
  task id, implicit fan-out parallelizes; `run_in_background:false` = synchronous
  inline; env forces every dispatch foreground. Stays **partial** on a renamed
  residual: settlement pushed to an idle parent **next turn** vs Claude mid-turn
  (web-verified). Two residuals explicitly **labelled INFERRED** (not documented
  Claude semantics): no `agent_completed`-style Notification for in-session
  settlement (conservative under-claim), and env-over-`background:true`-frontmatter
  precedence.
- **`tool.Task`** — was: `same default-foreground divergence`. Now: `same
  background-by-default routing (Claude 2.1.198) with the next-turn-settlement timing
  residual`.
- **`feature.background-agents`** — removed the lead GAP `PiCC defaults foreground
  unlike Claude 2.1.198`; lead reworded to `background-by-default dispatch`; the
  timing gap now stated as the residual. All other gaps kept verbatim (TaskStop
  task_id-only, stopped-agent resume, idle-parent next-turn, no Agent View, no
  remote/cloud, cooperative stop).
- **`agent.frontmatter.background`** — was: `dispatches … without run_in_background`.
  Now: `forces background dispatch even against an explicit run_in_background:false
  (its remaining significance now that dispatch is background-by-default)`.
- Ran `npm run gen:capabilities` → `doc/supported-features.md` regenerated (148
  capabilities). Diff = exactly the 4 registry rows above; freshness guard
  (`registry.test.ts:420-431`) green. Never hand-edited.

## Prose living docs

- **README.md** (~57-61) — "Dispatch defaults to the **foreground** … runs serially
  unless background is requested" → "runs in the **background by default** … fan-out
  parallelizes; each dispatch returns a task id collected via `TaskOutput`/`TaskStop`;
  `run_in_background: false` for a synchronous inline result; env forces foreground".
- **doc/user-guide.md**
  - dispatch bullet (~168) — inverted `run_in_background: true returns a task id` to
    background-by-default; added `false` synchronous path, `background: true` forcing,
    and env forcing foreground.
  - env table row (~299) — "`run_in_background` dispatches run foreground instead" →
    "Force **every** `Agent`/`Task` dispatch foreground (background is otherwise the
    default); `SendMessage` resume is inherently async and is **not** governed by this
    switch" (the documented async exception).
  - Partial section (~339) — struck "PiCC defaults foreground … runs serially"; now
    "background subagent dispatch — now the **default**", residual = settlement timing;
    added the nested per-depth-bound divergence sentence.
- **doc/architecture.md**
  - `background-tasks.ts` bullet (~125) and flow step 6 (~214) — reframed
    "`run_in_background: true` registers …" to "registers by default (`false` opts
    into a blocking foreground run)"; env "falls back to foreground" → "forces every
    dispatch to the foreground".
  - error-contract "default direction diverges" paragraph (~262) — rewritten: the
    default now **matches** Claude; the F15 default-direction gap is **closed**;
    residual = settlement timing.
  - **Added** a new §4 top-level bullet "Nested background fan-out is
    concurrency-bounded (F15 t02)": per-depth budgets, total ≤ `maxDepth ×
    concurrency`, deadlock-free (monotone depth on wait edges); **divergence** from
    Claude's single global ~10 cap stated as a conservative PiCC choice, not parity;
    **bounded-wait** not infinite parallelism; and the foreground-nested bypass noted
    as a behavioural choice (deadlock-free either way), per the t02 observation.
- **doc/design/pi-integration.md** (~77) — **correct vs supersede decision: corrected**
  in place. Rationale: pi-integration.md is a *living* design reference (architecture
  cites its §3.1/§3.4 as current), and the sentence was a point-in-time "v1" claim now
  simply false, not a dated record. A superseded-note would leave the wrong assertion
  standing; correcting keeps the doc truthful. "(background degrades to foreground in
  v1, noted in result)" → "(dispatch is **background-by-default** since F15 — omitted
  returns a task id; `false` = synchronous foreground; env forces foreground)".
- **CHANGELOG.md** — added `### Changed — background-by-default subagent dispatch
  (2026-07-14)` at the top of `[Unreleased]` (matches the dated-subsection format):
  default flip + implicit fan-out parallelizes, `run_in_background: false` synchronous,
  env forces foreground, nested per-depth bound + divergence, registry retiering +
  regenerated matrix. Prior dated CHANGELOG entry `### Changed — registry truthfulness
  (2026-07-13)` (line 180, still says "defaults foreground") left intact — a
  point-in-time record; rewriting it would falsify history (Keep-a-Changelog
  convention). My new entry supersedes it.

## Tests (`test/registry.test.ts`)

- Rewrote the stale prose comment (~195) and the `tool.Agent`/`tool.Task` assertion
  to test the **new** meaning, not surviving substrings: asserts
  `BACKGROUND-BY-DEFAULT`, `run_in_background:false`, `next turn` (the residual),
  `2.1.198`; Task asserts `alias` + `background-by-default`. (A bare
  `.toContain("foreground")` would be vacuous post-flip — the note still contains
  "foreground" in "forces every dispatch foreground".)
- `feature.background-agents` test (~297) — removed `PiCC defaults foreground` from
  the gap loop; added a positive `toContain("background-by-default")` and a
  `not.toContain("PiCC defaults foreground")` guard so the flip can't silently regress.

## Friction / notes

- No runtime or t01/t02-test changes were needed; the only test edits were the two
  wording assertions in the writable surface.
- `agent.frontmatter.background` test (~276) asserts `toContain("background: true")` —
  the reworded note still begins "background: true forces …", so it stayed green
  without editing that test.

## Close-review fixes

Three feature-close polish fixes; no runtime behaviour change. Gates after: typecheck
clean; full `npm test` = **1038 passed / 16 skipped** (baseline); `gen:capabilities`
idempotent, matrix diff = exactly the one `feature.background-agents` row.

1. **Registry truthfulness — t02 nested model now machine-readable.**
   `src/registry/capability-registry.ts` `feature.background-agents` note: inserted a
   clause after "stop is cooperative." — "Nested (depth ≥ 2) background fan-out is
   bounded by per-depth budgets (total ≤ maxDepth × concurrency, deadlock-free), a
   deliberate conservative PiCC choice that diverges from Claude's single global (~10)
   parallel-agent cap — not parity." Regenerated `doc/supported-features.md` (never
   hand-edited). Extended the `registry.test.ts` `feature.background-agents` test with
   three positive assertions on the new clause ("per-depth budgets",
   "maxDepth × concurrency", "Claude's single global (~10) parallel-agent cap") so the
   nested-model claim can't silently regress — not just surviving old substrings.

2. **Stale JSDoc on `isBackgroundAgent`** (`src/runtime/subagents.ts` ~512-517):
   reworded the significance of `background: true` from "must run in the background even
   when the Agent tool call omits `run_in_background`" (wrong distinguishing case
   post-flip — omitting already backgrounds) to "runs in the background even against an
   explicit `run_in_background: false` … its remaining significance now that dispatch is
   background-by-default", mirroring the registry's `agent.frontmatter.background` note.
   Comment only.

3. **/usage assertion on the omitted-flag default path** (`test/background-tasks.test.ts`,
   the "DEFAULT (no run_in_background) … settles + drains" test). Extended `gatedSdk` with
   an optional `stats?: PiSessionStats` param (passed through to `fakeSdk`) so the default
   dispatch reports usage; scripted `{ tokens: { input: 11, output: 7 }, cost: 0.002 }`.
   With usage present, the completed-path TaskOutput `content` appends the compact usage
   line, so the existing verbatim `.toBe("DEFAULT-PATH-VERBATIM")` became
   `.toBe("DEFAULT-PATH-VERBATIM\nusage: in 11 · out 7 · $0.002")` (still exact — proves
   the body is verbatim AND usage surfaces) plus a new
   `expect(out.details.usage).toEqual({ inputTokens: 11, outputTokens: 7, costUsd: 0.002 })`.
   This mirrors `subagent-usage.test.ts`'s background-usage read (`details.usage` +
   compact line) but on the omitted-flag default path rather than `run_in_background: true`.
   Not redundant with the existing background usage test (that one uses the explicit flag).
   Touched the adjacent "text IS the verbatim result" comment to say "verbatim result plus
   the appended usage line".
