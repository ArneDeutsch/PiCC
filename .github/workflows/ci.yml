name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: test (${{ matrix.os }}, node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        # Node 20 is not supported: Pi bundles undici 8.x (engines >=22.19),
        # whose worker_threads.markAsUncloneable dependency does not exist on 20.
        node: [22, 24]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Install
        run: npm ci

      - name: Typecheck
        run: npm run typecheck:all

      - name: Unit tests
        run: npm run test:unit

      - name: Offline integration tests
        run: npm run test:integration

  e2e:
    name: e2e (real Pi CLI + mock model, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci

      # Drives the real Pi CLI with local model traffic and no subscription.
      # The packaged-launcher consumer install may access the npm registry.
      - name: End-to-end tests
        run: npm run test:e2e
